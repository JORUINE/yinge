# 音格 · 系统设计文档

> 项目名称：音格——音乐专辑对决与人格测评互动平台

> 文档类型：系统设计文档（毕业设计提交材料之一）

> 编制人：胡祖锐　　学号：9920250499

> 学院／专业：新媒体学院 · 数字媒体技术（2025级专升本）

> 版本：v1.0　　编制日期：2026 年 9 月 16 日

[TOC]

## 一、文档说明

### 1.1 编写目的

本文档承接《音格 · 需求分析文档》，把"系统要做什么"转化为"系统怎么做"。文档给出系统总体架构、功能模块划分、专辑对决的完整赛制规则、数据库表结构与字段定义、接口组织方式、关键业务流程、安全措施与部署方案，是编码实现、测试验收与论文第三、四章写作的直接依据。

### 1.2 设计依据

（1）《音格 · 需求分析文档》中的 30 条功能需求（FR-01 至 FR-30）与 6 类非功能需求（性能、安全、可用性、兼容性、可维护性、数据准确性）；

（2）毕业设计规范中"赛道 C 全栈设计与开发"方向对前端完整性、后端完整性、数据驱动与云端部署的四项要求；

（3）对所选外部音乐数据接口的实测结论。本系统的数据源不是任意可选的，以下几项均由实测确定，属于设计的硬约束：

| 实测结论 | 对设计的影响 |
| --- | --- |
| 外部接口无需密钥、无需登录、不受访问风控 | 可以作为唯一数据源，允许公开部署 |
| 接口直接提供 30 秒试听片段地址 | 系统不需要自建音频存储与分发，不需要音频处理 |
| 检索接口的地区参数取 `cn` 时对大量华语歌手无收录，且无结果时可能返回不相关歌手 | 地区参数不得使用 `cn`，且服务端必须做检索结果相关性校验 |
| 按歌手标识查询曲目时，歌曲均携带试听地址；但按专辑标识查询曲目不会展开曲目列表 | 曲目数据必须按歌手标识拉取后再按专辑标识分组落库 |
| 同名不同版本的专辑归属不能用名称比对 | 专辑归属必须按歌手唯一标识比对 |

### 1.3 读者对象

- 指导教师与答辩评审：核查架构合理性与设计完整性；
- 开发者（本人）：作为编码与测试的依据；
- 后续维护者：理解各模块职责、字段含义与关键约束。

### 1.4 与需求文档的对应关系

本文档第四章（赛制设计）对应需求文档模块四与模块九；第五章（数据库设计）对应需求文档第七章数据需求；第六章（接口设计）的完整清单另见《音格 · API 接口文档》；第七章（关键流程）对应需求文档的可用性与准确性要求；第八章（安全设计）对应需求文档 6.2 节。

## 二、系统总体设计

### 2.1 设计原则

**（1）前后端分离。** 前端只负责渲染与交互，全部业务规则、数据校验与权限判断集中在服务端。这样做的直接收益是：同一套接口可以同时支撑网页端与移动端，且规则变更不需要重新发布前端。

**（2）外部依赖必须可降级。** 音乐数据来自外部接口，外部接口随时可能因网络、频率限制或服务变更而不可用。因此所有外部数据首次获取后立即落库，后续优先读本地；外部不可用时系统仍能基于已有缓存完整运行。

**（3）数据只读不落版权内容。** 系统仅存储元数据（名称、封面地址、发行日期、曲目数）与 30 秒试听片段地址，不存储也不转存任何完整音频文件。

**（4）计算结果一次性落库。** 对决的对阵表、每组战绩、每场比分、冠军路径都在服务端计算后写入数据库，前端只做展示。避免"前端算结果"导致不同端看到不同数据。

**（5）规则可解释。** 专辑为什么要被过滤掉、某场为什么判这张赢、某个账号为什么被判定为异常投票，规则本身要对用户与管理员双向可见。这是本系统与"黑箱推荐"类产品在体验上的关键差别。

### 2.2 系统总体架构

系统采用四层结构，自上而下为表现层、接口层、业务层、数据层，右侧为外部依赖。

```
┌──────────────────────────────────────────────────────────────┐
│  表现层（浏览器 · 响应式）                                      │
│  前台 13 页：首页 / 登录 / 个人中心 / 对决四页 / 分享图          │
│             测评三页 / 人格图鉴 / 排行榜                        │
│  后台 6 页：后台登录 / 数据看板 / 题目管理 / 类型管理            │
│             音乐数据管理 / 用户管理                             │
│  技术：Vue 3 + Vite + Pinia + Vue Router + Element Plus        │
└────────────────────────────┬─────────────────────────────────┘
                             │ HTTPS · JSON · Bearer 令牌
┌────────────────────────────▼─────────────────────────────────┐
│  接口层（Express 路由 + 中间件）                                │
│  路由分组：认证 / 音乐数据 / 对决 / 测评 / 收藏与分享            │
│            / 排行榜 / 后台管理                                  │
│  中间件：统一响应包装 · 令牌校验 · 角色校验 · 入参校验           │
│           · 频率限制 · 异常捕获                                 │
└────────────────────────────┬─────────────────────────────────┘
                             │
┌────────────────────────────▼─────────────────────────────────┐
│  业务层（服务模块）                                             │
│  账号服务 · 音乐数据服务（抓取/过滤/缓存）                        │
│  赛制服务（分组/对阵/轮次/胜负/复活/夺冠路径）                    │
│  投票服务（校验/防刷/统计）                                      │
│  测评服务（计分/类型映射） · AI 解读服务（提示词/降级/缓存）       │
│  分享图服务 · 榜单服务 · 后台服务                                │
└────────────────────────────┬─────────────────────────────────┘
                             │ Mongoose ODM
┌────────────────────────────▼─────────────────────────────────┐
│  数据层（MongoDB）                                              │
│  四个数据域 · 12 张集合                                         │
│  用户域 3 张 / 音乐数据域 3 张 / 对决域 3 张 / 测评域 3 张        │
└──────────────────────────────────────────────────────────────┘

外部依赖：音乐数据接口（元数据 + 30 秒试听地址）
          大语言模型接口（人格解读文本生成）
          对象存储或本地静态目录（分享图文件）
```

### 2.3 部署视图

系统按"静态资源 + 应用服务 + 数据库"三段式部署，三段分别落在不同的云服务上，互不影响。

| 组成部分 | 部署位置 | 说明 |
| --- | --- | --- |
| 前端静态资源 | 静态站点托管平台 | 构建产物直接托管，自动 HTTPS 与全球加速 |
| 后端应用服务 | 云应用托管平台 | Node 服务常驻，通过环境变量注入配置 |
| 数据库 | 云数据库服务 | 免费额度足够演示规模，白名单限制来源 |
| 分享图文件 | 后端静态目录 | 演示阶段存于服务本地目录，由后端直接对外提供 |

### 2.4 技术选型与理由

| 层次 | 选型 | 选择理由 |
| --- | --- | --- |
| 前端框架 | Vue 3（组合式） | 上手成本低，单文件组件组织清晰，学习资料充足 |
| 构建工具 | Vite | 冷启动快，配置简单，构建产物体积小 |
| 状态管理 | Pinia | Vue 3 官方推荐，写法比旧方案更简洁 |
| 路由 | Vue Router | 官方方案，支持路由守卫做登录拦截 |
| 组件库 | Element Plus | 表格、分页、表单、弹窗等后台常用组件完备，可节省大量样式工作 |
| 图表 | ECharts | 对阵图、得分雷达图、占比图可统一用一套库完成 |
| 分享图 | html2canvas | 直接把页面节点渲染为图片，不需要服务端图形环境 |
| 后端框架 | Node.js + Express | 与前端同语言，中间件生态成熟，接口写法直观 |
| 数据访问 | Mongoose | 提供模式定义与字段校验，能在写入前拦住非法数据 |
| 数据库 | MongoDB | 文档结构与音乐元数据这种嵌套结构天然契合，免去多表关联 |
| 认证 | JWT + bcrypt | 令牌无状态便于前后端分离；密码单向加密存储，不保存明文 |
| 大模型 | 通义千问接口 | 中文表达质量好，有免费额度，接入方式简单 |
| 版本管理 | Git + GitHub | 规范要求提交源代码仓库，且提交记录本身是过程证据 |

选型的统一标准是**降低实现风险**：优先选择资料多、社区大、与前端同语言的方案，避免在毕业设计周期内引入需要额外学习成本的组件。

### 2.5 工程目录结构

```
yinge/
├─ docs/                     文档与图表（需求、设计、接口、设计稿）
│  ├─ design/                19 页 UI 设计稿与图片资源
│  └─ figures/               架构图、ER 图、赛程流程图
├─ frontend/                 前端工程
│  ├─ src/
│  │  ├─ api/                接口封装（按业务域分文件）
│  │  ├─ assets/             静态资源
│  │  ├─ components/         通用组件（专辑卡、专辑架、播放条、空状态）
│  │  ├─ router/             路由与守卫
│  │  ├─ stores/             Pinia 状态（用户、对决、主题）
│  │  ├─ utils/              请求封装、格式化、分享图导出
│  │  └─ views/              页面
│  │     ├─ front/           前台 13 页
│  │     └─ admin/           后台 6 页
│  └─ vite.config.js
├─ backend/                  后端工程
│  ├─ src/
│  │  ├─ config/             配置读取
│  │  ├─ middlewares/        令牌、角色、校验、限流、异常
│  │  ├─ models/             12 个数据模型
│  │  ├─ routes/             路由分组
│  │  ├─ services/           业务服务（赛制、抓取、测评、AI、分享图）
│  │  └─ utils/              响应封装、日志、缓存时效计算
│  └─ app.js
└─ README.md
```

## 三、功能模块设计

系统共 14 个功能模块，前台 9 个、后台 5 个。

### 3.1 前台功能模块

**模块一　首页与导航**

负责平台定位表达、两种玩法入口、榜单概览与近期热门专辑展示，并提供全局导航。首页承担"让用户在 10 秒内明白这个平台是干什么的"这一职责，因此首屏必须同时出现专辑架形式的内容与两个玩法入口，而不是大段文字介绍。

**模块二　用户与账号**

负责注册、登录、登录态维持、退出、个人中心与收藏。个人中心汇总"我的对决""我的测评""我的收藏"三类个人数据。密码使用单向加密存储，服务端不保留明文。

**模块三　音乐数据浏览**

负责歌手检索、专辑列表与专辑详情的展示，并维护本地缓存与过期更新。本模块是全部玩法的数据基础，其余模块不直接访问外部接口，一律通过本模块获取数据。

**模块四　专辑对决**

系统核心模块。包含五种参赛范围模式、专辑准入过滤、对阵生成、逐场投票、轮次推进（小组赛 / 复活赛 / 淘汰赛）、冠军生成与结果汇总。完整规则见第四章。

**模块五　试听播放**

负责播放专辑的 30 秒试听片段、播放状态管理与多实例互斥控制。同一时刻只允许一个片段在播放，切换播放对象时自动停止上一个。无试听资源时给出明确提示而不是静默失败。

**模块六　音乐人格测评**

负责测评说明、12 道题的作答流程（含至少 2 道听感题）、按选项累加维度得分、由得分分布映射人格类型、以及结果展示。

**模块七　AI 个性化解读**

负责构造提示词、调用大模型生成个性化解读文本、超时与失败时降级为模板文案、以及对结果做缓存以避免重复计费。

**模块八　结果可视化与分享**

负责对阵图与夺冠路径图绘制、人格卡绘制、导出为图片、以及分享记录入库。分享记录入库的目的是让用户日后能找回自己生成的分享图。

**模块九　排行榜与人格图鉴**

负责全球最受欢迎专辑榜（按累计票数排序、支持范围筛选与分页），以及人格图鉴（全部人格类型及其占比分布）。

### 3.2 后台功能模块

**模块十　数据看板**　展示用户数、对决次数、投票总数、测评完成数、人格类型分布等核心指标。

**模块十一　测评题目管理**　题目与选项的新增、修改、删除与排序，以及每题选项对应分值的维护。计分模型因此可以不改代码调整。

**模块十二　人格类型管理**　人格类型的类型码、名称、描述与推荐专辑的维护。

**模块十三　音乐数据管理**　查看已缓存的歌手与专辑数据、手动刷新指定歌手、清理失效数据。

**模块十四　用户管理**　查看用户列表、按条件检索用户、禁用违规账号。只做禁用不做物理删除，保证数据可追溯、误判可恢复。

### 3.3 模块、页面与接口的对应关系

| 模块 | 对应页面 | 主要接口分组 |
| --- | --- | --- |
| 首页与导航 | 1 首页 | 首页聚合接口、榜单接口 |
| 用户与账号 | 2 登录注册、3 个人中心 | 认证接口、收藏接口 |
| 音乐数据浏览 | 4 选择范围（检索部分） | 音乐数据接口 |
| 专辑对决 | 4 选择范围、5 对阵表、6 投票、7 冠军结果 | 对决接口 |
| 试听播放 | 6 投票（内嵌） | 音乐数据接口返回的试听地址 |
| 音乐人格测评 | 9 说明、10 答题、11 人格卡 | 测评接口 |
| AI 个性化解读 | 11 人格卡（内嵌） | 测评接口的解读字段 |
| 结果可视化与分享 | 8 分享图、11 人格卡（导出） | 分享接口 |
| 排行榜与人格图鉴 | 12 人格图鉴、13 排行榜 | 榜单接口 |
| 后台五模块 | 14 至 19 | 后台管理接口 |

## 四、专辑对决赛制设计

本章是系统的业务核心，也是答辩中最容易被追问的部分，因此把每一项规则的判定条件都写清。

### 4.1 参赛范围：五种模式

用户在创建对决时先选择范围模式，再确定具体参赛对象。

| 模式 | 标识 | 用户操作 | 说明 |
| --- | --- | --- | --- |
| 单歌手 | `artist` | 选 1 位歌手 | 该歌手的正式专辑进入参赛池，适合"这位歌手哪张最好"的问题 |
| 多歌手混战 | `multi-artist` | 逐位添加 2 至 6 位歌手，可分别设置每人抽取张数 | 多位歌手的专辑进入同一池后混合配对 |
| 按流派或年代 | `genre` / `era` | 选流派或年代区间 | 自动汇集符合条件歌手的专辑 |
| 手动挑选 | `custom` | 逐张选专辑 | 完全自定义参赛名单 |
| **对位赛** | `aligned` | 选 2 至 4 位歌手，按发行顺序逐张对位 | 第 1 张打第 1 张、第 2 张打第 2 张，见 4.2 |

其中"多歌手混战"是本平台区别于普通投票工具的关键：它让不同歌手的作品进入同一个比较场域，直接回答"跨歌手比较谁更强"这一日常乐迷最常争论、却长期缺少工具去回答的问题。

> **术语约定（供论文第三、四章论述数据流时引用）。** 本平台存在三种容易混淆的"分数"概念，须严格区分：
> - **胜场**：对决类玩法（小组赛、对位赛）的核心度量。来自 `battle_matches` 集合的每场胜负记录，服务端按专辑或歌手聚合得出"胜场数"，决定晋级与优胜。
> - **积分**：仅用于**对位赛**，即"胜场积分"——每胜一场得 1 分，累计定优胜。小组赛不另设独立积分字段。
> - **票数**：仅用于**排行榜**（`votes` 集合），按专辑被投的累计票数排序，与胜场、积分均无直接换算关系。
>
> 一言以蔽之：**对决拼胜场，榜单拼票数，积分只留给对位赛。** 前端展示时，小组赛战绩表标题统一为"小组战绩"，不得出现"积分"字样，以免与对位赛积分混淆。

### 4.2 对位赛模式

**要解决的问题。** 混战模式回答的是"这堆专辑里哪张最好"，但乐迷还有另一类非常具体的比较需求：两位歌手**同阶段的作品**到底谁更强。例如"陶喆的首张专辑和周杰伦的首张专辑，谁更经典"、"两位歌手的第二张专辑哪张更好"。这类比较在混战模式下无法表达，因为混战会打乱专辑序号，首张专辑可能对上对方的第五张，比较失去意义。

**玩法定义。** 用户选定 2 至 4 位歌手，系统对每位歌手按发行日期升序排列其正式专辑，取前 N 张（N 由用户指定，上限取各歌手专辑数的较小值）。然后把**序号相同的专辑放在同一组**，组内两两配对对战。

**场次计算。** 设歌手数为 A、对位张数为 N，则：

| 歌手数 A | 组内配对数 | 总场次 | 示例 |
| --- | --- | --- | --- |
| 2 | 1 | N | 2 位歌手各取 5 张 → 5 场 |
| 3 | 3 | 3N | 3 位歌手各取 5 张 → 15 场 |
| 4 | 6 | 6N | 4 位歌手各取 3 张 → 18 场 |

**胜负判定。** 对位赛不使用淘汰制，改用**胜场积分**：每场胜者得 1 分，全部场次结束后按歌手累计胜场排序，胜场最多者获得"对位赛优胜"。若出现并列，依次比较：总得票数、单场最大分差、序号靠前的组胜场数。

**为什么不用淘汰制。** 对位赛的比较单位是"同序号专辑"，如果把败者淘汰掉，第 3 张专辑的胜者就没有第 4 张的对手可比，赛程会断裂。用积分制可以让每一组都完整比完，用户得到的是一张"逐张对照表"，信息量远大于一个冠军名次。

**展示形式。** 结果页呈现为一张逐行对照表，每行是一组对位：

| 序号 | 陶喆 | 比分 | 周杰伦 | 胜者 |
| --- | --- | --- | --- | --- |
| 第 1 张 | David Tao（1997） | 14 : 6 | Jay（2000） | 陶喆 |
| 第 2 张 | I'm OK（1999） | 9 : 11 | 范特西（2001） | 周杰伦 |
| 第 3 张 | 黑色柳丁（2002） | 12 : 8 | 八度空间（2002） | 陶喆 |

这张表本身就是可分享的内容——它比一个"冠军"更接近乐迷真正想讨论的东西。

**实现要点。** 对位赛不需要新增数据表，也不需要改动数据库结构：复用对战场次集合的 `roundIndex` 字段存放对位序号（第 k 张），`scopeType` 取 `aligned`，`scopeKey` 存放参与歌手的标识串，歌手的抽取张数沿用多歌手模式已有的 `artists` 数组结构。这是把它设计成"范围模式"而不是"独立玩法"的原因——**玩法差异在赛制服务内部消化，数据层与接口层保持统一**。

### 4.3 专辑准入过滤规则

外部接口返回的"专辑"包含大量非正式发行物，直接参赛会严重影响体验（用户会看到单曲、精选集、 soundtrack 混在里面）。因此在进入参赛池之前统一过滤，七条规则如下：

| 序号 | 规则 | 判定方式 | 过滤原因 |
| --- | --- | --- | --- |
| 1 | 类型与体量 | 返回类型必须为专辑，且曲目数不少于 7 首 | 排除单曲与 EP，其体量与专辑不对等 |
| 2 | 署名为该歌手本人 | 专辑的歌手标识等于所选歌手的标识 | 排除他人作品与拼盘；**必须按标识比对，不可按名称比对** |
| 3 | 非现场专辑 | 名称命中现场类关键词则剔除 | 现场版与录音室版不可比 |
| 4 | 非影视原声 | 名称命中原声类关键词则剔除 | 原声带不体现歌手本人的创作意图 |
| 5 | 非精选集 | 名称命中精选类关键词则剔除 | 精选集是已有作品的重复集合 |
| 6 | 非合辑拼盘 | 名称含多歌手连接符则剔除 | 合辑无法归属到单一歌手 |
| 7 | 同名去重 | 同名专辑只保留最早发行、且不带豪华版后缀的版本 | 避免同一张专辑以多个版本重复参赛 |

其中规则 3 至 6 通过统一的名称关键词匹配实现，关键词集合集中配置，可在后台维护。

**两点必须做到：**

**其一，过滤结果对用户可见。** 系统在参赛池预览处明确展示"共检索到 X 张，剔除 Y 张，实际参赛 Z 张"，并列出每一张被剔除专辑的名称与命中规则。用户如果有异议，可以手动把某张加回参赛池。这项设计对应需求 NFR-D5"结果可复核"，目的是让用户信任这个名单，而不是怀疑系统漏掉了他的爱碟。

**其二，归属校验按标识而非名称。** 同一位歌手在不同地区返回的名称可能存在繁简体或写法差异，若按名称比对会出现"同一歌手的专辑被判为不属于他"的错误。因此归属比对的依据只能是歌手唯一标识。

### 4.4 分组规则

参赛专辑确定后进入分组阶段，规则如下：

（1）每 3 张专辑编为一组，按专辑总数向上取整确定组数；

（2）**同一歌手的多张专辑不得落在同一组**。若某歌手专辑数较多，采用轮转方式分散到各组，让它在小组赛阶段就与其他歌手的作品交手；

（3）某组不足 3 张时，该组按实际数量进行组内单循环，不做补齐；

（4）分组结果在生成后固定写入数据库，不随刷新变化。这一点很重要——如果每次刷新都重新分组，用户看到的小组构成会变，投票的依据就不成立了。

以 12 张专辑为例：分为 4 组，每组 3 张，且每组横跨 3 位不同歌手。

### 4.5 轮次编排与场次计算

完整赛程由三部分组成：小组赛、复活赛（可选）、淘汰赛。

**小组赛。** 每组内部单循环，即组内任意两张专辑恰好交手一次。3 张专辑的组产生 3 场（3 取 2 的组合数为 3）。组内按胜场数排序，胜场相同则比较总得票数，决出每组第一名。

**复活赛。** 各组第二名共计 4 张专辑进入复活赛，两两配对，产生 2 场，胜者补足淘汰赛名额。

**淘汰赛。** 4 强进行半决赛 2 场，胜者进入决赛 1 场，决出冠军。如果启用复活赛，淘汰赛参赛者由"各组第一 + 复活赛胜者"共同构成。

**场次数量对照表（以 12 张专辑为例）：**

| 阶段 | 场次 | 参赛对象 | 晋级结果 |
| --- | --- | --- | --- |
| 小组赛 | 12 场 | 12 张专辑，4 组各 3 张 | 各组第一名 → 4 强 |
| 复活赛（可选） | 2 场 | 各组第二名共 4 张 | 胜者 2 张补入 4 强 |
| 半决赛 | 2 场 | 4 强两两配对 | 胜者 2 张 |
| 决赛 | 1 场 | 2 张 | 冠军 |
| **合计** | **15 场**（不含复活赛） | | |

场次数由参赛数与赛制唯一确定，系统在生成对阵表时必须与上表一致。决赛结束后结果页显示的"已投 X / 15 场"中的分母，只能来自这个计算，不允许手工填写。

**奇数参赛数的处理。** 当某轮参赛数为奇数时，安排一张专辑轮空直接进入下一轮，并在对阵表中明确标注轮空位置，让用户知道为什么这场没有对手。轮空不计入用户的投票场次。

### 4.6 对阵生成算法

对阵生成的输入是参赛专辑列表，输出是完整的场次列表。算法步骤如下：

```
输入：参赛专辑列表 albums，是否启用复活赛 withRevival
输出：对阵表（分组 + 各轮次场次）

第一步「过滤」：对 albums 逐张应用 4.3 的七条准入规则，
                得到合法列表 valid，同时记录被剔除名单与被剔除原因。

第二步「分组」：按 4.4 的规则分组。为保证同一歌手分散，
                先用歌手的专辑数降序排列，再按轮转法依次分配到各组。

第三步「组内单循环」：对每组内的专辑列表，用双重循环生成所有
                两两组合，得到小组赛场次。每场记录组号与组内场序。

第四步「组内排名」：小组赛全部结束后，按胜场数降序、总票数降序
                排序，各组第一名进入四强列表，第二名进入复活候选列表。

第五步「复活赛」（可选）：对复活候选列表两两配对，生成复活赛场次。

第六步「淘汰赛」：四强（或四强加复活胜者）两两配对生成半决赛，
                胜者生成决赛。每轮若出现奇数，安排一张轮空并标注。

第七步「落库」：把分组结构、各轮次场次、轮次总数与是否含轮空
                一次性写入数据库，返回对阵表摘要给前端。
```

算法中"组内排名"必须由服务端计算并落库。前端只读取已算好的结果，不重复实现排序逻辑，避免前后端判定不一致。

### 4.7 投票、胜负判定与异常处理

**投票规则。** 一场对决展示两张专辑，用户二选一。同一用户对同一场次只能投一票，重复提交由服务端拒绝并返回明确提示。每张专辑在一场中的得票数即该场的比分。

**胜负判定。** 单场得票多者胜。若两方得票相同，按以下顺序裁决：先比较该专辑在本轮累计得票数，再比较专辑发行年份较早者（作为确定性的最终兜底），保证任何情况下都能得出唯一结果，不会出现"平局无法推进"。

**投票异常判定。** 刷票判定不把"投票数量"作为唯一依据。普通用户单日投票数十场属于正常行为，若只看数量会大量误伤，因此改用行为特征分级判定：

| 等级 | 触发条件 | 处理 |
| --- | --- | --- |
| 轻度 · 限流 | 连续两次投票间隔小于 3 秒；或 1 分钟内超过 20 场；或 1 天内超过 300 场 | 返回"操作过于频繁"，不计违规 |
| 中度 · 警告 | 对同一场次重复提交；或连续 15 场总投同一侧；或注册不足 1 小时已投 50 场以上 | 该票作废并警告，记 1 次违规 |
| 重度 · 复核 | 同一来源或设备下多账号集中投同一对象 | 标记相关账号，转管理员复核 |

**违规累计处理。** 第 1 次站内警告；第 2 次警告并限流 48 小时；第 3 次自动停权 24 小时；第 5 次或经人工判定为恶意的，长期禁用（保留申诉通道）。

**两项配套约定。** 第一，被判定为异常的投票不计入排行榜与人格分布统计，让违规行为既无收益也有代价；第二，上述规则在首页以"投票规范"形式向用户公示，用户进入平台即可看到，而不是等被处罚后才知情。

### 4.8 冠军生成与夺冠路径

决赛结束后，服务端把冠军专辑标识与完整夺冠路径写入对决记录。夺冠路径由该专辑参与过的全部场次按轮次顺序组成，包含每场对手、比分与轮次名称。结果页与分享图都基于这份路径数据渲染，因此两者展示的路径一定一致。

## 五、数据库设计

### 5.1 设计约定

（1）集合的 `_id` 统一使用 MongoDB 原生对象标识，作为主键；

（2）来自外部接口的标识单独用字段保存，并使用唯一索引，用于判重与更新，不与主键混用；

（3）所有集合包含创建时间字段；需要更新的集合包含更新时间字段；

（4）引用其他集合时保存对象标识，不保存名称快照。名称在查询时按需聚合，避免出现"名称改了但历史记录还是旧名称"的不一致；

（5）本章给出的字段定义是**建表的唯一依据**。实体关系图（见 `figures/ER图-音格数据库.png`）用于展示实体与主外键关系，为了让图可读，图中只绘制核心字段；某个集合有多少字段，以本章数据字典为准。

### 5.2 数据域与集合总览

系统共设计 12 张集合，按业务划分为四个域：

| 域 | 集合 | 中文名 | 职责 |
| --- | --- | --- | --- |
| 用户域 | `users` | 用户 | 账号、角色、状态 |
| 用户域 | `favorites` | 收藏 | 用户与专辑的收藏关系 |
| 用户域 | `share_cards` | 分享图记录 | 用户生成过的分享图 |
| 音乐数据域 | `artists` | 歌手 | 歌手元数据与缓存时间 |
| 音乐数据域 | `albums` | 专辑 | 专辑元数据与缓存时间 |
| 音乐数据域 | `tracks` | 曲目 | 曲目元数据与试听地址 |
| 对决域 | `battles` | 对决 | 一次对决的整体状态 |
| 对决域 | `battle_matches` | 对战场次 | 单场对阵、比分与胜负 |
| 对决域 | `votes` | 投票 | 用户对单场的投票记录 |
| 测评域 | `personality_questions` | 测评题目 | 题目、选项与分值 |
| 测评域 | `personality_types` | 人格类型 | 类型定义与推荐专辑 |
| 测评域 | `personality_results` | 测评结果 | 用户作答、得分与解读 |

实体关系如下（完整图形见 ER 图）：

```
users ──1:N──→ battles            用户发起对决
users ──1:N──→ favorites          用户收藏专辑
users ──1:N──→ share_cards        用户生成分享图
users ──1:N──→ votes              用户投票
users ──1:N──→ personality_results 用户作答
artists ──1:N──→ albums           歌手拥有专辑
albums ──1:N──→ tracks            专辑包含曲目
battles ──1:N──→ battle_matches   对决包含场次
albums ──1:N──→ battle_matches    专辑参与对阵
battle_matches ──1:N──→ votes     场次产生投票
personality_types ──1:N──→ personality_results 类型归属
```

### 5.3 用户域字段定义

#### 5.3.1 用户　users

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| _id | ObjectId | 是 | 主键 |
| account | String | 是 | 登录账号，全局唯一 |
| passwordHash | String | 是 | 密码单向加密结果，不保存明文 |
| nickname | String | 是 | 昵称，全局唯一 |
| role | String | 是 | 角色，取值 `user` 或 `admin` |
| status | String | 是 | 账号状态，取值 `active` 或 `banned` |
| bannedReason | String | 否 | 禁用原因，取值见下方枚举 |
| bannedNote | String | 否 | 判定说明，记录触发的具体行为 |
| bannedAt | Date | 否 | 禁用时间 |
| bannedBy | ObjectId | 否 | 执行禁用的管理员 |
| violationCount | Number | 是 | 违规累计次数，默认 0 |
| restrictUntil | Date | 否 | 限流或停权的截止时间，到期自动解除 |
| lastLoginAt | Date | 否 | 最近登录时间 |
| createdAt | Date | 是 | 创建时间 |
| updatedAt | Date | 是 | 更新时间 |

`bannedReason` 的取值必须与前台禁用提醒、后台禁用操作三处使用同一套枚举，保证用户看到的理由与管理员的选项完全对应：

| 取值 | 含义 | 对应处理 |
| --- | --- | --- |
| `vote_fraud` | 刷票行为 | 禁用 |
| `spam_content` | 昵称或账号名含广告、违规、攻击性内容 | 禁用 |
| `abuse_request` | 绕过前端高频调用接口 | 禁用 |
| `self_request` | 用户本人申请注销 | 禁用 |
| `appeal_overturned` | 误判申诉成立 | 恢复启用 |

#### 5.3.2 收藏　favorites

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| _id | ObjectId | 是 | 主键 |
| userId | ObjectId | 是 | 收藏发起用户，外键指向 users |
| targetType | String | 是 | 收藏对象类型，取值 `album` 或 `personality_type` |
| targetId | ObjectId | 是 | 收藏对象标识 |
| createdAt | Date | 是 | 收藏时间 |

同一用户对同一对象只能有一条收藏记录，因此对（userId, targetType, targetId）建立唯一索引，重复收藏由数据库层直接拒绝。

#### 5.3.3 分享图记录　share_cards

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| _id | ObjectId | 是 | 主键 |
| userId | ObjectId | 是 | 生成者，外键指向 users |
| type | String | 是 | 分享图类型，取值 `bracket`（对阵图）、`champion`（夺冠之路）、`personality`（人格卡） |
| refId | ObjectId | 是 | 关联对象标识：对决标识或测评结果标识 |
| imageUrl | String | 是 | 图片访问地址 |
| createdAt | Date | 是 | 生成时间 |

本表的作用是让用户日后能找回自己生成过的分享图，因此不保存图片二进制内容，只保存地址。

### 5.4 音乐数据域字段定义

#### 5.4.1 歌手　artists

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| _id | ObjectId | 是 | 主键 |
| artistId | Number | 是 | 外部接口返回的歌手标识，唯一索引，用于判重与更新 |
| name | String | 是 | 歌手名称 |
| genre | String | 否 | 主要流派 |
| region | String | 否 | 数据地区参数，记录该歌手数据取自哪个地区，用于地区回退排查 |
| albumCount | Number | 否 | 已缓存的专辑数量 |
| cachedAt | Date | 是 | 数据缓存时间，用于计算新鲜度 |
| createdAt | Date | 是 | 创建时间 |
| updatedAt | Date | 是 | 更新时间 |

#### 5.4.2 专辑　albums

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| _id | ObjectId | 是 | 主键 |
| albumId | Number | 是 | 外部接口返回的专辑标识，唯一索引 |
| artistId | ObjectId | 是 | 所属歌手，外键指向 artists |
| artistExternalId | Number | 是 | 歌手的外部标识，用于归属校验（按标识比对而非按名称） |
| name | String | 是 | 专辑名称 |
| artworkUrl | String | 是 | 封面地址 |
| trackCount | Number | 是 | 曲目数，参与准入过滤的规则 1 |
| releaseDate | Date | 否 | 发行日期。对位赛需要对位序号，序号由本字段升序排序后计算，不单独落库 |
| isEligible | Boolean | 是 | 是否通过准入过滤 |
| excludeReason | String | 否 | 被剔除时命中的规则名称，用于向前端展示剔除原因 |
| cachedAt | Date | 是 | 缓存时间 |
| createdAt | Date | 是 | 创建时间 |
| updatedAt | Date | 是 | 更新时间 |

`isEligible` 与 `excludeReason` 两个字段的存在，是为了让"删除原因对用户可见"这一要求可以直接由数据支撑，而不是前端再算一遍过滤逻辑。

#### 5.4.3 曲目　tracks

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| _id | ObjectId | 是 | 主键 |
| trackId | Number | 是 | 外部接口返回的曲目标识，唯一索引 |
| albumId | ObjectId | 是 | 所属专辑，外键指向 albums |
| albumExternalId | Number | 是 | 专辑的外部标识，用于把按歌手拉取的曲目归类到专辑 |
| artistExternalId | Number | 是 | 歌手的外部标识 |
| name | String | 是 | 曲目名称 |
| previewUrl | String | 否 | 30 秒试听地址。为空表示该曲无试听资源，前端需降级提示 |
| duration | Number | 否 | 时长，毫秒 |
| discNumber | Number | 否 | 碟号 |
| trackNumber | Number | 否 | 曲序，试听代表曲默认取第 1 首时使用 |
| cachedAt | Date | 是 | 缓存时间 |
| createdAt | Date | 是 | 创建时间 |
| updatedAt | Date | 是 | 更新时间 |

曲目数据只能按歌手标识批量拉取后再按专辑识别，因为按专辑标识查询曲目时接口不会展开曲目列表。本条来自实测，编码时不可想当然。

### 5.5 对决域字段定义

#### 5.5.1 对决　battles

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| _id | ObjectId | 是 | 主键 |
| userId | ObjectId | 是 | 发起用户，外键指向 users |
| scopeType | String | 是 | 范围模式，取值 `artist`、`multi-artist`、`genre`、`era`、`custom`、`aligned` |
| scopeKey | String | 否 | 范围关键字。单范围模式存放歌手标识或流派年代；对位赛存放参与歌手标识串 |
| artists | Array | 否 | 多歌手与对位赛模式使用，元素为 `{ artistId, name, albumCount }` |
| alignCount | Number | 否 | 对位赛使用，表示每位歌手取前几张参与对位 |
| withRevival | Boolean | 是 | 是否启用复活赛 |
| status | String | 是 | 状态，取值 `playing` 或 `finished` |
| groupCount | Number | 是 | 小组数量 |
| roundCount | Number | 是 | 总轮次数 |
| currentRound | Number | 是 | 当前轮次 |
| matchTotal | Number | 是 | 总场次数，由赛制计算得出，不允许手工填写 |
| hasBye | Boolean | 是 | 是否安排了轮空 |
| albumIds | Array | 是 | 参赛专辑标识列表，分组后固定不变 |
| championAlbumId | ObjectId | 否 | 冠军专辑，未结束时为空 |
| createdAt | Date | 是 | 创建时间 |
| updatedAt | Date | 是 | 更新时间 |

#### 5.5.2 对战场次　battle_matches

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| _id | ObjectId | 是 | 主键 |
| battleId | ObjectId | 是 | 所属对决，外键指向 battles |
| roundIndex | Number | 是 | 轮次序号。小组赛为组内场序；淘汰赛为轮次编号；**对位赛为对位序号（第 k 张专辑）** |
| roundName | String | 是 | 轮次名称，取值 `group`、`revival`、`semi`、`final` |
| groupNo | Number | 否 | 小组编号，小组赛使用 |
| matchOrder | Number | 是 | 同一轮次内的场序，决定用户投票顺序 |
| leftAlbumId | ObjectId | 是 | 左侧专辑，外键指向 albums |
| rightAlbumId | ObjectId | 否 | 右侧专辑。为空表示本场为轮空 |
| leftVotes | Number | 是 | 左侧专辑得票数，默认 0 |
| rightVotes | Number | 是 | 右侧专辑得票数，默认 0 |
| winnerAlbumId | ObjectId | 否 | 胜者专辑，未结束时为空 |
| isBye | Boolean | 是 | 是否为轮空场次 |
| isRevival | Boolean | 是 | 是否属于复活赛 |
| createdAt | Date | 是 | 创建时间 |
| updatedAt | Date | 是 | 更新时间 |

#### 5.5.3 投票　votes

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| _id | ObjectId | 是 | 主键 |
| matchId | ObjectId | 是 | 所属场次，外键指向 battle_matches |
| battleId | ObjectId | 是 | 所属对决，冗余字段，用于按对决批量统计而不必回表 |
| albumId | ObjectId | 是 | 被投的专辑，外键指向 albums |
| userId | ObjectId | 是 | 投票用户，外键指向 users |
| ip | String | 否 | 来源地址，用于同来源多账号集中投票的判定 |
| deviceHash | String | 否 | 设备特征摘要，用于同设备多账号判定 |
| isInvalid | Boolean | 是 | 是否被判定为异常票，默认否 |
| invalidReason | String | 否 | 判定为异常的原因 |
| createdAt | Date | 是 | 投票时间，同时用于投票间隔与频率的判定 |

对（matchId, userId）建立唯一索引，从数据库层保证"单场次单用户单票"，即使前端校验被绕过也无法写入第二票。所有榜单与人格分布的统计口径都必须加上 `isInvalid` 为否的条件。

### 5.6 测评域字段定义

#### 5.6.1 测评题目　personality_questions

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| _id | ObjectId | 是 | 主键 |
| order | Number | 是 | 题号，唯一，决定出题顺序 |
| type | String | 是 | 题型，取值 `choice`（普通选择题）或 `audio`（听感题） |
| title | String | 是 | 题干 |
| audioRef | String | 否 | 听感题使用的试听地址，普通题为空 |
| dims | Array | 是 | 本题涉及的维度列表 |
| options | Array | 是 | 选项列表，元素为 `{ key, label, score }`，其中 score 为各维度的加分对象 |
| createdAt | Date | 是 | 创建时间 |
| updatedAt | Date | 是 | 更新时间 |

把分值写在选项对象里而不是写死在代码中，是为了让后台的题目管理模块能直接调整计分模型，不需要改代码重新部署。

#### 5.6.2 人格类型　personality_types

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| _id | ObjectId | 是 | 主键 |
| code | String | 是 | 类型码，唯一，与测评结果中的类型码对应 |
| name | String | 是 | 类型名称 |
| description | String | 是 | 类型描述，用于人格卡展示 |
| dims | Object | 是 | 该类型的维度特征，用于与用户得分做匹配 |
| recommendAlbumIds | Array | 是 | 推荐专辑标识列表 |
| createdAt | Date | 是 | 创建时间 |
| updatedAt | Date | 是 | 更新时间 |

#### 5.6.3 测评结果　personality_results

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| _id | ObjectId | 是 | 主键 |
| userId | ObjectId | 是 | 作答用户，外键指向 users |
| typeCode | String | 是 | 得出的人格类型码，外键指向 personality_types |
| answers | Array | 是 | 作答明细，元素为 `{ questionId, optionKey }`，用于结果可复查 |
| scores | Object | 是 | 各维度累计得分 |
| aiComment | String | 否 | 个性化解读文本 |
| aiCommentSource | String | 是 | 解读来源，取值 `llm` 或 `template`，显式暴露是否降级 |
| recommendAlbums | Array | 是 | 推荐专辑列表 |
| createdAt | Date | 是 | 结果生成时间 |

`answers` 字段保留完整作答明细，是"结果可复核"的实现方式：用户可以回看自己每一题选了什么，而不是只拿到一个结论。

### 5.7 索引设计

| 集合 | 索引字段 | 类型 | 用途 |
| --- | --- | --- | --- |
| users | account | 唯一 | 登录查询与账号唯一性 |
| users | nickname | 唯一 | 昵称唯一性 |
| artists | artistId | 唯一 | 按外部标识判重与更新 |
| albums | albumId | 唯一 | 按外部标识判重与更新 |
| albums | artistId | 普通 | 按歌手查专辑列表 |
| tracks | trackId | 唯一 | 按外部标识判重与更新 |
| tracks | albumExternalId | 普通 | 按专辑归集曲目 |
| battles | userId + createdAt | 复合 | 个人中心"我的对决"按时间倒序 |
| battles | status | 普通 | 统计进行中的对决 |
| battle_matches | battleId + matchOrder | 复合 | 按对决取下一场 |
| battle_matches | winnerAlbumId | 普通 | 榜单统计与夺冠路径回溯 |
| votes | matchId + userId | 唯一 | 保证单场单用户单票 |
| votes | battleId | 普通 | 按对决统计票数 |
| votes | albumId + isInvalid | 复合 | 榜单统计，只统计有效票 |
| personality_results | userId + createdAt | 复合 | 个人中心"我的测评" |
| personality_results | typeCode | 普通 | 人格图鉴占比统计 |
| favorites | userId + targetType + targetId | 唯一 | 收藏唯一性与列表查询 |
| share_cards | userId + createdAt | 复合 | 分享记录列表 |

### 5.8 缓存策略与新鲜度

音乐数据采用"外部获取 + 本地缓存"两级策略。缓存新鲜度按缓存时间划分为三个档次，前台与后台都使用同一套判定：

| 状态 | 判定条件 | 系统行为 |
| --- | --- | --- |
| 新鲜 | 缓存时间在 30 天以内 | 直接读本地，不请求外部接口 |
| 将过期 | 超过 25 天 | 仍正常使用，用户下次访问时在后台顺手刷新 |
| 已过期 | 超过 30 天 | 用户发起对决时强制重新拉取后再进入下一步 |

这样设计的两个原因：一是外部接口有调用频率限制，集中刷新容易触发限制；二是外部接口不可用时，本地缓存仍能支撑系统完整运行，对应可用性要求中的外部依赖降级。

### 5.9 字段级校验与约束

所有写操作在进入数据库之前统一校验，校验不通过直接拒绝，不写库：

（1）账号长度、格式与唯一性；密码长度下限；昵称长度与敏感词；

（2）范围模式的取值必须在本章 5.5.1 列出的枚举范围内，未知取值拒绝；

（3）创建对决时参赛专辑数不得少于 4 张，否则赛制无意义；

（4）对位赛的歌手数限定在 2 至 4 位，对位张数取各歌手专辑数的最小值；

（5）投票时校验该场次属于该对决、该用户对该场次无历史投票、该专辑确实在本场对阵中；

（6）测评作答必须覆盖全部题目，缺题则不允许提交；

（7）所有时间字段由服务端生成，不接收客户端传入的时间。

## 六、接口设计

### 6.1 接口分组与规模

接口按业务域分为七组，完整定义（含每个接口的路径、方法、入参、返回示例与错误码）见《音格 · API 接口文档》。

| 分组 | 前缀 | 职责 | 鉴权等级 |
| --- | --- | --- | --- |
| 认证 | `/api/auth` | 注册、登录、退出、获取当前用户 | 部分公开 |
| 音乐数据 | `/api/music` | 歌手检索、专辑列表、专辑详情、曲目 | 公开（带限流） |
| 对决 | `/api/battles` | 创建对决、取下一场、投票、复活、结果 | 需登录 |
| 测评 | `/api/personality` | 取题、提交作答、取结果、取图鉴 | 部分公开 |
| 收藏与分享 | `/api/favorites`、`/api/share-cards` | 收藏管理、分享图记录 | 需登录 |
| 排行榜 | `/api/rankings` | 专辑榜、人格占比 | 公开 |
| 后台管理 | `/api/admin` | 看板、题目、类型、音乐数据、用户管理 | 需管理员 |

### 6.2 统一响应结构与状态码

所有接口返回同一结构，前端只需实现一套解析逻辑：

```
{
  "code": 0,              // 0 表示成功，非 0 表示业务错误
  "message": "操作成功",   // 面向用户的提示文案，可直接展示
  "data": { }             // 业务数据，错误时为空
}
```

| 状态码 | 含义 | 使用场景举例 |
| --- | --- | --- |
| 0 | 成功 | 正常返回 |
| 1001 | 入参不合法 | 字段缺失、类型错误、超出范围 |
| 1002 | 未登录或令牌失效 | 令牌缺失、过期、签名不通过 |
| 1003 | 权限不足 | 普通用户访问后台接口 |
| 1004 | 资源不存在 | 指定的对决或专辑不存在 |
| 1005 | 操作重复 | 对同一场次重复投票、重复收藏 |
| 1006 | 操作过于频繁 | 触发频率限制 |
| 1007 | 账号已被禁用 | 被禁用户尝试登录或投票 |
| 2001 | 外部数据获取失败 | 外部音乐接口不可用 |
| 2002 | 大模型调用失败 | 解读生成失败，前端据此显示模板文案 |

把"操作重复""操作过于频繁""账号已被禁用"做成独立状态码而不是统一报错，是为了让前端能给出准确提示：用户需要知道的是"你为什么不能投票"，而不是"请求失败"。

### 6.3 鉴权分级

| 等级 | 要求 | 涉及接口 |
| --- | --- | --- |
| 公开 | 无需令牌 | 登录注册、歌手检索、榜单、人格图鉴 |
| 需登录 | 校验令牌有效期与签名 | 创建对决、投票、收藏、测评提交、分享记录 |
| 需管理员 | 校验令牌且角色为管理员 | 全部后台接口 |

鉴权在中间件层统一完成，业务代码不重复判断。后台接口在中间件层一次性校验角色，避免某个后台接口漏加判断。

### 6.4 分页与排序约定

列表类接口统一接受页码与每页条数两个参数，返回体统一包含列表、总数、当前页与每页条数四项。排序默认按业务相关性（榜单按票数、个人记录按时间倒序），需要其他排序时通过参数显式指定。

### 6.5 典型调用时序

以"完成一次含 12 张专辑的对决"为例，接口调用顺序如下：

```
1.  POST /api/auth/login                          获取令牌
2.  GET  /api/music/artists/search?keyword=周杰伦   取得歌手标识
3.  GET  /api/music/artists/:id/albums             取得专辑（首次会写入缓存）
4.  POST /api/battles                              创建对决，返回场次总数与分组
5.  GET  /api/battles/:id/matches/:matchId         取一场的对阵详情与试听地址
6.  POST /api/battles/:id/matches/:matchId/vote    提交选择（循环 5-6 共 12 场）
7.  POST /api/battles/:id/revival                  按需确认复活赛
8.  GET  /api/battles/:id/result                   取得冠军与夺冠路径
9.  POST /api/share-cards                          记录分享图归属
```

以"完成一次对位赛"为例，第 4 步的请求体使用 `aligned` 模式，第 5 至 6 步的循环次数等于对位张数乘以组内配对数。

## 七、关键流程设计

### 7.1 注册、登录与令牌校验

注册时服务端先校验账号与昵称的唯一性，再对密码执行单向加密后落库，数据库中不出现明文。登录时用账号取出加密结果与输入比对，通过后签发令牌返回前端，前端持久化保存并在后续请求的请求头中携带。中间件在每次请求时校验令牌的存在、签名与有效期，并额外检查账号状态：

这里有一个容易漏掉的环节——**被禁用的账号即使持有有效令牌也必须被拦住**。因此在令牌校验通过之后，还要查一次账号状态，状态为禁用则返回 1007 并附上禁用原因，让用户知道是账号问题而不是网络问题。前台据此展示"账号已被禁用"提醒，并给出申诉入口。

### 7.2 音乐数据获取与缓存

流程分五步：

（1）**检索**：用关键词调用检索接口取歌手标识，地区参数按顺序尝试，默认不使用大陆参数；

（2）**相关性校验**：逐条比对返回歌手的名称与关键词是否相关，不相关的条目丢弃；若全部不相关，明确提示"未找到该歌手"，不返回无关结果。这一步不可省略——外部接口在无匹配时会返回一批不相关歌手，若直接展示，用户会看到完全无关的人；

（3）**专辑获取与归属校验**：按歌手标识取专辑列表，逐张比对其歌手标识是否等于目标歌手标识，不等则丢弃。归属比对只能用标识，用名称会在繁简体与写法差异上出错；

（4）**准入过滤**：应用第六章 4.3 节的七条规则，逐张判定并记录 `isEligible` 与 `excludeReason`，把全部结果（含被剔除项与原因）一并返回前端；

（5）**曲目获取与落库**：按歌手标识取曲目，得到带试听地址的曲目列表，再按专辑标识分组归入对应专辑。首次获取的数据连同缓存时间一起落库。

缓存过期的判定与刷新行为见 5.8 节。

### 7.3 对决创建与推进

创建对决时，服务端先取参赛专辑（必要时触发缓存刷新），应用准入过滤，计算分组与完整对阵表，把对决记录与全部场次记录一次性写入数据库，然后向前端返回场次总数、分组结构与是否包含轮空。**对阵表在创建时就全部生成并固定**，而不是边投票边生成，这样用户在对阵页看到的结构与后续每一场都对得上。

推进过程中，前端每完成一场投票就取下一场。全部场次完成或达到该轮次结束时，服务端计算本轮排名并生成下一轮场次。复活赛是否启用由创建时的参数决定。四个以上专辑的决赛结束后，写入冠军标识并生成夺冠路径，把对决状态置为已结束。

### 7.4 投票与异常判定

一次投票要经过五道校验，全部通过才落库并计票：

```
第一道：该场次存在，且属于该对决
第二道：投票对象确实是本场对阵的两张专辑之一
第三道：账号未被禁用
第四道：频率判定——读取该用户最近一次投票时间，计算间隔；
        间隔、每分钟场次、每日场次三项中任一项超阈值则返回 1006
第五道：唯一性——由数据库的复合唯一索引兜底，
        重复投票写入时被拒绝，返回 1005
```

频率判定在第四道完成，属于"轻度"处理，只拒绝本次操作、不记违规。若在唯一性校验中发现已有投票记录（说明绕过了前端直接调接口），则按"中度·警告"处理：把新票作废并记 1 次违规，累计到阈值后按 4.7 节的分级规则处理账号状态。

计票时同时累加场次记录的左右票数，并把该场次的胜者写入。票数统计与榜单统计都必须过滤 `isInvalid`，禁止把异常票计入。

### 7.5 人格测评计分

计分完全数据驱动，不把分值与题目写死在代码里：

（1）前端取题时，服务端按题号顺序返回题目、选项与选项对应的维度分值；

（2）用户提交作答后，服务端校验作答覆盖了全部题目（缺题拒绝提交），然后遍历作答的选项，把每个选项在各维度上的分值累加到总得分；

（3）把用户得分向量与各人格类型的维度特征逐一比对，取最接近者作为结果类型；

（4）结果连同完整作答明细一起落库，作答明细用于结果页的"复查"入口。

分数与映射规则都存放在数据库中，因此后台的题目管理模块可以直接调整计分模型，不需要修改代码。

### 7.6 AI 解读与降级

AI 解读是增强项而非必需项，因此设计上把"不可用"当作正常情况处理：

（1）服务端把作答组合、得分、得出的人格类型拼装为提示词；

（2）调用大模型生成解读文本，设置超时上限，超时立即中止；

（3）成功时把文本与来源标记 `llm` 一起落库；

（4）失败或超时时，取该人格类型的模板文案作为解读，来源标记为 `template`，接口依然返回成功；

（5）同一组作答的解读做缓存，重复提交不再调用大模型，避免重复计费。

接口返回体中显式包含解读来源字段，前端据此决定是展示"AI 解读"还是普通描述。**用户永远不会因为大模型不可用而拿不到测评结果**，这就是这一层降级的意义。

### 7.7 分享图生成与记录

分享图在前端生成：页面把结果区域渲染完成后，用画布把该节点渲染为图片，再触发下载与预览。生成成功后，前端把图片地址与类型、关联对象标识提交给服务端记录归属，用户日后可在个人中心找回。图片本身在演示阶段存放于后端静态目录，由后端直接对外提供访问，不引入额外的对象存储服务。

## 八、安全设计

| 需求编号 | 安全要求 | 实现措施 |
| --- | --- | --- |
| NFR-S1 | 密码安全 | bcrypt 单向加密存储，数据库中不出现明文；登录比对在服务端完成 |
| NFR-S2 | 身份认证 | 令牌含有效期与签名，中间件每次请求校验；令牌校验通过后再校验账号状态 |
| NFR-S3 | 接口鉴权 | 中间件按路由分组统一校验等级；后台接口额外校验角色，避免个别接口漏判 |
| NFR-S4 | 输入校验 | 统一在中间件层做类型、范围与枚举校验，不通过直接拒绝，不进入数据库 |
| NFR-S5 | 防刷票 | 数据库复合唯一索引保证单场单用户单票；三级行为特征判定（见 4.7）；异常票不计入统计 |
| NFR-S6 | 密钥保护 | 大模型密钥与数据库连接串仅存于服务端环境变量，不下发前端，不写入仓库 |
| NFR-S7 | 越权防护 | 涉及个人记录的接口一律以令牌中的用户标识作为查询条件，不接受客户端传入的用户标识 |

补充三点实现原则：

（1）**禁止把权限判断交给前端。** 前端隐藏按钮只是体验优化，真正的判断必须在服务端做一次；

（2）**用户标识只能来自令牌。** 任何"由客户端告诉我他是谁"的写法都会造成越权，一律不接受请求体中的用户标识；

（3）**错误提示不泄露内部信息。** 对用户只给出可理解的提示，数据库报错、堆栈与外部接口原始响应一律不返回前端，只写入服务端日志。

## 九、部署设计

### 9.1 部署拓扑

```
用户浏览器
   │
   ├──→ 静态站点托管（前端构建产物）        自动 HTTPS
   │
   └──→ 应用托管（后端服务，Node 常驻）
            │
            ├──→ 云数据库（MongoDB）
            ├──→ 外部音乐数据接口
            ├──→ 大模型接口
            └──→ 本地静态目录（分享图文件）
```

前端与后端分别部署，通过环境变量中的接口地址互相定位，因此更换任一端都不影响另一端。

### 9.2 环境变量清单

| 变量 | 说明 | 是否敏感 |
| --- | --- | --- |
| 服务端口 | 后端监听端口，由托管平台注入 | 否 |
| 数据库连接串 | MongoDB 连接地址 | 是 |
| 令牌密钥 | 令牌签名使用的密钥 | 是 |
| 令牌有效期 | 令牌有效时长 | 否 |
| 大模型密钥 | 大模型接口调用密钥 | 是 |
| 数据地区参数 | 音乐数据接口的地区参数，默认香港 | 否 |
| 缓存有效天数 | 音乐数据缓存的有效天数，默认 30 | 否 |
| 前端接口地址 | 后端接口的公开地址，供前端构建时注入 | 否 |

敏感项一律通过托管平台的环境变量界面配置，**不写入代码、不写入仓库、不出现在任何文档正文中**。

### 9.3 上线步骤

（1）在云数据库服务创建数据库实例，配置允许来源地址，取得连接串；

（2）在应用托管平台创建服务，关联代码仓库，配置 9.2 中的环境变量；

（3）部署后端并访问健康检查接口确认服务可用；

（4）在静态站点托管平台创建项目，配置前端构建命令并把后端接口地址注入构建环境；

（5）部署前端，取得公开访问地址；

（6）在后端配置中允许前端域名跨域访问，重新部署后端；

（7）用注册、登录、检索、创建对决、投票、测评的完整链路验证线上环境。

### 9.4 免费额度与容量说明

演示环境使用云平台免费额度，容量约束如下，这些约束直接影响 2.1 节的设计原则：

| 约束 | 影响 | 应对 |
| --- | --- | --- |
| 应用服务在空闲后可能休眠 | 首次访问响应变慢 | 演示前先访问一次唤醒；在论文中说明冷启动现象 |
| 数据库免费额度空间有限 | 不能无限缓存音乐数据 | 缓存只存元数据与试听地址，不存音频文件 |
| 静态托管流量有限 | 分享图不能过大 | 图片按需生成、控制分辨率 |
| 并发能力有限 | 不支持大规模并发 | 定位为评选与测评工具，演示规模为数十人同时在线 |

## 十、设计对需求的覆盖与追溯

下表说明每类需求的实现落点，用于自查"需求是否有设计承接、设计是否有需求来源"。

| 需求范围 | 需求编号 | 设计落点 |
| --- | --- | --- |
| 用户与账号 | FR-01 至 FR-04 | 5.3.1 与 5.3.2 表结构、7.1 流程、6.1 认证接口 |
| 音乐数据浏览 | FR-05 至 FR-07、FR-28、FR-29 | 5.4 表结构、7.2 流程、缓存策略 5.8 |
| 专辑对决 | FR-08 至 FR-11、FR-26、FR-27 | 第四章全部、5.5 表结构、7.3 与 7.4 流程 |
| 试听播放 | FR-12、FR-30 | 5.4.3 的试听地址字段、7.2 的曲目获取 |
| 音乐人格测评 | FR-13 至 FR-15 | 5.6 表结构、7.5 流程 |
| AI 解读 | FR-16、FR-17 | 7.6 流程、5.6.3 的来源标记字段 |
| 可视化与分享 | FR-18 | 5.3.3 表结构、7.7 流程 |
| 排行榜与图鉴 | FR-19、FR-20 | 5.7 榜单相关索引、6.1 榜单接口 |
| 后台管理 | FR-21 至 FR-25 | 3.2 模块设计、5.3.1 的账号状态与禁用字段 |
| 性能 | NFR-P1 至 NFR-P6 | 首页聚合接口、列表分页、缓存策略、AI 超时降级 |
| 安全 | NFR-S1 至 NFR-S7 | 第八章 |
| 可用性 | NFR-U1 至 NFR-U5 | 外部依赖降级、三类状态提示、二次确认、耗时预告 |
| 可维护性 | NFR-M1 至 NFR-M4 | 前后端分离、配置外置、文档与代码同步 |
| 数据准确性 | NFR-D1 至 NFR-D5 | 7.2 的相关性校验与归属校验、4.3 的准入过滤、过滤结果可见 |

**一处需要特别说明的设计变更**：需求文档中"专辑对决"的范围模式原为四种，本设计扩充为五种，新增"对位赛"模式（4.2 节）。该模式解决的问题是：混战模式会打乱专辑序号，无法表达"两位歌手同阶段作品谁更强"这类比较需求。对位赛复用多歌手模式的歌手选择结构，赛制差异在赛制服务内部消化，数据层与接口层不需要新增表，只需在范围模式的枚举中增加一个取值。需求文档与接口文档已同步更新，三份文档在这一点上口径一致。
