<!--
  单场对决台（PK 页）
  ------------------------------------------------------------
  2026-09-21 用户要求："能不能 pk 专辑时进来直接就是页面划过的这个位置"
  —— 之前点「开始对决 / 继续投票」会落在 BattlePlayView，那一页上方还有
     进度条、阶段说明等整块内容，要往下滚才看到对战台（用户划过的位置）。

  做法：BattlePlayView 通过 `pk-only` 属性隐藏掉除对战台之外的所有块
  （页头、进度条、阶段说明、底部条），只留「两张专辑 + 投票按钮 + 播放条」，
  逻辑与组件完全复用（不复制一份，避免两套行为漂移）。

  这个路由名 `battle-pk` 是「进对决」的统一落点：
  创建完成 / 继续投票 / 我的对决 / 同款签表 / 对阵表 / 结果页 都指到这里。
-->
<template>
  <BattlePlayView pk-only />
</template>

<script setup>
import BattlePlayView from './BattlePlayView.vue';
</script>
