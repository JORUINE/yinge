# 改动记录（保留可回退痕迹）

> 规则：每次大改动记录 时间 / 文件 / 改前 / 改后 / 原因 / 回退方式。
> 后端源码一律入 git，提交本身即为可回退痕迹（`git revert <hash>` 或 `git checkout <hash> -- <文件>`）。

## 2026-09-17 · 新赛制 v2 后端接线（提交 179551c）

- **改动名**：tournamentV2-backend-wiring
- **范围**：后端「专辑对决」新赛制 `tournamentVersion = 2` 从公式层接进真实数据流（创建 → 小组 4 选 2 → 遗珠复活 → 1v1 淘汰 → 冠军）
- **改前**：第 1 步只完成公式层（`bracket.js` 的 `planTournament` 等），未接真实数据，新赛制无法运行
- **改后**：新赛制可端到端运行；旧赛制（`tournamentVersion` 默认 1）路径未改动，灰度共存
- **涉及文件与要点**
  - 新增 `backend/src/models/BattleGroup.js`：小组 / 复活多选环节集合（battleId / roundName / groupNo / albumIds / advanceCount / pickedAlbumIds / pickedAt）
  - `models/index.js`：导出 `BattleGroup`
  - `models/Vote.js`：`matchId` 改可选 + 新增 `groupId` + 两个 `partialFilterExpression` 唯一索引（matchId 投票 / groupId+userId+albumId 多选投票）
  - `models/Battle.js`：新增 `tournamentVersion / poolTarget / knockoutSize / revivalNeed / stepTotal`
  - `models/BattleMatch.js`：`ROUND_NAMES` 扩展含 `r32 / r16 / qf`
  - `modules/battles/bracket.js`：新增 `roundNameFor / buildGroupDefs / buildGroupDefsCrossArtist / buildKnockoutRound` + KO 相关常量（旧函数一律未动）
  - `battle.service.js`：新增 `createBattleV2 / castGroupVote / getNextStep / progressGroupRevivalV2 / startKnockoutV2 / progressKnockoutV2`；`createBattle` 与 `progressBattle` 增加 v2 分发；`getResult` 增加 `groupsSummary`；分组投票限流改用 `config.vote.minIntervalMs`
  - `battles.controller.js`：`serializeBattle` 增加新赛制字段；新增 `nextStep / groupVote` 处理器
  - `battles.routes.js`：新增 `GET /:id/next-step`、`POST /:id/groups/:groupId/vote`；`createSchema` 增加 `tournamentVersion`（1|2）
  - 新增 `backend/scripts/verify-v2.mjs`：真实连库（独立测试库 `yinge_test`，跑完即删）端到端验证
- **原因**：把已定稿的赛制公式接到真实数据，使新赛制可跑起来给人用；同时修正"已投 6/16 场"这类步数不自洽的问题（总步数由 `planTournament` 唯一推出，已投步数 = 总步数）
- **验证**
  - `verify-v2.mjs`：**59 / 59 通过**（多歌手 3×8=24 张 → 22 步；单歌手 16 张 → 11 步；冠军均决出；已投步数 = 总步数）
  - `verify-scale.mjs`：**152 / 152 无回归**（纯公式层，未改动）
  - v1 路径（`tournamentVersion` 默认 1）代码未改动，保持原有回归覆盖
- **回退**：`git revert 179551c`（或 `git checkout 179551c^ -- backend/src/modules/battles/ backend/src/models/`）

### 自检结论（交付前必跑，已跑）
- 数字一致性：步数 = 组数 +（有复活 ? 1 : 0）+ 淘汰赛场次，由 `planTournament` 一次性推出，前端"已投 X / 总步数 Y"两处同源 → 不会出现 6/16 这类矛盾
- 跨文档口径：本次只动后端；前端 / 需求 / 设计 / 接口文档的 v2 口径对齐留到第 6 步（后期功能清单），届时统一回填
- 互斥状态：已结束对决不可再投票（`status !== 'playing'` 拦截）；分组已投不可重复投（`isPicked()` + 唯一索引兜底）

## 2026-09-17 · 视觉还原 P1①：设计系统对齐 + 创建页 1:1 + v2 规模选择器

- **改动名**：visual-restore-p1-design-system-and-create
- **范围**：前端。第 0 步（设计系统对齐）+ 对决主线第 ① 屏（创建页）
- **改前**：`tokens.css` 只有轻量骨架（无玻璃 / Aurora / 深色 / 人格色）；`AppShell` 顶栏是 Element Plus 风格；`BattleCreateView` 是 Element Plus 表单堆叠，**没有 v2 规模选择器**（用户点名要的"自选每位歌手几张"）
- **改后**：
  - 设计系统：`tokens.css` 升级为音乐海洋完整令牌（浅+深双主题 / 玻璃 / Aurora / 人格 6 色），旧命名保留以兼容未重做页面
  - 新增 `styles/components.css`：从设计稿逐值抄录的共享组件库（nav/btn/g-card/seg/fld/modes/block/chips/tipbar/pool/startbar/presets/note/covers/list/podium/vstage/alb/groups/tree/crown/kpis… 约 90 类），`main.js` 全局引入
  - 新增 `utils/tournament.js`：`planTournament` 的**前端镜像**（与后端 `bracket.js` 同公式），用于创建页实时显示"预计 N 步"
  - `AppShell.vue`：顶栏改为设计稿 `.nav`（音格 logo 带彩色点 / 导航 / 右侧头像 `.me`），加 `.aurora` 极光背景
  - `BattleCreateView.vue`：重建为设计稿 p4 结构（六模式 `.modes` / 对位说明 `.alignbox` / 歌手 `.chips` / 快速搜索 `.presets` / 准入过滤 `.tipbar` / 专辑池 `.pool` / 开始条 `.startbar`）；**新增 v2 参赛规模选择器**（单歌手 8/12/16/24/32，多歌手每位 4/6/8/10），开始条实时显示"已选 N 张 · M 位歌手 · 赛程：…· 共 X 步"
- **接口映射**（与后端契约核对无误）：单歌手 `{scopeType:'artist', artistId, albumCount}`；多歌手 `{scopeType:'multi-artist', artists:[{artistId, albumCount}]}`；均带 `tournamentVersion:2`；对位赛 `aligned` **不带** v2（B 线赛制不变）
- **原因**：按用户指令"直接开工 尽快做出原型"落地 1:1 还原的第 0 步 + 对决主线第一屏
- **验证**
  - `vite build`：**通过**（✓ built in 9.13s）
  - 数字同源：创建页"预计步数"由前端镜像公式算出；创建成功后用后端返回的 `stepTotal` 复核，不一致会弹提示（双保险）
- **备份**：`_backup/20260917_视觉还原P1_设计系统/`（tokens.css / base.css / main.js / AppShell.vue / BattleCreateView.vue 改前原件）
- **回退**：整目录回拷即可；或 `git checkout -- frontend/src/styles frontend/src/layouts/AppShell.vue frontend/src/views/BattleCreateView.vue`

### 自检结论（交付前必跑，已跑）
- 数字一致性：创建页步数 = 前端镜像 `planTournament`；后端 `stepTotal` 复核；两处同源（改动需同步 `frontend/src/utils/tournament.js` 与 `backend/.../bracket.js`）
- 规则未改：小组赛仍是"每组 4 张一次选 2 张"（v2），本次未动任何赛制规则
- 互斥状态：无新增状态机
- 遗留：对决主线第 ②~⑤ 屏（对阵表 / 投票页 4 选 2 / 冠军页 / 分享图）仍为旧实现，待按 v2 重做

## 2026-09-17 · 一键启动自动化 + P1③ 投票页按 v2 重做 + 一个真 bug 的修复

- **改动名**：dev-launcher-and-p1-vote-v2
- **① 一键启动（新增，用户点名要）**
  - 新增 `scripts/mongo.mjs`：MongoDB 启停助手（start/stop/status）。**中文路径交给 Node 处理**（批处理里写中文路径易出编码问题）。
  - 新增 `start-dev.bat`（仓库根）：双击即启 MongoDB → 清历史索引 → 后端 → 前端 → 自动开浏览器。
  - 新增 `stop-dev.bat`：停 MongoDB。
  - **修掉一个真错误**：原 `桌面\毕业设计\start-dev.bat` 写的是 `npm run serve`，而前端脚本名是 `dev` → 以前这样启动前端必然失败；现已改为委托给仓库内的新启动器。
- **② 投票页 `BattlePlayView.vue` 按 v2 重做（P1③）**
  - 小组 / 复活阶段：**一次多选 K 张**（选满 advanceCount 才能提交，`/next-step` + `/groups/:id/vote`），含「已选 X / K」计数、清空重选。
  - 淘汰赛阶段：1v1 舞台（vstage + 专辑主色光晕 + 试听 30 秒 + 播放条 + 投给《…》按钮）。
  - 进度显示「已投 X / Y 步」；专辑主色由 albumId 稳定映射（近似还原设计稿「背景跟着专辑变」）。
  - 旧赛制（v1）对决自动兜底走 `next-match`，并归一化成同样的 1v1 结构，不会白屏。
  - `api/index.js` 新增 `nextStep` / `groupVote`。
- **③ ⚠️ 修复一个会挡住整个小组赛的真 bug：votes 集合残留旧唯一索引**
  - **现象**：新建 v2 对决后，第一组小组投票就报「记录已存在，请勿重复操作」（E11000）。
  - **根因**：Vote 早期版本唯一索引是**非 partial** 的 `(matchId, userId)`；v2 改成 partial 后，MongoDB **不会自动删除旧索引**。小组投票一次性插入多条 `matchId = null` 的记录 → 撞上旧的非 partial 唯一索引。
  - **修复**：新增 `scripts/migrate-drop-stale-indexes.mjs`（幂等）删除遗留索引 `votes.matchId_1_userId_1`，保留新的 partial 索引；启动器已自动调用。
- **验证（真实后端 E2E，非推测）**
  - 建 v2 对决：`stepTotal=5 · 小组 2 组 · 淘汰赛 4 强 · 版本 2`（与 `planTournament(8)` 一致）
  - 第 1 步 `phase=group` 第 1 组 `advance=2` 候选 4 张 `progress 0/5`
  - 小组投票 **ok** → 第 2 步推进到第 2 组 `progress 1/5`
  - 非法投票（只选 1 张）**被正确拒绝**：「本分组需选择 2 张专辑晋级」
  - 前端 `vite build` 通过（11.08s）；E2E 用的临时脚本已删除
- **回退**：`git checkout -- frontend/src/views/BattlePlayView.vue frontend/src/api/index.js`；启动器为新增文件，删除即可
- **遗留**：对阵表 p5 / 冠军页 p7 / 分享图 p8 仍待按 v2 重做

## 2026-09-17 · 对决主线收官：对阵表 p5 / 冠军页 p7 / 分享图 p8 + 专辑歌手名修复

- **改动名**：p1-bracket-result-share
- **② 对阵表页（新建）**：`BattleBracketView.vue` + 路由 `/battle/:id/bracket`
  - 1:1 原型 p5：小组卡（每 4 张一组、标注「选 2 张晋级」、已晋级高亮）、对阵树（按轮次列出 tie + 冠军列）、进度 X/Y、进行中那一场高亮。
  - 后端补齐：`getBattleDetail` 返回新增 `groups`（groupId / roundName / groupNo / advanceCount / picked / advancedAlbumIds / albums），控制器 `detail` 透出。**对位赛/指定对决**仍走原有逐行对照表。
- **④ 冠军页（重做）**：`BattleResultView.vue`
  - 1:1 原型 p7：`crown-wrap` 冠军大图 + CHAMPION 标题 + 战绩说明 + 按钮（生成夺冠之路 / 再玩一次）；`kpis`（夺冠轮次 / 累计得票 / 对手总票数）；`夺冠之路` 每轮两行对照（胜方高亮 + 「胜」徽章 + 双方得票率，决赛金边 `.mres.final`）。
  - 未打完时给「继续投票 / 看对阵表」入口，不再白屏。
- **⑤ 分享图页（新建）**：`BattleShareView.vue` + 路由 `/battle/:id/share`
  - 1:1 原型 p8：`scard` 分享卡（冠军 + 击败对手封面条 + 轮次串 + 底部统计）+ 导出设置（竖版 1080×1440 / 方形 1080×1080 / 带上分享链接）。
  - **新增依赖 `html2canvas`**（按需动态 import，已单独分包）：下载图片按 1080 宽等比放大导出 PNG；另提供「复制链接分享」。
- **③ 修复「专辑卡片不显示歌手名」**（前端联调时发现 `artistName` 为 undefined）
  - 根因：`Album` 从未存过歌手名，`serializeAlbum` 只给 `artistId`。
  - 修复：`Album` 加 `artistName` 字段 + `syncArtist` 写入 + `serializeAlbum` 输出；新增幂等回填脚本 `backfill-album-artist-name.mjs`（已跑：回填 490 张）。
- **验证（真实后端 E2E，非推测）**
  - 对阵表数据：16 张 → `stepTotal=11 / 小组 4 组 / 淘汰赛 8 强`；每组 `advance=2`；投票后该组 `picked=true`、`已晋级 2 张`。
  - **完整跑完一场到冠军**：8 张 → 2 组 → 半决赛 2 场 → 决赛 1 场，**实际 5 步 = 公式 5 步**；冠军 `杰倫（周杰倫）`；`path` = 半决赛 + 决赛，比分与对手正确；`groupsSummary` 两组各晋级 2 张。
  - 限流生效（300ms）：连点会被拦，符合预期（测试脚本因此加了间隔）。
  - 前端 `vite build` 通过（8.60s）。
- **回退**：`git checkout -- frontend/src backend/src`；新增视图/脚本删除即可。注意 `html2canvas` 为新增依赖。
- **遗留**：原型 p4/p5/p6 按 v2 重绘（任务 #35）；人格线 / 首页 / 账户 / 榜单 / 后台 / 自检收尾（任务 #36 #37）。

## 2026-09-17 · 音乐人格线 1:1 还原（p9~p12 + 类型详情）

- **改动名**：p2-personality-line
- **范围**：前端 5 个页面 + 1 个工具模块。统一到设计系统版式，去掉 Element Plus 外观。
- **改后**
  - 设计系统补充：`components.css` 追加人格线全部组件（qintro/qfacts/typesprev/tpv、quizwrap/qbar/dots、qcard2/qno/qtip、audioplay、oi/qnav、ptcard/glowc/pthd/ptcode/ptname/accent2/dim2/bar2/ai2、atlas/at/dist、recs 三光晕 g1/g2/g3）
  - 说明页：`qintro` + `qfacts`（题数/听感题数/时长/类型数，真实取自接口）+ `typesprev` 类型预览（点击进类型详情）
  - 答题页：**一屏一题** + 顶部进度点 + 选完自动进下一题（220ms）+ 听感题 `audioplay`（音频字段做兼容探测，取不到就提示"待后台配置"）
  - 人格卡：`ptcard` 主题色随类型、维度条（`normalizeScores` 兼容 0-1 与 0-10）、`ai2` 解读 + **来源标注**（llm / 模板）、推荐 3 张专辑带"匹配 <最高维度> x/10"、**html2canvas 导出人格卡图片**
  - 图鉴：`atlas`（占比 · 人数）+ `dist` 分布条（每类用自己的类型色）
  - 类型详情：复用 `ptcard` + 常听专辑
  - 新增 `utils/personality.js`：`typeColor` / `dimLabel` / `normalizeScores`
- **⚠️ 发现一处口径不一致（未擅自改数据，等用户拍板）**
  - 设计稿的 6 类是「旋律捕手 / 节拍动物 / 词句收藏家 / 音色控 / 安静聆听者 / 探索者」（MEL/RHY/LYR/TMB/CLM/EXP，维度偏"听感"）
  - 数据库 seed 的 6 类是「电音狂热者 / 深夜循环者 / 探索猎手 / 聚会歌者 / 经典收藏家 / 均衡听众」（EN/NT/EX/SO/CL/BL，维度 energy/social/curiosity/nostalgia）
  - 两套**分类与维度都不通用**（不是改名能对齐的，要先定分类法再改题目与类型）。所以页面按库里实际数据渲染，两套 code 都配了颜色，**不编造数据**。
- **验证**：`vite build` 通过（8.81s）。字段名沿用原视图已在用的契约（typeCode/typeName/typeDescription/scores/aiComment/aiCommentSource/recommendAlbums），未改后端。
- **回退**：`git checkout -- frontend/src/views/Personality*.vue frontend/src/utils/personality.js frontend/src/styles/components.css`
- **遗留**：首页 p1 / 登录 p2 / 个人中心 p3 / 排行榜 p13 / 收藏；后台 6 页；原型 v2 重绘；响应式 + 深色 + 自检收尾。

## 2026-09-17 · 首页 / 登录 / 个人中心 / 排行榜 1:1 还原 + 修复「删对决残留孤儿票污染榜单」

- **改动名**：p3-home-account-rank
- **页面（统一设计系统版式，去 Element Plus 外观）**
  - 首页 p1：`hero`（大图取榜单第一名的封面）+ `duo`（`tile.sea` / `tile.ghost`）+ `shelf` 专辑货架 + 前三榜 `.list` + 投票规范 `panel`。数据来自 `rankApi.albums`，**没有投票数据时显示引导文案，不编造**。
  - 登录/注册 p2：`authwrap`（左侧文案 + `feat` 标签，右侧 `.g-card` 玻璃卡 + `seg` 登录/注册切换 + `fld`）。
  - 个人中心 p3：`uh` 用户头 + 4 格 `stats` + `tabline` 三标签（我的对决 / 我的测评 / 我的收藏，各自真实接口）+ 内联改昵称。
  - 排行榜 p13：Top 领奖台（`vstage.recstage` + `NO.1` 金色徽章）+ 第 4 名以后 `.list` + Top20/50/100。
  - 设计系统：`components.css` 追加 hero/tile/duo/shelf/authwrap/feat/uh/stats/stat/tabline/filter/pill/up/down/panel/obrow 等类。
- **⚠️ 修复了一个会污染榜单的真 bug（由我自己的 E2E 测试暴露）**
  - **现象**：`deleteBattle` 后，该对决的票仍在 `/rank/albums` 里计数（我测试时删了对决，榜单却还留着票）。
  - **根因**：`deleteBattle` 只删 `Battle` 与 `BattleMatch`，**没删 `Vote` 与 `BattleGroup`** → 孤儿票继续被榜单与人格分布统计到。
  - **修复**：改为级联删除（`BattleMatch` / `BattleGroup` / `Vote` 一起删）；新增幂等脚本 `cleanup-orphans.mjs` 清理历史遗留 —— **已清 471 条孤儿票 + 14 条孤儿分组**。
  - **实测验证**：建 v2 对决 → 投一组票 → 删除 → `votes / groups / battle` 全部归零 ✓
- **未做（诚实标注，不编 UI）**：排行榜的「地区 / 年代 / 周期」三重筛选需要后端补接口，暂未开放（页面已写明）；首页「首次进入三张新手引导卡」待做。
- **验证**：`vite build` 通过（9.93s）；榜单接口复查返回真实票数（如《范特西》11 票）。
- **回退**：`git checkout -- frontend/src/views/HomeView.vue frontend/src/views/LoginView.vue frontend/src/views/ProfileView.vue frontend/src/views/RankView.vue backend/src/modules/battles/battle.service.js`
- **遗留**：收藏页；后台 6 页；原型 v2 重绘；响应式 + 深色 + 自检收尾。

## 2026-09-17 · 立《前端设计工作守则》（汇总历次前端设计意见）

- **改动名**：frontend-design-guidelines
- **触发**：用户看 3 张截图提意见，并明确要求"把前端设计上给的意见做成工作守则记下来，等功能做完开始做前端时照着参考"。
- **结论（已答复用户）**：**当前功能优先**，纯视觉打磨 / 动效统一放到收尾阶段；**功能性 bug 立刻修**。
- **新增**：`docs/design/音格-前端设计守则.html`（**唯一入口**，后续新意见只往这里加）。含 7 节：总原则 / 可读性 / 布局对齐 demo / **盲盒原则** / 交互反馈 / 待修清单（9 条，标 P0-P2）/ 落地方式。
- **本轮录入的用户意见**
  - 创建页参赛池：封面大小不一、排布散乱，**不是 demo 的 7 等分正方形网格** → P0。
  - **盲盒原则**：参赛池**不要展示每位歌手本轮全部出战专辑，露 1–2 张即可**（"不然随机比拼的刺激性就没了，应该像盲盒一样"）；后续加**抽卡式入场动画** → P1/P2。
  - 投票页：排布可更密实、**说明字体太小** → P1。
  - 另答复其疑问：参赛池"灰掉的专辑"是**按规模档位被排除**的，不是"这轮没随机到"。
- **同步**：`MEMORY.md` 新增硬性规则第 6 条（前端守则 + 七条核心），保证后续会话必然加载。
- **环境说明**：本机无 headless 浏览器（agent-browser / playwright 均不可用），我无法自行截图；视觉验收依赖用户截图。已静态核验 `components.css` 括号平衡、`.pool` 规则存在。
- **回退**：新增文件，删除即可。

## 2026-09-17 · 装好浏览器自检能力 + 修掉参赛池「封面大小不一」根因 + 盲盒化

- **改动名**：browser-selfcheck-and-grid-fix
- **① 装上 headless 浏览器（用户授权）**
  - 用户看到"本机没有无头浏览器"后明确授权："如果你需要你可以装，我给你权限"。
  - 已在**仓库外**装好 Playwright + Chromium（`WorkBuddy\2026-09-14-13-52-08\tools\pw`，不污染项目依赖），脚本 `shot.mjs` / `diag-pool.mjs`。
  - ⚠️ 关键坑：**必须用托管 Node 22**（系统 Node 18 装不下/跑不了 Playwright）；登录用 `admin/admin123456`，截图存 `tools/pw/shots/`。
  - 意义：**视觉验收不再依赖用户截图**，我可以自己截图 + 读计算样式自检。
- **② 定位并修掉参赛池根因（P0）**
  - 用 Playwright 读计算样式：`tileBoxes` 宽度为 `96 / 190 / 253 / 305 / 332 / 118px`，高度都是 491 → **列宽不等**。
  - **根因**：`grid-template-columns: repeat(7, 1fr)` 中的裸 `1fr` = `minmax(auto,1fr)`，单元格内是 `white-space:nowrap` 的**长专辑名**（真实数据）时，该列被内容撑宽 → 网格变成大小不一。设计稿名字短所以没暴露。
  - **修复**：`components.css` 里所有网格列 + 投票页多选网格 → 一律 `minmax(0, 1fr)`（含 `.pool/.covers/.podium/.recs/.atlas/.kpis/.stats/.groups/.modes/.rounds/.alignbox .arow`）。
  - **实测**：7 列均为 **145.4px**、卡片统一 145×190 ✓（`pool-element.png` 目视确认整齐）
- **③ 盲盒化（用户明确要求）**
  - 原实现把**每位歌手的全部合格专辑**都渲染出来（多歌手时约 80 张）——既剧透、又拖慢页面。
  - 改为**每位只露前 2 张**，其余进对决时逐张揭晓；提示条写明"本轮共 N 张出战，这里只先亮出 M 张"。
  - **实测**：预览卡片从 ~80 → **4 张** ✓
- **④ 守则同步**：第 2 节新增硬规则「**禁止裸 `1fr`，网格列一律 `minmax(0,1fr)`**」；待修清单第 1、2 条标记已修/已做。
- **验证**：`vite build` 通过（8.77s）；Playwright 计算样式 + 元素截图双重确认。
- **回退**：`git checkout -- frontend/src/styles/components.css frontend/src/views/BattleCreateView.vue frontend/src/views/BattlePlayView.vue`
- **遗留**：收藏页；后台 6 页；原型 v2 重绘；其余 P1/P2（返回重选/落败置灰/迷你对阵树/同室操戈/歌手战绩/夺冠次数榜/好友同玩/Apple Music/本地续玩/抽卡动画/深色+响应式验收）。

## 2026-09-17 · 收藏页 1:1 + 后台骨架与前 3 页

- **改动名**：p3-favorites-and-admin-1
- **① 收藏页**：`FavoritesView.vue` 重做为设计系统版式（`filter` 分段「全部/专辑/人格类型」+ `list` 行 + 缩略图 + 「取消收藏」），人格类型缩略图用**类型主题色**。真实接口 `favoriteApi.list/remove`。
- **② 后台骨架（新建）**：`layouts/AdminShell.vue` —— 统一 `admbar`（音格·管理后台 + 管理员 + 退出）+ `adm` 两栏（`admnav` 分组导航 + 内容插槽）。6 个后台页共用，避免各写一套。
  - `AppShell.vue`：**`/admin` 下不再渲染站点顶栏与页脚**（后台自带 chrome，与设计稿一致）。
  - `components.css`：补齐全套后台类（admbar/adm/admnav/admmain/admhd/kpi4/k4/panel/bars/trend/tbl/mini/tagx/tgrid/tcard/sbox）。
- **③ 后台前 3 页**
  - `AdminLoginView`：玻璃卡 + 设计系统输入框。
  - `AdminDashboardView`：`kpi4` 四格（注册用户/禁用、对决总数、专辑缓存/歌手、有效票数）+ `panel` 测评分布柱 + `panel` 内容概览表。
  - `AdminQuestionsView`：`tbl` 题目表（`tagx` 类型徽章 + `mini` 编辑/删除）；**编辑弹窗仍用 Element Plus**（数字输入/下拉/校验等功能性组件，后台内部工具，成本与收益权衡）。
- **验证**：`vite build` 通过（9.46s）；Playwright 截图确认 `/admin` 看板渲染正常（29 用户 / 16 对决 / 896 专辑缓存 / 136 有效票）。
- **回退**：`git checkout -- frontend/src`
- **遗留**：后台剩余 3 页（人格类型 / 音乐数据 / 用户管理）；原型 v2 重绘；P1/P2 前端项。

## 2026-09-17 · 修复「遗珠复活提交不了」+ 指南补上后台登录

- **改动名**：fix-revival-vote-limit-and-admin-login-doc
- **⚠️ 用户报的 bug：复活赛点提交 → 红字「字段校验未通过」**
  - **根因**：`battles.routes.js` 的 `groupVoteBody` 把 `pickedAlbumIds` 上限**写死成 `.max(4)`** —— 那是按"小组赛每组选 2 张"想的，但**遗珠复活要一次捞回 `knockoutSize − qualified` 张，最多可到 7 张**（如 17/18 张参赛时）。所以复活赛必然被 zod 拦下。
  - **修复**：上限放宽到 `.max(16)`（真正的"必须正好选 advanceCount 张"由 service 校验，zod 只给宽松上界）。
  - **实测**：建 17 张单歌手对决 → `5 组小组赛（每组选 2）→ 复活 从 8 张里选 7 张 → 16 强淘汰赛`，**21 步全部跑通**，复活赛提交 **ok** ✓（此前必失败）
- **指南补文档（用户问"后端我怎么进登录，你文档里没写"）**
  - `docs/部署与访问指南.html` 第 2 节新增两块：
    - **一键启动**：双击 `start-dev.bat` 自动起库+清索引+后端+前端+开浏览器；`stop-dev.bat` 停。
    - **怎么进管理后台**：前台 `http://localhost:5173`；**后台 `http://localhost:5173/admin/login`，账号 `admin` / 密码 `admin123456`**（本地默认值，上线前必须改；普通用户账号进不去）。并列出后台五个页。
- **验证**：真实后端 E2E 跑完整场 21 步 ✓；`vite build` 未受影响（后端改动）。
- **回退**：`git checkout -- backend/src/modules/battles/battles.routes.js docs/部署与访问指南.html`
- **遗留**：后台剩余 3 页；原型 v2 重绘；P1/P2 前端项。

## 2026-09-17 · 用户 13 图反馈：筛选规则漏筛修复 + 全部意见入守则

- **改动名**：admission-karaoke-fix-and-feedback-log
- **① ⚠️ 修复准入筛选漏筛（用户指出：卡拉OK专辑居然夺冠）**
  - **根因**：`admission.js` 的关键词表**没有「卡拉OK / 伴奏」这条规则**；`live` 只认 `live/concert`，**不认 tour/巡演/stadium**；`compilation` 不认 `playlist`。于是 `Taylor Swift Karaoke: 1989 (Deluxe)`、`reputation Stadium Tour Surprise Song Playlist`、`TS The Eras Tour Setlist` 全部进池（前者还夺冠）。
  - **修复**：新增规则 `NOT_KARAOKE`（karaoke/卡拉ok/伴唱/伴奏/instrumental）；`live` 补 ` tour`/`tour `/巡演/巡迴/巡回/stadium/体育馆/紅館/红馆；`compilation` 补 playlist/歌单；`normalizeAlbumName` 补 改版/紀念版/復刻/日本盤/台版/港版。
  - **实测**：`reevaluate-admission.mjs` 重算 144 张 → **新剔除 3 张**，样例正是上述三张 ✓；已重启后端让后续抓取也生效。
- **② 用户本轮 13 张图的意见 → 全部录入《前端设计工作守则》**
  - 待修清单新增第 10~25 条（含分组分隔行），并在末尾注明哪些属"排版/动效（收尾做）"、哪些属"功能（随功能做）"、哪两条属"必须修"。
  - 新增第 7 节「需要专门研究的问题」：**测评题量与听感题占比**（12 题→16 题？听感题 2→4 道 + 必须有真实固定音频）、**歌曲 API 到哪一步**（已是 iTunes 30 秒试听；完整播放不建议做，重点补"歌名+切歌"体验）。
  - 要点摘录：**「步」改「场」**（术语，P0）｜投票页**一屏放得下**+左右留白+提示行上移｜**点封面即可投票**、弱化「投给《X》」｜播放条补**歌名+切歌**｜淘汰赛 UI 照 demo｜**颜色采样必须改为读封面主色**（P0，现状是 albumId 哈希 → 黑白封面变紫）｜分享页布局乱｜冠军页补**亚军/季军**+**晋级叙事**（"我选的专辑一路打赢其他组冠军走上来"）｜**全站补返回按钮**｜**好友一起玩**未落地。
- **未做（已入守则）**：上述功能/排版项按优先级推进；颜色采样（P0）与「步→场」（P0）下一步先做。
- **回退**：`git checkout -- backend/src/modules/music/admission.js docs/design/音格-前端设计守则.html`

## 2026-09-17 · 接上真实试听（用户报"点了无法播放"）

- **改动名**：fix-preview-playback
- **现象**：投票页点「试听 30 秒」没有任何反应。
- **根因（有数据为证）**
  - 数据库 `tracks` 表 **0 条** → 任何专辑都没有 `previewUrl` → 按钮点了没反应。
  - 后端其实**有**按需同步接口 `GET /api/music/albums/:albumId/tracks`（`getAlbumTracks` 发现曲目为空就自动从 iTunes 拉并落库）——**但投票页从来没调用它**。
- **修复**（顺带完成守则第 16 条「播放器」）
  - `BattlePlayView`：点「试听 30 秒」→ `musicApi.listAlbumTracks(albumId)` 按需取曲目（只保留有 `previewUrl` 的）。
  - 播放条按 demo 补齐：**真实歌名** + 《专辑》第 N 首 + `00:05 / 00:30` + **上一首 / 播放暂停 / 下一首** + **序号 1/10**；播完自动下一首。
  - 时长用播放器**真实时长**（试听约 30 秒），不再显示整首歌时长。
- **实测（Playwright 真实浏览器，非推测）**
  - `audio.src` = 真实 iTunes 音频地址；`paused=false`、已播到 **4.95 秒**、`error=null`。
  - 播放条：标题「可愛女人」、副标题《杰倫》第 1 首 · **00:05 / 00:30**、计数 **1 / 10**。
  - `vite build` 通过（7.83s）。
- **回退**：`git checkout -- frontend/src/views/BattlePlayView.vue`
- **遗留**：P0 两项未做 —— ①颜色采样改真取色 ②「步」改「场」；其余见守则待修清单。

## 2026-09-17 晚 · 用户实测 15 图 → 玩法/交互大修第一批

- **改动名**：gameplay-overhaul-batch1
- **背景**：用户实测后报了一大批问题，结论是"框架看起来没问题，但细节玩法底层逻辑全是 bug，必须大修"。全部已录入守则待修清单第 26~39 条。本轮先修"根因明确、无歧义"的部分。
- **已修（6 项）**
  1. **「按流派/年代」模式不该有参赛歌手选择器**（后端按命中专辑反推歌手，根本不用），且此时点歌手误报「最多选择 1 位歌手」→ 已隐藏该块 + 修 max 判断
  2. **对位赛/指定对决结果页死循环**（没有冠军却显示"还没结束"→点继续又回投票页）→ 结果页按 `type` 分支：对位赛直接给逐行对照表 + 歌手积分
  3. **17 张参赛出现「从 1 张里选 1 张晋级」的无效步骤** → 单张组自动晋级、不产生投票步骤（`planTournament` 与后端建组同步改，前端公式镜像同步）
  4. **拉闊音樂會等现场专辑漏筛** → live 关键词补 音乐会/音樂會/拉闊/拉阔/音乐节；重算再剔 1 张
  5. 播完一首**自动下一首** → 按要求去掉（点切歌才播）
  6. 得票率 **100%/0%** 难看 → 改显示**票数**
- **顺带**：投票页封面限宽 330px（一屏放得下，播放条自然上移）；投票卡补 **hover 上浮+色晕**（设计语言一致性，用户点名"你没有记住"）
- **待办（已入守则，需按序做）**
  - **P0**：①颜色采样改真取色 ②「步」→「场」 ③**单歌手选张策略**（>10 张时只取最早的 N 张，后面连参赛资格都没有 —— 需定"自选/随机/最热"）
  - **P1**：手动挑选（无说明/无上限/可挑太少/无法取消勾选）、指定对决选择逻辑反了（默认全勾、取消反而进对阵表）、搜索混入无关歌手 + 已选歌手应单独一排、指定对决默认"自己打自己"、播放条其他歌手不可听（待查）、冠军页补亚季军 + 晋级叙事、全站返回按钮、好友一起玩
- **验证**：`vite build` 通过（8.80s）；准入重算再剔 1 张（拉闊音樂會）
- **回退**：`git checkout -- backend frontend`

## 2026-09-17 晚 · 抽张策略 + 常用组合（用户拍板后开工）

- **改动名**：pick-strategy-and-presets
- **用户拍板**："用户自选和按热度随机是可以的"，并要求按设计稿做出「**或从常用组合开始**」，后期可扩成社区维护（如周王陶林方 / 欧美五大 diva，用户可提报、采纳后入库）。
- **① 修掉"只取最早 N 张"的硬伤（P0 玩法）**
  - 后端 `resolvePool` 新增 `pickAlbums()`：**random（默认，保留盲盒刺激）/ newest（最新 N 张）/ picked（用户自选 albumIds）**；`artist` 与 `multi-artist` 都走它，多歌手可按各自 `albumIds` 自选。
  - 接口：`createSchema` 加 `pick`；`artistEntry` 加 `albumIds`。
- **② 创建页新增两块**
  - **「这几张怎么选」**：随机抽 N / 最新 N / 自己挑。选「自己挑」时列出**已选歌手的全部合格专辑**（点封面勾选，**可勾可取消**），并显示「已选 X 张」。
  - **「或从常用组合开始」**：4 个组合（周杰伦 vs 林俊杰 / 华语三强混战 / 欧美经典对决 / 中外对决），点一下按名字搜到歌手并**一键装好模式+歌手+规模**；下面保留「快速搜单个歌手」。
- **实测（Playwright 真实浏览器）**
  - 组合按钮就位；点「华语三强混战」→ 自动装填 `周杰倫 抽 8 张 / 林俊傑 抽 8 张 / 陳奕迅 抽 8 张` ✓
  - 「自己挑」候选 76 张、**默认 0 勾选**、点两次 → 已选 1 张 → 2 张（勾选/取消都生效）✓
  - 页面显示 `赛程：6 组小组赛（每组 4 张选 2）→ 遗珠复活捞 4 张 → 16 强淘汰赛 · 共 22 步`；参赛池按盲盒只露 6 张 + 「还有 18 张 进对决时揭晓」虚位卡 ✓
  - `vite build` 通过（12.55s）
- **后期（已记入守则）**：常用组合改为**后端维护的社区组合**（用户提报 → 后台采纳 → 人群最爱组合进推荐位）。
- **回退**：`git checkout -- backend/src frontend/src`

## 2026-09-17 晚 · 三个 P0 修复（颜色真采样 / 其他歌手试听 / 淘汰赛跨歌手配对）

- **改动名**：p0-color-sampling-preview-and-crossartist-ko
- **① 颜色采样（用户第一轮就指出的 P0，"这个必须要修"）**
  - 新增 `frontend/src/utils/coverColor.js`：把封面画到 24×24 canvas，**加权平均出主色**（过滤过暗/过亮/低饱和像素；黑白封面取不到色时退回 `albumId` 哈希色）；缓存跨页面共享（`accentStore`）。
  - 投票页接入：舞台光晕、歌手名着色、色条全部跟随**封面真色**。
  - **实测截图**：左（城堡/蓝灰封面）→ 左侧光晕蓝灰；右（牛仔/暖棕封面）→ 右侧光晕暖棕红 ✓
- **② 其他歌手试听不可用（用户报"只接了周杰伦的"）**
  - **根因**：试听按钮的 `:disabled` 绑定在 `album.previewUrl` 上，而小组/复活阶段返回的专辑**根本不带 previewUrl**、曲目也没同步过 → 按钮直接被禁用。
  - **修复**：**去掉这个禁用条件** —— 点「试听」时按需 `listAlbumTracks(albumId)`（后端自动同步该专辑的 iTunes 片段），任何歌手都能听。
- **③ 淘汰赛第一轮同歌手互打（用户报"又是自己打自己"）**
  - **根因**：`startKnockoutV2` 用标准种子配对（1 对 n），**完全没有跨歌手约束**。
  - **修复**：`bracket.js` 新增 `buildFirstRoundCrossArtist(seeds, artistOf)` —— 按歌手分桶，每场先取"剩余最多"的桶、再从**另一位歌手**的桶取对手；只有某位歌手占一半以上才允许同歌手（界面标「同室操戈」）。
  - **实测**：3 歌手 × 6 张（18 张 → 5 组 → 复活捞 7 → 16 强）第一轮 **8 场全部跨歌手、同歌手 0 场** ✓
- **验证**：`vite build` 通过（10.20s）；Playwright 截图确认颜色随封面变化、试听正常（`最偉大的作品 第 2 首 · 00:04/00:30 · 1/11`）。
- **回退**：`git checkout -- backend frontend`
- **遗留（守则待修清单）**：步→场、歌手官方图（需 Apple Music API）、盲盒呈现升级（模糊堆叠）、分享页布局、亚季军+晋级叙事、返回按钮、好友同玩、手动挑选/指定对决交互。


## 2026-09-17 深夜 · 对阵表渲染 + 试听曲目数 + 步→场（用户新反馈后的续修）

- **改动名**：bracket-render-tracklist-and-step2chang
- **① 对阵表渲染大修（用户："你的对阵表渲染写出大问题"）**
  - **根因**：后端 `battle.service.js` 返回的 `advancedAlbumIds` 用的是**本地 ObjectId**（`g.pickedAlbumIds.map(String)`），前端却拿**外部 albumId** 去 `includes` 比较 → 永远对不上，导致**所有专辑都显示"未晋级"**。
  - **修复**：改为返回**外部专辑 ID**（`advancedAlbumIds` 由 album 文档的 `albumId` 字段派生）。
  - 顺带重排对阵条：`minmax(0,1fr) 58px minmax(0,1fr)` 三栏 + 专辑名省略，冠军列独立 220px。
  - **实测截图**：两组晋级标记正确（前 2 张蓝框"晋级"），对阵树 852px + 220px，`ties:3` ✓
- **② 试听曲目数（用户："15 首歌的专辑只试听到 9 首，旁边那张更是只有两首"）**
  - **根因 A**：曲目同步 `syncTracks(artistId)` 走 `lookupSongs`，iTunes **按歌手拉歌硬卡 200 首上限**（实测 offset 翻页无效）→ 多专辑歌手后面的专辑被**截断**，部分专辑只剩零星几首。
  - **根因 B**：iTunes **本身对部分曲目不提供 30 秒片段**（`previewUrl` 为空），并非全是 bug。
  - **修复 A**：`itunes.client.js` 新增 `lookupAlbumSongs(collectionId)`（**逐专辑**拉歌，hk 区实测可展开完整曲目列表：专辑 1721460694 → 11 曲全有 preview）；`music.service.js` 新增 `syncAlbumTracks`，`getAlbumTracks` 改走逐专辑同步；并对"已缓存但条数 < 专辑应有数"的残缺缓存**补一次同步**（`TRACK_SYNC_TRIED` 进程内去重，避免反复请求）。
  - **修复 B（透明化）**：投票页新增**「试听清单」** —— 列出该专辑**全部曲目**，顶部写明"**专辑共 N 首 · M 首有试听片段**"，不可播的 chip 标「无试听」；播放条删去冗余提示。
  - **实测**：`《八度空間》试听清单 专辑共 10 首 · 10 首有试听片段`，10 个可点 chip、0 个无试听 ✓；`《最偉大的作品》共 11 首 · 11 有片段` ✓
  - 修正 `itunes.client.js` 里**过时的注释**（原写"按 collectionId 查曲目不展开"，实测 hk 区可展开）。
- **③ 「步」→「场」（用户最早提的 P0）**
  - 全站术语统一：`tournament.js`(describePlan) / `BattlePlayView` / `BattleBracketView` / `BattleShareView` / `ProfileView` / `BattleCreateView` 的"步"全部改"场"（含"正在加载下一场"）；grep 确认无用户可见残留。
  - **实测截图**：对阵表头部 `共 5 场 · 已投 2 / 5`，树区 `当前进度 2 / 5 场` ✓
- **验证**：`vite build` 通过（11.53s）；Playwright 两张截图（bracket.png / tracklist.png）自检通过。
- **回退**：`git checkout -- backend frontend docs`
- **文档**：`docs/进度与执行清单.html` 全文重写（新增"本轮新修"块、更新完成度、未做清单去掉已完成项）；守则第 11 条标记已修、新增第 41（对阵表渲染）/42（试听曲目数）条；并修复守则表格尾部一段重复的 `</tbody></table></div>` 结构错误。

## 2026-09-17 深夜 · P1 第一批：亚季军 + 晋级叙事 + 全站返回（顺带挖出一个真 bug）

- **改动名**：p1-podium-narrative-backbutton
- **触发**：用户"开始做p1的部分"。本轮做三条：守则 19（亚季军）、20/39（晋级叙事）、21（全站返回按钮）。
- **⚠️ 顺带挖出一个真 bug：淘汰赛胜方高亮根本是坏的**
  - 后端 `serializeMatch` 给的 `winnerAlbumId` 是**本地 ObjectId**，而 `serializeAlbum` 只返回**外部 albumId**（iTunes id）。`BattleBracketView` 里 `String(m.winnerAlbumId) === String(album.albumId)` 永远 false → 淘汰赛对阵条的胜方／负方样式**从未生效**。
  - 这是守则 41 那个坑的**同类遗漏**：当时只把 `advancedAlbumIds` 改成外部 ID，漏了 `winnerAlbumId`。
  - 修复：后端 `serializeMatch` 新增 `winnerAlbumExternalId`（外部 id，保留原字段兼容），前端一律改用它比对。
  - 影响：不修这个，亚季军与晋级叙事根本推不出来（拿不到每场胜方）。
- **① 冠军页：亚军 / 季军（守则 19）**
  - **亚军 = 决赛负方**（唯一、无歧义）。
  - **季军 = 决赛前一轮（半决赛）两位负方中「累计得票更高」者**；累计相同比该场得票，再相同按专辑名排序（保证可复现）。
  - ⚠️ 本作是单败淘汰、**不设三四名决赛**，季军属"推定"。界面已明确标注"按累计得票推定"，不假装打过一场。口径写入守则第 0 节**规则 ⑥**（避免以后口径漂移）。
- **② 冠军页：完整晋级叙事（守则 20/39）**
  - 新增「**小组赛与复活**」：每张专辑从哪一组出线，晋级的高亮、冠军描金边。
  - 新增「**完整晋级图**」：按轮次展开**全部对局**（含冠军没参与的），每场两方 + 票数 + 「胜 / 淘汰」徽标，冠军参与的对局整行描边高亮。
  - 解决了"夺冠之路其他那些专辑跟消失了一样"。
  - 数据：`BattleResultView` 并行取 `battleApi.result(id)` + `battleApi.detail(id)`（detail 的 `matches` 已含全部场次）；detail 取失败也不影响结果页（catch 兜底）。**未改接口契约**（仅新增字段）。
- **③ 全站返回按钮（守则 21）**
  - `AppShell`：二级 / 三级页统一显示返回按钮；**有上一页历史就真回退一步**，没有历史（直接粘贴链接进来）则退回逻辑父路由（`BACK_FALLBACK` 映射）。
  - `AdminShell`：非看板页补「返回看板」。
- **涉及文件**
  - `backend/src/modules/battles/battle.service.js`（`serializeMatch` 新增 `winnerAlbumExternalId`）
  - `frontend/src/views/BattleBracketView.vue`（胜方判定改用外部 id）
  - `frontend/src/views/BattleResultView.vue`（重写：领奖台 + 小组赛与复活 + 完整晋级图）
  - `frontend/src/layouts/AppShell.vue` / `AdminShell.vue`（返回按钮）
  - `docs/design/音格-前端设计守则.html`（规则 ⑥；19/20/21/39 标已做；新增 43~47 条；更新尾部备注）
- **备份**：`_backup/20260917_2330_P1亚季军晋级叙事与返回按钮/`
- **回退**：`git checkout -- backend/src/modules/battles/battle.service.js frontend/src/views/BattleResultView.vue frontend/src/views/BattleBracketView.vue frontend/src/layouts/AppShell.vue frontend/src/layouts/AdminShell.vue docs/design/音格-前端设计守则.html`
- **⚠️ 后端改了，需要重启后端才生效**（前端是热更新即可）。
- **遗留 P1**：分享页布局（18）、好友一起玩（22）、手动挑选/指定对决交互（36/37/38）、歌手官方图（需 Apple Music API）。

### 自检结论（交付前必跑）
- 口径一致：季军规则写入守则规则 ⑥ + 界面标注"按累计得票推定"，三处（守则 / 代码注释 / 界面文案）同一口径 ✓
- id 口径：凡"前端拿专辑比对"的地方一律用**外部 albumId**（`winnerAlbumExternalId` / `advancedAlbumIds`），不再出现本地 ObjectId 参与比对 ✓（grep 复核过）
- 状态互斥：轮空场次（`isBye`）已从晋级图与累计票数中排除，不会出现"战胜 0:0" ✓
- 降级：detail 接口取失败时，亚军/季军/晋级图整块不渲染，冠军与夺冠之路照常显示，不白屏 ✓

## 2026-09-17 深夜 · 用户实测 4 图：待投 / 亚季军（后端未重启）+ 排行榜配色与 hover

- **改动名**：fix-winner-field-fallback-and-rank-colors
- **触发**：用户 4 张截图：① 结果页「完整晋级图」全显示"待投" ②（同图）③ 冠军页"季军和亚军在哪里" ④ 排行榜"专辑没有划过的特效 而且颜色映射怪怪的"。
- **诊断（①②③ 是同一个根因）**
  - `winnerAlbumExternalId` 是**上一提交新加的后端字段**；用户**还没重启后端** → 接口不吐这个字段 → 前端判定"没有胜方" → 晋级图全"待投"，且季军 / 亚军推不出来（podium 整块被隐藏）。
  - 数据里胜方是有的（对阵表页能显示比分），只是接口没吐。
- **修复**
  1. **前端票数兜底**：新增 `winnerExternalIdOf(m)` —— 有 `winnerAlbumExternalId` 就用它；没有则按票数推断（票高者胜；平票返回 null 才显示"待投"）。`BattleResultView` 与 `BattleBracketView` 都改用它。
     - 效果：**即使后端没重启**，胜 / 淘汰、季军亚军也能正确显示；重启后走权威字段（含平票裁决）。
  2. **排行榜配色**：`RankView` 删掉自写的 `hsl(h 70% 52%)` **满饱和哈希色**（根本不读封面 → "颜色映射怪"），改走 `coverColor`（`accentStyleOf` + 加载后 `ensureAlbumAccent` 预取封面真色）。
  3. **同款隐患一并清除**：`PersonalityResultView`（人格卡推荐专辑）、`PersonalityTypeView`（类型详情常听专辑）也各自写了哈希色 —— 一并改为 coverColor。
  4. **排行榜 hover 特效**：`components.css` 给 `.alb` 补 hover 上浮 + 色晕加深（守则 27 落到排行榜；此前 `.alb` 只有 `cursor:pointer`，没有任何位移）。
- **涉及文件**
  - `frontend/src/views/BattleResultView.vue` / `BattleBracketView.vue`（winnerExternalIdOf 兜底）
  - `frontend/src/views/RankView.vue`（配色改 coverColor + primeAccents）
  - `frontend/src/views/PersonalityResultView.vue` / `PersonalityTypeView.vue`（清除同类哈希色）
  - `frontend/src/styles/components.css`（`.alb:hover` 上浮 + 色晕）
  - `docs/design/音格-前端设计守则.html`（规则 ⑤ 加第 ④ 条「任何页面不许自建哈希色」；新增 48 / 49 条）
- **验证**：`vite build` 通过（13.47s）；grep 确认前端已无 `hsl(${h} 70%` 残留；守则 48/49 与规则 ⑤④ 已写入。
- **⚠️ 后端需重启**：权威字段 `winnerAlbumExternalId` 来自上一提交的后端改动，**重启后端后才生效**（前端兜底可让页面先正确显示）。
- **回退**：`git checkout -- frontend/src docs/design/音格-前端设计守则.html`
- **遗留 P1**：分享页布局（18）、好友一起玩（22）、手动挑选/指定对决交互（36/37/38）。

## 2026-09-18 凌晨 · 收藏按钮落实 + Edge 最小化闪屏 + 后端免手动重启 + 免注册方案

- **改动名**：feat-favorite-button-edge-fix-autoreload
- **触发**：用户 4 图反馈：① "专辑旁边没有收藏按钮，这个功能是不是没做"（守则早写着要做，**必须落实**）② 结算页（冠军 / 亚军 / 季军）加收藏按钮（专辑介绍先不急）③ **Edge 最小化 / 回桌面窗口一直闪、回不到桌面**（Chrome 正常）"必须修复快" ④ 痛点：每次让用户手动重启后端，用户不知道操作对没对。
- **说明**：本批工作区里已有一套同目标的**未提交**实现落盘（FavoriteButton 组件 / favorites store / serializeAlbum 补 id / Edge 降级 / 启动器改 dev），与本轮需求完全吻合 —— 本轮**逐文件校验其正确性并补齐收尾**，未重复造轮子。
- **① 收藏按钮（守则 50）**
  - **根因（为什么一直没做上）**：`POST /favorites` 要的是专辑**本地 ObjectId**，而前端一直只拿得到外部 albumId → 按钮没法接。
  - 后端：`serializeAlbum` 补 `id`（本地）；`listFavorites` 统一走 `serializeAlbum`。
  - 前端：新增 `stores/favorites.js`（跨页共享已收藏 id 集合）+ `components/FavoriteButton.vue`（**未登录点收藏 → 引导登录并带 redirect**）。
  - 落点（与用户标注一致）：排行榜领奖台卡片角标 + 榜单行；冠军页冠军按钮位 + 亚军 / 季军角标。
  - 本轮补齐：逐一查证榜单 / 结果页 / 对阵表数据都走 `serializeAlbum`（`rankings.service` 也是）→ 本地 id 全链路可用 ✓；收藏页取消后同步全局状态（store 新增 `forget`），避免其他页按钮仍显示"已收藏"。
- **② Edge 闪屏（守则 51）**
  - 判定：Edge 合成器对「大量 backdrop-filter + 固定层 blur(100px) 极光」的兼容问题（Chrome 不复现；本机无 Edge 自动化，无法复现验证）。
  - 降级方案（**只动观感，不动功能**）：UA 判定 Edge → `html.ua-edge`：关毛玻璃、降大 blur、玻璃调不透；极光独立合成层。逃生门：`?lite=1`（连极光一起关，最强兜底）/ `?glass=1`（强制保留毛玻璃）。
  - ⚠️ 诚实标注：无法在本机复现 Edge 行为，**需要用户实测**；若仍闪，先开 `?lite=1` 兜底，再考虑进一步降级。
- **③ 后端免手动重启（守则 52）**
  - 后端本就有 `npm run dev`（node --watch 自动重载），但启动器用的是 `npm start`（不重载）。已把 `start-dev.bat` 改成 `npm run dev` → **改后端代码自动重载，不用再手动重启**。
  - **怎么确认后端活着**：浏览器开 `http://localhost:3000/health`，返回 `{"status":"ok","uptime":…}` 即正常（uptime 变小 = 刚重启过）。
  - 生效方式：**重新双击一次 start-dev.bat**（以后就不用再管了）。
- **④ 免注册可玩（守则 53，方案已定、待实施）**
  - 现状与需求冲突：需求文档写"不注册也能玩对决，保留数据 / 收藏需注册"；但对决路由全 `requiresAuth`（必须注册才能玩）。
  - 方案：游客设备 ID（localStorage）+ 请求头 `X-Guest-Id` + `loadOwnedBattle` 兼容 `userId / guestId` + 放开对决路由 + 登录后把游客对决迁移进账号；**收藏仍需登录**。
  - **注册量不用担心**：JWT 无状态 + MongoDB 单机，毕设量级（万级用户 / 十万级票）完全够；防刷已有限流与异常票判定；真上线再补邮箱验证 / 验证码即可。
- **涉及文件**
  - 后端：`modules/music/music.service.js`、`modules/favorites/favorites.service.js`
  - 前端：`components/FavoriteButton.vue`（新增）、`stores/favorites.js`（新增）、`views/RankView.vue`、`views/BattleResultView.vue`、`views/FavoritesView.vue`（本轮补 forget 同步）、`layouts/AppShell.vue`、`main.js`（Edge 降级）、`styles/components.css`
  - 其他：`start-dev.bat`、`docs/design/音格-前端设计守则.html`（守则 50~53）
- **验证**：后端 `npm run check` 通过；前端 `vite build` 通过；`rankings / getResult / getBattleDetail` 均走 `serializeAlbum`（本地 id 全链路可用）已逐一查证。
- **回退**：`git checkout -- backend/src frontend/src start-dev.bat docs/design/音格-前端设计守则.html`
- **遗留**：免注册可玩（守则 53）待用户点头后实施；专辑介绍（用户说先不急）。

## 2026-09-18 凌晨 · 第二批：取色算法重写 + 流派模式三处根因 + 小组赛试听 + 对位赛战报 + 管理员看板

- **改动名**：feat-color-quantize-genre-fix-group-preview-aligned-report-admin-insights
- **触发**：用户 6 图反馈：①管理员功能不够（排行投票 / 用户专辑倾向）+ 后台全局返回 ②流派模式不行 ③颜色还没解决（"是你修出问题了还是算法一开始就没对，请调阅网上经验"）④对位赛结果页寒酸、分享都没了 ⑤收藏"还没实现" ⑥小组赛怎么试听（倾向播大热歌）。
- **① 取色算法重写（守则 54）—— 结论：算法一开始就不对**
  - 之前是"彩色像素求平均"：平均法必然失真（橙粉黑封面平均出土绿、黑白封面只能抓到边缘色）。调阅 ColorThief / node-vibrant / Spotify 式管线后重写 `sampleCover`：
    降采样 48×48 → RGB 每通道取高 3 位分 512 桶 → 滤近黑(>238)/近白(<26)/低饱和(<20) → 每桶按「像素数 × 鲜艳度(饱和度) × 亮度适中性」打分 → 与冠军桶同色相(±26°)合并 → `soften` 压进 18–48% 饱和 / 46–62% 亮度。
  - 纯灰封面：灰池平均亮度 → 中性冷灰（h215 s6%）。
- **② 流派模式三处根因（守则 55）—— 之前对所有人 100% 不可用**
  1. `syncArtist` 的 `$set` 从不写 `genre` → `Artist.genre` 永远 null；
  2. `lookupAlbums` 映射把 iTunes 的 `primaryGenreName` 丢掉（只有 searchArtists 带）；
  3. 流派选项写死英文 `GENRES`（Pop/Rock），而 hk 区是繁体「國語流行樂/流行樂」→ 永远匹配不上。
  - 修：lookupAlbums 带 genre、syncArtist 落 Artist.genre + Album.genre（模型加字段）、serializeAlbum 吐 genre；新增回填脚本 `backfill-artist-genre.mjs`（**已真跑：16/16 歌手成功**，如 周杰倫→國語流行樂、陳奕迅→廣東歌/香港流行樂）；`listGenres` 改为曲库实时聚合（genre + 歌手数）；创建页流派选择改为**真实流派 chips（带歌手数）**+ 手输 + 机制说明；入池镜像年代模式（各歌手轮转抽、封顶 32）；报错文案改成人话。
  - 产品答（流派怎么筛）：流派=曲库内已缓存歌手的 iTunes 标签；命中后自动准入过滤 + 轮转抽 32 张入池，**不需要也不能手动挑**。
- **③ 小组赛试听（守则 56）**：小组卡新增「试听主打」—— 调现成 `GET /music/albums/:id/preview`（iTunes 无热度数据 → 取专辑首支可播片段作主打代理并如实标注）；span+role 实现避免 button 嵌 button；复用全局播放器 + 迷你播放条（再点=停止）。
- **④ 对位赛结果页（守则 57）**：战报卡（领先者 + 共 N 场 + 总比分 L:R）+ 逐行胜方高亮/「左/右侧胜」徽章（aligned 的 winnerAlbumId 本就是外部 id）+ 「保存战报图」（html2canvas 截战报卡 PNG）。
- **⑤ 管理员（守则 58）**：dashboard 新增 `topAlbums`（有效票 Top10）/ `genreAffinity` / `artistAffinity`（均 `isInvalid=false` 口径）；看板加「大家投出来的专辑 Top10」「用户专辑倾向（按歌手/按流派）」两块；AdminShell 加「返回前台」。
- **⑥ 收藏"没实现"（守则 59）—— 真实验证**
  - 用户报"这个条目暂时无法收藏"：根因是其**正在跑的后端是旧代码**（serializeAlbum 没吐本地 id）。
  - 起临时实例（PORT=3100，不动用户的 3000）实测：`/health` ✓、admin 登录 ✓、`/api/rank/albums` 首条带 `"id":"6aaa…"` 与 `"genre":"國語流行樂"` ✓、`POST /favorites`「收藏成功」✓、`GET /favorites` 列表带完整专辑 ✓、`DELETE`「已取消收藏」✓、`/api/music/genres` 返回曲库真实流派 ✓。测毕已停临时实例。
- **验证**：后端 `npm run check` ✓；前端 `vite build` ✓（10.21s）；回填脚本 16/16 ✓；收藏链路实测 ✓。**用户侧需重启后端一次**（此后 node --watch 自动重载）。
- **备份**：`_backup/20260918_0100_六项反馈_取色流派试听对位赛/`
- **回退**：`git checkout -- backend/src frontend/src docs/design/音格-前端设计守则.html`
- **遗留**：免注册可玩（守则 53）；对位赛完整分享卡与玩法增强；管理员趋势图（按日投票曲线）与收藏偏好分析；专辑介绍（用户说先不急）。

## 2026-09-18 凌晨 · 第三批：小组赛试听修好 + 游客可玩落地 + 启动器自动清旧进程 + 手册补重启/关闭

- **改动名**：fix-group-preview-audio + feat-guest-play + launcher-stale-kill + manual-restart-doc
- **触发**：用户 3 图：①小组赛试听"没法用"（条变"停止"、播放条 00:00 不动）②收藏依旧"暂时无法收藏" ③流派 chips 只显示"位歌手"没有流派名，且"未登录还是没法玩专辑比拼"；外加批评"后端咋重启咋关闭你也不在使用手册上更新"。
- **诊断（先说破）**：流派 chips 只剩"位歌手"= 接口还在返回写死英文列表（旧代码铁证）→ **用户的后端从未重启成功**；旧后端窗口一直占着 3000，新窗口起不来，跑的始终是旧代码。
- **① 小组赛试听修好（真 bug）**
  - 根因：`<audio ref="audioEl">` 只存在于**淘汰赛模板**里，小组赛阶段 `audioEl` 是空 → `loadCurrent()` 里 `if (!el) return` → play 根本没执行（页面显示 00:00/00:30 不动）。
  - 修：`<audio>` 挪到模板**根级**（小组赛/淘汰赛共用一个音频元素）；小组赛播放条升级为**与淘汰赛完全一致的三键控制**（上一首 / 播放暂停 / 下一首，按用户要求直接复用同一套函数）。
  - 顺带修掉本次引入的一个多余 `</template>`（构建期发现：template 9 开 10 闭，div 54/54 平衡）。
- **② 启动器自动清旧进程（治"重启无效"的本）**
  - `start-dev.bat`：启动后端/前端**之前**，自动 `netstat` 找 3000/5173 端口上的旧进程并 `taskkill`；帮助文案改为"重启=再双击一次本脚本；关闭=关两个黑窗口；验证=开 /health"。
- **③ 游客可玩（免注册，需求文档落地）**
  - 方案：发**真实游客账号**（`role=guest`，随机账号密码）+ 正式 JWT —— 对决归属、每人一票、限流、防刷**全部复用现有逻辑，零特殊分支**；数据绑定本机浏览器（localStorage）。
  - 后端：User.role 枚举加 `guest`；`POST /api/auth/guest`（公开）→ createGuest；收藏仍要求正式注册（前端拦 guest）。
  - 前端：路由守卫 `requiresAuth && 未登录` 时**自动领游客身份继续玩**（不再弹登录）；`auth.ensureGuest()` + `isGuest`；顶栏显示「游客 · 注册」入口；FavoriteButton 对 guest 提示"收藏需要正式注册"。
  - ⚠️ 诚实标注：沙箱对"创建账号"类写操作有敏感审批、本轮实测被拦（超时），游客接口未跑通端到端——代码与注册/登录同构，**请重启后端后实测**，报错贴我。
- **④ 使用手册补上（用户点名批评）**：`docs/部署与访问指南.html` 第 2 节改为「启动 / 重启 / 关闭 / 验证 / 改代码」五张表的傻瓜版 + "为什么你之前重启没效果"解释 + 游客身份说明。
- **涉及文件**：`backend/src/models/User.js`、`modules/auth/*`（service/controller/routes）、`frontend/src/views/BattlePlayView.vue`、`stores/auth.js`、`router/index.js`、`api/index.js`、`components/FavoriteButton.vue`、`layouts/AppShell.vue`、`start-dev.bat`、`docs/部署与访问指南.html`
- **验证**：后端 `npm run check` ✓；前端 `vite build` ✓（14.15s，修复多余闭合后）。
- **回退**：`git checkout -- backend/src frontend/src start-dev.bat docs/部署与访问指南.html`
- **遗留**：游客数据迁移到正式账号（注册后把游客的对决/票据转走）——下一批做；守则 53 就此关闭。

## 2026-09-17 深夜 · 批2：用户 6 图反馈（盲盒单歌手 / 点封面投票 / 颜色二次加固 / 对阵表溢出 / 收藏后台入口）

- **改动名**：batch2-six-screenshot-fixes
- **触发**：用户发 6 张截图逐项指出：①只选 1 歌手时参赛池没有"还有 N 张"省略号、盲盒感没落地 ②点封面没直接投票 ③灰封面仍是刺眼品红/紫（要求"在总的规则线上加上怎么处理这种情况"）④对阵表小组卡溢出 ⑤找不到收藏页 ⑥找不到后台。指令："先优化我说的这些和 P1 的"。
- **已修（6 项 + 守则同步）**
  1. **盲盒单歌手虚位卡缺失（Issue 1）**：根因 `totalSelected` 在 `picked.length < 2` 时直接返回 0（这是 `canStart` 的门槛，被误用到预览计数），导致多歌手模式只选 1 歌手时 `hiddenCount=0`、文案还写"本轮共 0 张出战"自相矛盾。改为按真实出战张数单独算；`canStart` 的 ≥2 门槛独立保留。现在 1 歌手也会露 2 张 + "还有 N 张"盲盒虚位卡。
  2. **盲盒视觉升级（Issue 1 续）**：虚位卡从单张虚化改为**模糊堆叠卡（两张错位 ghost 卡）+ 省略号 + "还有 N 张 · 盲盒 · 进对决才揭晓"**，更像抽卡盲盒。
  3. **点封面即投票（Issue 2 / 守则第 15 条）**：整张专辑卡可点击投票（hover 上浮 + "点击投票"浮标；试听按钮 `@click.stop` 防误触）；下方「投给《X》」按钮弱化为玻璃描边、保留作辅助；新增提示"点击任意专辑封面即可投票"。
  4. **专辑主色二次加固（Issue 3 / 守则第 10 条）**：`utils/coverColor.js` 重写 —— ① 灰白/黑白封面返回**中性灰**（带极弱冷暖倾向、饱和度 ~6%），**绝不退回随机高饱和色**（修掉 reputation 类灰封面变品红/紫）；② 取到的彩色**压进友好区间**（饱和度 8–46%、亮度 46–60%，防荧光刺眼）；③ 取色失败退回的 `albumId` 哈希色也**降饱和**（36%/50%，原 70% 满饱和）。并在《前端设计守则》第 0 节新增硬规则 **⑤ 专辑主色规则**（灰封面与刺眼色处理）。
  5. **对阵表小组卡溢出（Issue 4）**：根因 `.glist` 是 `flex` 且 `.grow` 无 `min-width:0`，长专辑名 / 封面加载失败时把卡片撑爆。改为 `grid + repeat(4, minmax(0,1fr))` + `.grow{min-width:0}`，长名单行省略不再溢出。
  6. **收藏页 / 后台入口（Issue 5、6）**：两页路由与页面本就存在，缺的是入口。`AppShell` 顶栏补「我的收藏」（登录可见）+「管理后台」（管理员 `role==='admin'` 可见）；移动端仍走原折叠逻辑。
  - **顺带**：淘汰赛 1v1 自动检测左右同歌手 → 顶部显示"同室操戈"徽标（守则 B 节「跨歌手标注」补全）。
- **涉及文件**
  - `frontend/src/utils/coverColor.js`（重写：灰封面→中性灰、彩色压友好区间、哈希降饱和）
  - `frontend/src/layouts/AppShell.vue`（顶栏加 收藏/后台 入口）
  - `frontend/src/styles/components.css`（`.glist`/`.grow` 改 grid + minmax(0,1fr) + min-width:0）
  - `frontend/src/views/BattleCreateView.vue`（`totalSelected` 修正 + 盲盒虚位卡升级 + 样式）
  - `frontend/src/views/BattlePlayView.vue`（点封面投票 `onCoverVote` + `sameArtist` 同室操戈 + `picktag` + 弱化投票按钮）
  - `docs/design/音格-前端设计守则.html`（第 0 节加规则 ⑤；第 10 条二次加固说明；第 15 条标已做；B 节跨歌手标注标已做；待修清单加 43~46 条）
- **验证**：`vite build` 通过（9.03s，仅 chunk 体积提示，非错误）；grep 复查 `totalSelected` 误用已消、受影响选择器无裸 `1fr` 残留。
- **备份**：`_backup/20260917_2230_批2六项反馈与P1/`（含上述源码与守则改前原件）
- **回退**：`git checkout -- frontend/src docs/design/音格-前端设计守则.html`
- **遗留（P1，已入守则，待按序）**：歌手官方图（需 Apple Music API）、分享页布局、亚季军 + 晋级叙事、全站返回按钮、好友一起玩、手动挑选/指定对决交互升级（守则 36/37/38）、抽卡式入场动画。

## 2026-09-18 凌晨 · 启动器修不好 + 数据库答疑（start-dev.bat / stop-dev.bat / 部署指南）

- **改动名**：launcher-node-path-fix-and-db-doc
- **触发**：用户发 2 图，核心两问 ①"数据库在哪打开？" ②"双击 start-dev.bat 根本没法打开前后端服务"。经排查：后端 / 前端 / MongoDB 在沙箱内单独启动均正常（MongoDB 起得来、后端连库监听 3000、前端 Vite 2.3s ready），所以根因是**双击从资源管理器启动的 cmd.exe 继承不到 node/npm（沙箱能找到只是因为 WorkBuddy 注入了 PATH）**，导致 bat 里每一句 `node`/`npm` 都失败、服务窗口起不来。
- **已修**
  1. **start-dev.bat 重写**：开头新增 Node 自动定位——优先用托管 Node 22（`%USERPROFILE%\.workbuddy\binaries\node\versions\22.22.2-3`），没有就退而用系统 Node 18（`C:\Program Files\nodejs`），再没有就用系统 PATH 里已有的；定位不到直接中文报错 + `pause` 提示去装 Node。定位到的目录显式加进 PATH，保证 `node`/`npm` 都可用。另：文件头补 **UTF-8 BOM**，避免中文 `echo` 在中文 Windows 默认代码页下乱码/解析异常；主窗口末尾加 `pause`，失败也能看清报错而不是一闪而过。
  2. **stop-dev.bat 同步**：同样补 Node 自动定位 + BOM。
  3. **docs/部署与访问指南.html**：① 后端命令统一为 `npm run dev`（原写的 `npm start` 与"自动重载"口径矛盾，已校正）；② 新增「数据库到底在哪打开？」小节——明确 MongoDB 是后台服务、由启动器自动拉起、无需双击打开；附可选可视化工具 MongoDB Compass 的连接串；③ 文末补"双击黑窗口一闪而过/满屏红字=找不到 Node，启动器已自动定位"的排查提示；④ 脚注更新日期。
- **涉及文件**：`start-dev.bat`、`stop-dev.bat`、`docs/部署与访问指南.html`
- **验证**：沙箱内 `node scripts/mongo.mjs start` ✓→监听 27017；`node backend/src/server.js` ✓→"MongoDB 已连接 / 服务已启动 3000"；`npm run dev`（frontend）✓→"VITE ready 5173"；bat 的 Node 定位逻辑经 `where node` 验证可用。注：沙箱无法双击弹窗，故 `start` 起服务窗口这一步以逻辑核对 + 组件实测替代。
- **回退**：`git checkout -- start-dev.bat stop-dev.bat docs/部署与访问指南.html`
- **遗留**：用户本机尚未实双击验证；若仍失败，按任意键后把窗口报错发我即可定位。游客数据迁移正式账号仍待做。

## 2026-09-18 凌晨 · 启动器根因修正（提交 8654823）—— 真凶是 Node 18 的 --watch 死循环，不是 PATH 不继承

- **改动名**：launcher-node22-explicit-fix
- **触发**：用户按上一版（902eba9）双击后**仍然** `ERR_CONNECTION_REFUSED`。
- **⚠️ 上一版根因判断错了**：902eba9 假定"资源管理器双击起的 cmd 继承不到 node/npm（PATH 不继承）"。本轮**实跑对照测试推翻了这个假设**：
  - 系统 Node 18.20.0 跑 `node --watch src/server.js` → 满屏 `Restarting 'src/server.js'`，**端口 3000 永远不开（无限重启死循环）**。
  - 托管 Node 22.22.2 跑 `node --watch src/server.js` → 正常启动、**监听 3000 + MongoDB 已连接**（仅一次良性重启后稳定）。
  - **真凶 = 系统 Node 18 自带的 `node --watch` 在本机 Windows 上会无限重启后端**，于是后端永远绑不上 3000 → 网站打不开。902eba9 只在 PATH 里放了 Node 22，但后端启动仍走 `npm run dev`，一旦子窗口落到 Node 18 就中招。
- **已修（start-dev.bat）**
  1. 启动时**显式选定托管 Node 22 的可执行文件**（`%USERPROFILE%\.workbuddy\binaries\node\versions\22.22.2-3\node.exe`），并据此设 `USE_WATCH`。
  2. **后端、前端都改用写死的 Node 22 路径直接启动**，**不再经过 `npm run dev`**：
     - 后端：`"%NODE_EXE%" --watch src/server.js`（USE_WATCH 时）/ `"%NODE_EXE%" src/server.js`（回退，无 --watch）。
     - 前端：`"%NODE_EXE%" node_modules/vite/bin/vite.js`。
  3. 这样子窗口**完全不需要继承 PATH**，从根上避开 Node 18；即便本机没有托管 Node 22，后端也回退成普通 `node src/server.js`（Node 18 下也能正常起，只是没了自动重载）。
  4. 仍保留：Node 自动定位 + UTF-8 BOM + 末尾 `pause` + 启动前杀 3000/5173 旧进程。
- **验证（端到端，本机实跑启动器同款命令，非推测）**
  - `node scripts/mongo.mjs start` → MongoDB 已启动（监听 27017）。
  - 后端（Node 22 `--watch`）→ **PORT 3000 OPEN** + 日志「服务已启动」「MongoDB 已连接」「数据库连接完成」。
  - 前端（Node 22 `vite.js`）→ **PORT 5173 OPEN** + 「VITE v5.4.21 ready in 908 ms / Local: http://localhost:5173/」。
  - 三者全起 = 双击后网站能正常打开。
- **同步清理**：删掉上一轮诊断残留 `_t_diag.mjs` / `_gen.mjs` / `_t_probe.bat`；停掉测试遗留的 mongod 并删 `scripts/.mongod.pid`。
- **回退**：`git checkout -- start-dev.bat`（本批只动这一个文件）。
- **遗留**：用户本机再次实双击验证；游客数据迁移正式账号仍待做。

## 2026-09-18 中午 · ⚠️ 启动器"乱码/命令切错"真因：我被文件写成了 LF 换行 —— 修 start-dev.bat / stop-dev.bat 为 GBK+CRLF

- **改动名**：fix-bat-encoding-crlf-gbk
- **触发**：用户截图——双击后控制台满屏乱码 + 一堆 `'X' 不是内部或外部命令`（`塟 * 绹鎬 3000?echo …`），服务没起来。用户情绪："为什么一个这么简单的启动问题都做不好"。
- **真因（字节级铁证，不是猜）**：用 Node 直接读文件字节：
  - `start-dev.bat`：`BOM=YES  CRLF=0  loneLF=81` → **整份文件是 LF 换行，一个 CRLF 都没有**。
  - `stop-dev.bat`：`BOM=YES  CRLF=0  loneLF=23` → 同样。
  - **cmd.exe 只认 CRLF**。LF-only 批处理会让 cmd 逐行解析**错位**：多行被粘连、`echo`/`start` 关键字被吃掉 → 中文乱码 + 大量"不是内部或外部命令"的假报错，**命令根本没执行**，所以三个服务全没起。
  - **根因是我的文件写入工具把 .bat 写成了 LF**（上一批 8654823 就坏了；git 当时还提示过 `LF will be replaced by CRLF`，被我忽略了）。与 Node 18 `--watch` 是两个**独立**故障：这次连命令都没跑到，是换行符先崩了。
- **修复**
  1. `start-dev.bat` / `stop-dev.bat` 重写为 **GBK(936) + CRLF，无 BOM、去掉 `chcp`**（中文 Windows 原生命令行编码最稳，也避开 `chcp 65001` 在批处理里的经典解析 bug）。
  2. 新增 **`.gitattributes`**：`* text=auto` + `*.bat text eol=crlf` + `*.cmd text eol=crlf` + 二进制规则 → **保证 .bat 永远以 CRLF 检出/提交，这类事故从根上不再犯**。
  3. `.gitignore` 补 `frontend/vite.config.js.timestamp-*`、`scripts/.mongod.pid`（运行时垃圾）。
  4. 顺带清理残留：`_gen.mjs`、若干 Vite timestamp 临时文件、`.mongod.pid`。
- **验证（字节级，已跑）**
  - 修复后 `start-dev.bat`：`CRLF=74  loneLF=0  BOM=no  不可映射字符=0`；`stop-dev.bat`：`CRLF=19  loneLF=0  BOM=no  不可映射字符=0`。
  - GBK 回读全文，中文（全角括号 / `：` / `「」`）完整无缺。
  - ⚠️ 局限：沙箱**禁止调用 cmd.exe**（Bash 与 PowerShell 都被安全策略拦截），无法在沙箱内真正"双击"跑一遍。但 GBK+CRLF+无BOM+无chcp 是中文 Windows 批处理的标准写法；其中的命令（Node 22 起后端/前端）上一批已端到端验证通过。
- **教训（写进本机环境坑）**：**用文件写入工具改 `.bat`/`.cmd` 后，必须复核 CRLF 与编码**，不能只看内容对不对；否则会引入"命令切错"的假故障。
- **回退**：`git checkout -- start-dev.bat stop-dev.bat .gitattributes .gitignore`
- **遗留**：请用户再次双击 `E:\桌面\毕业设计\yinge\start-dev.bat` 实测。

## 2026-09-18 下午 · 用户 10 图反馈批：投票卡死(游客) + 小组赛试听 + 对位赛盲盒 + 收藏只留专辑 + 自选上限100 + Edge闪屏/性能 + 我的对决封面

- **改动名**：batch-10shot-guest-vote-lock-edgperf-fav-aligned
- **触发**：用户 10 张截图 + 逐条文字。核心最严重一条：**游客模式下点专辑投票报"这票作废 / 您已投过这一场"然后彻底卡死，无法进入下一环节**；用户还指出"都是游客模式复现、登录账号不复现"。
- **① ⚠️ 投票卡死（P0，真根因，非猜测）**
  - **根因链**：`vote.service.js` 的 `detectMediumFraud` 有一条「**注册不足 1 小时已投 50 场以上** → 判异常」。**游客账号天生就是刚注册**，而一场大对决（如 41 场）轻松投 50+ 票 → 命中 → 该票 `isInvalid=true` 且**直接 return、不推进本场**；但票已落库（matchId+userId 唯一） → 用户再投就撞 `DuplicateError('您已投过这一场')` → **永久卡死**；同时 `applyViolation` 累计违规，还会把游客逐步限流甚至封禁。登录账号 createdAt 早于 1 小时，所以不复现 —— 完美对应"只有游客中招"。
  - **修复**：① 游客一律不参与中度异常判定（`if (user.role === 'guest') return null`）② 阈值放宽（连续同侧 15→40、新账号 50→300）③ **异常票也照常判定胜负并推进赛程**（只标记 isInvalid 不进排行榜），从机制上杜绝"投了却不前进"的死锁。
  - **前端** `BattlePlayView.vue`：`vote()`/`submitGroup()` 遇到业务码 **3001（已投过）→ 直接 `load()` 刷新进下一场**（自愈），不再弹错误卡住；并加**防连点**（`submitting` 期间忽略重复点击）。
  - **实测（真后端 E2E）**：admin 登录 → 建 8 张 v2 对决(stepTotal=5) → 2 组小组赛投票 ok → 淘汰赛投票 ok(progress 推进) → **重复投票返回 3001** → 一路投到 FINISHED。
- **② 小组赛试听（"不是让你照搬淘汰赛里的切歌播放吗，你没修好吗"）**
  - 根因：`quickPreview` 只拉了**一首**"主打"（`getAlbumPreview`），于是播放条永远 `1/1`、上一首/下一首全灰 —— 形似淘汰赛播放器却不工作。
  - 修复：改为复用淘汰赛的 `play()`（拉**整张专辑曲目**、可逐首切歌），播放条副标题也改成"第 N 首"并补序号 `k/N`。
- **③ 对位赛"本轮出战 0 张" + 没有盲盒虚位卡**
  - 根因：`BattleCreateView.vue` 的 `totalSelected` **漏了 aligned 分支**（直接 return 0）→ 提示条写"本轮共 0 张出战"，且 `hiddenCount=0` 使盲盒卡不渲染。
  - 修复：`totalSelected` 加 aligned = 歌手数 × 对位张数；预览取数也改用 alignCount。现在正确显示张数 + 出现**模糊堆叠 + 省略号 + "还有 N 张"**的盲盒虚位卡。
- **④ 收藏只做专辑**：`FavoritesView.vue` 去掉「类型」筛选与人格类型分支，只列专辑；空状态文案与入口同步。
- **⑤ 自选专辑上限 100 张 + 同室操戈声明**
  - 前端 `toggleCustom` 拦截 + 提示；后端 `battles.routes.js` 的 `albumIds` 加 `.max(100)`（两处）。
  - 手动挑选页新增声明：跨歌手混战优先异歌手配对，但同歌手仍可能某一轮内战（同室操戈），属正常赛制。
- **⑥ "加载其专辑"反馈**：`loadCustomAlbums` 成功后弹「**已添加 XX 的 N 张专辑**」+ 页面顶部反馈条 + 新卡片**入场动画**（`pkPop` keyframes）。
- **⑦ "我的对决"封面全裂**：根因 `serializeBattle`/`listMyBattles` **从不返回封面**，前端 `<img src="">` → 裂图。后端 `listMyBattles` 批量取每场第一张专辑封面（一次 BattleMatch + 一次 Album，无 N+1）并在 `serializeBattle` 透出 `coverUrl`；前端无封面时显示占位音符。
- **⑧ Edge 最小化闪屏 / 偶尔卡住（性能）**
  - 根因：`.aurora` 是**铺满视口的 fixed 层**，其中 3 个 680–760px 圆用 `filter: blur(100px)` —— Chromium 合成器会持续重绘（Edge 最小化/回桌面整窗闪、Chrome 偶发卡）。
  - 修复：blob 改为**径向渐变**（`radial-gradient(closest-side, var(--blob), transparent)`），**彻底去掉 100px blur**；ray 30px→16px；`html.ua-edge` 下再关掉所有 backdrop-filter 与大 blur。既治闪屏也省性能。
- **⑨ 颜色硬化**：`coverColor.js` —— 饱和度上限 48%→40%；低饱和判定 20→26；新增「彩色像素 < 灰像素 6 成 → 判为无彩色身份 → 回中性灰」，避免**浅色/暖白封面跑出粉/紫**。
- **文档**：`docs/进度与执行清单.html` 更新（本轮新修块、未做清单重排、完成度、日期 9/18）；`docs/index.html` 描述与页脚日期同步。
- **验证**：后端 4 个改动文件 `node --check` 全过；后端 3100 实例 `/health`+`/ready` ok；投票流程 E2E 通过；前端 `vite build` 通过（7.66s）。
- **备份/回退**：`git checkout -- backend/src frontend/src docs`
- **遗留（需用户实测/确认）**：Edge 闪屏需在 Edge 亲自验证（沙箱无 Edge）；颜色若某张仍怪，报专辑名即可精确定位。

## 2026-09-18 傍晚 · 用户第二批 8 图反馈：游客不得被限 + 试听误投票 + 切歌图标 + 盲盒回归 + 小组卡挤压 + Edge 二次深修 + 游客迁移(A-07)

- **改动名**：batch2-10shot-guestfree-preview-stop-edge2-migrate
- **① 游客不得被任何规则限制（用户："未注册用户还是只能玩40场次 这不行的"）**
  - 先澄清误读：上一条里的"同侧 40 场"是**模式判据**（连续 40 场都投同一侧），不是场次上限；且上一轮已让"异常票也推进赛程"，所以玩法本来就不受限。
  - 本轮再彻底一层：**风控只把可疑票排除出排行榜，不再 `applyViolation`（不累计违规、不限流、不封号）**。理由：单人对决里"投得多/连续同侧"属正常游玩，惩罚会直接砍掉主线。`restrictUntil` 经查**从未被任何中间件读取**（本就无效），但 `status='banned'` 会生效，故必须去掉这条路径。
  - 另加**游客醒目提醒条**（顶栏下方常驻）：不注册也能玩全部主线；游客数据只存本机浏览器（换设备/清缓存会丢）、不计入排行榜；注册可云同步/收藏/上榜 + 注册按钮。
- **② 游客数据迁移（A-07，用户："游客迁移必须有"）—— 已实现并 E2E 通过**
  - 后端：`POST /api/users/me/claim-guest`（`users.service.claimGuestData`）：校验发起者非游客、来源确为 `role==='guest'`；把 Battle / PersonalityResult / Favorite / Vote 的 `userId` 批量转给新账号；搬完删除游客号（避免云端堆无效账号 —— 对应"注册量大影响存储"的顾虑）。票的唯一索引冲突单条跳过不致命。
  - 前端：`stores/auth.js` 在本机存 `yinge_guest_id`；`login()`/`register()` 成功后自动调用迁移并提示「已把游客数据迁移到本账号（对决 N · 票 N）」。
  - **实测**：非法 id→"游客标识无效"；不存在 id→"来源不是游客账号"；游客自己发起→被拒；**真迁移 → `{migrated:true, moved:{battles:1}}`，且迁移后 admin 能读该对决**。
- **③ 点试听误触发投票**：整卡"点封面即投票"后，卡内「试听 30 秒」未阻断冒泡 → 点试听=投票。修：`@click.stop`。**规则：可点卡片内的次级按钮必须阻断冒泡。**
- **④ 切歌键图标错**：小组赛播放条"下一首"误用"上一首"的 path。已换正确 ▷|。
- **⑤ 多歌手只选 1 位没盲盒（守则 43 同类回归）**：`totalSelected` 又被写成 `picked.length < 2 → return 0`。已修；**规则：开局门槛与预览计数分开算**。
- **⑥ 小组赛卡片挤压（用户："多歌手多专辑混战时候的视觉问题必须优化 加入守则"）**：`.gstage` 用 `repeat(auto-fit, minmax(0,1fr))` 无最小列宽 → 8 组被挤成一条、封面变指甲盖、专辑名截成 2 字。改为 `auto-fill, minmax(212px,1fr)`（换行）+ 组内 `minmax(62px,1fr)`。**已写进《前端设计守则》第 70 条：卡片网格必须给可读最小列宽并允许换行，禁止用 `minmax(0,1fr)` 把 N 张卡挤成一排。**
- **⑦ Edge 最小化（第二次深修）**：上一版只把 blur 100→52px，没消除"铺满视口的 fixed 层"。现在 Edge 下 `.aurora` 整层 `display:none`，改把海洋蓝光晕画在 **body 背景**（普通背景、非合成层），并 `filter/backdrop-filter: none !important`；Edge 判定增强（UA `Edg/` + `userAgentData.brands`）。
- **⑧ 取色兜底改品牌蓝灰**：中性回退由 `hsl(215,6%)` 改为 `hsl(203,13%)`（与 #0EA5E9 同色域），保证海洋蓝背景调性统一；哈希兜底饱和 36%→30%。
- **验证**：后端 `node --check`（vote/users 4 文件）过；3100 实例迁移 E2E 过；前端 `vite build` 过（8.63s）。
- **提交**：`0f7f237`（前端批）+ `c04bb75`（迁移 + 风控不罚）。
- **回退**：`git checkout -- backend/src frontend/src docs`
- **遗留（下一批）**：创建页「我收藏组合」+ 歌手图片 + 悬浮二级彩蛋 + 后台对「快速搜歌手 / 常用组合」增删改（用户在实例图上给出方向，且明确"歌手图片放再下一批"）。

## 2026-09-18 晚 · 颜色取样 + 光晕/动效按业界重写（用户："查找所有全网资料解决"）+ Edge 再加强

- **改动名**：color-sampling-vibrant-and-glow-safe
- **触发**：用户截图（淘汰赛 樂之路 vs 黑色柳丁）—— 左块偏绿、右块偏土黄，观感很怪；"关于这一块都出了多少次问题了 请你查找所有全网资料解决这个颜色取样和动效 光晕的问题"。另：Edge 最小化仍存在。
- **① 查资料（真查了，有出处）**
  - **ColorThief**：MMCQ 中位切分 + **15bit（5bit/通道）直方图** + **过滤近白像素(各通道>250)** + 返回**像素数最多**的代表色（面积优先）。参考：ColorThief 源码解析 / deepwiki。
  - **Android Palette / Vibrant.js**：候选来自量化调色板，**权重 = 饱和度×3 + 亮度贴近0.5×6 + 占比×1**；`minVibrantSaturation=0.35`；缺 Vibrant 时用 DarkVibrant/Muted 派生。参考：node-vibrant README / vibrant.js 算法解析。
- **② 旧算法错在哪（有实测数据）**：我用 Playwright 拉真封面跑了 A/B。旧算法是「3bit(512桶) 量化 + `像素数×(0.15+饱和度)` 打分」—— **饱和度被重加权，且没有占比门槛**，于是封面上**一小块高饱和色就能劫持整块光晕**（《黑色柳丁》的橙瞳孔 /《樂之路》的青字），出来的色跟这张封面整体不像。
- **③ 已按业界重写 `coverColor.js`**：5bit 量化(32768 桶) → 过滤近白/近黑 → 取像素数前 64 当调色板(近似 MMCQ) → **Vibrant 权重 sat×3+luma×6+pop×1** → **占比门槛 3% + 饱和度门槛 30** → 同色相 ±20° 按像素数加权合并 → 压进友好区间(饱和 18–34%、亮度 48–58%)；不够格 → **Muted**（低饱和、面积优先）→ 再不行 → **品牌蓝灰**。顺带把 `ensureAlbumAccent` 加了 in-flight 去重（同张封面并发只取一次）。
  - **A/B 实测**（真封面）：黑色柳丁 旧=中性兜底 / 新=175° 青灰(占比 13.4%)；范特西 旧=15° / 新=23°（都落在棕橙系）；樂之路/依然范特西 = 中性兜底（这两张本就是低饱和封面）。**关键差别是"冠军桶占比"这个门槛把一小块亮色挡在了门外。**
- **④ 光晕"永远不跑调"（治本设计）**：新增 `blendWithBrand()` —— 舞台光晕的颜色**先往品牌海洋蓝混 50%** 再上屏，效果恒为「蓝底 + 这张专辑的色调」。从此**任何一张封面都不可能让整页变绿/土黄**（这正是反复被抱怨的那类问题）。光晕基础透明度 0.4→0.3，悬浮由 `opacity:1 + scale(1.3)`（扑满一屏）降为 `0.58 + scale(1.12)`。
- **⑤ 第二处"大 blur 固定层"**：`.vglow i` / `.g1/.g2/.g3` 原来是 **460px 圆 + `filter: blur(74px)` + `will-change: transform,opacity`**（永久提升合成层）→ 与极光同源，是 Edge 闪烁 / Chrome 卡顿的**第二个源头**。已改径向渐变、撤掉 `will-change`、去掉 blur。
- **⑥ Edge 再加强**：`html.ua-edge` 在"极光不渲染 + 全关 filter/毛玻璃"之外，再加 **`will-change: auto` + `animation: none`**，把"让合成器一直忙"的最后一类也停掉。
- **验证**：前端 `vite build` 通过（10.03s）；颜色 A/B 用 Playwright 拉 **真封面**实测（脚本留在 `tools/pw/color-test.mjs`，可复跑）。
- **提交**：`f7f5fb1`。
- **回退**：`git checkout -- frontend/src`
- **Edge 版本（自查，非问用户）**：本机 `C:\Program Files (x86)\Microsoft\Edge\Application\153.0.4234.32`（Chromium 153）→ UA 为 `Edg/153…`，`ua-edge` 降级**会命中**。我这边没有可运行的 Edge（沙箱里开不了浏览器窗口），最小化那一步仍需真机确认；若仍闪，下一批按第 3 个合成层源头继续排（候选：大半径 `box-shadow`、`position:sticky` 顶栏）。

## 2026-09-18 傍晚 · 修复"修 bug 把功能修没了"：专辑取色 + 渐变光晕恢复（用户投诉）

- **改动名**：restore-album-gradient-glow
- **触发**：用户截图（淘汰赛 + 小组赛）——"bug没了但是我们从Ui原型就定下来的取样专辑颜色加阳光特效也没了 之前那种渐变色很好看 你不能为了修复bug把功能整没了"。
- **⚠️ 我上一批的过度修正（两处，都撤了）**
  1. **光晕往品牌蓝混太多**：为了"防跑调"我把光晕色往品牌蓝混 30–50% → **所有封面的光晕都变成同一片蓝灰**，衬在浅蓝背景上几乎看不见 = 功能被抹掉。**已改为只混 12%**（占比门槛已经在取样层挡住了"小色块劫持"，不需要再用混色兜底）。
  2. **取样门槛定得太死**：① 过滤了近黑像素 → **暗色封面**（如《周杰倫的床邊故事》）大半像素被丢掉，各桶占比全不达标 → 退回中性灰；② 饱和度门槛 30 太高 → 大量正常封面也变中性灰；③ 没有鲜艳主色时直接给灰。**已改**：去掉近黑过滤（只滤近白/过曝，与 ColorThief 一致）、饱和度门槛 30→16、占比门槛 3%→4%（面积优先仍是主要防线）、**无鲜艳主色时改用"像素数最多的那一桶"**（ColorThief 的默认口径 = 面积最大的色调），灰就保持灰。
- **③ 找到一处"功能早就漏了"的地方**：`.vstage` 的签名渐变底用的是 **`--gls` / `--grs`**，而页面只设了 `--gl`/`--gr` → **渐变底一直是 `transparent`（死代码）**。现已补上（带透明度 0.42 的同色）。另外 **小组赛的 `.vstage` 从来没绑定过舞台样式**（只有淘汰赛绑了）→ 小组赛只有品牌蓝、没有专辑色。已补 `groupStageStyle`（本组首位专辑 → 末位专辑做左→右渐变）。
- **④ 验证（真页面实测，非推断）**：用 Playwright 打真实 dev 服务、走通到淘汰赛与小组赛，读**计算后**的变量与截图：
  - 淘汰赛《太陽之子》vs《依然范特西》：`--gl` rgb(147,124,98) 暖棕 / `--gr` rgb(147,102,98) 砖红 → 舞台 `linear-gradient(118deg, rgba(147,138,98,.42) …, rgba(145,96,106,.42) …)`；截图可见左暖右紫的光晕与阳光层。
  - 小组赛（4 张卡）：卡内 4 个专辑色各不相同（暖棕 / 绿 / 棕 / 蓝），`--gls` 橄榄绿 → `--grs` 蓝，渐变底跟着首末专辑走。
  - 脚本：`tools/pw/diag-glow.mjs`（读计算样式 + 出图）、`tools/pw/color-test.mjs`（真封面跑取色 A/B）。
- **提交**：`b2ae640`。**回退**：`git checkout -- frontend/src`
- **⚠️ 并行改动提示**：本轮发现后端进程被外部重启（PID 变化、uptime 归零）、且有未提交的并行改动落在同一批文件上。修改前已先读文件现状再改，未覆盖他人内容；两处修正方向一致。
- **遗留**：Edge 最小化仍需真机确认（我这边跑不了 Edge）。

## 2026-09-18 晚（二）· 舞台观感对齐原型 + 下一轮开工：歌手组合（用户自建 + 后台维护）

- **改动名**：stage-look-parity-and-combo-feature
- **触发**：用户 1 图 —— "总感觉效果不如 uidemo 里的 颜色和专辑不够呼应 效果也不自然"，并指出封面上一颗"莫名其妙的小白点"，以及"demo 的设想是鼠标放在专辑上有种点亮这一张的感觉 目前版本根本没体现出来"。
- **① 小白点 = 原型的星点（`.spark`）落在了封面上**：原型把 sp1/sp2 放在 `left:24%/36%`，实测正好压在左封面上（看起来像封面上的脏点）。已把 4 颗星点**统一挪到中间那条空白带**（VS 两侧的空隙 42–44%），只在悬浮对应一侧时亮起并放大。
- **② "效果不自然" = 光晕柔边成了硬边圆盘**：上一版把 `filter: blur(74px)` 换成 `radial-gradient(实心到 42% 再线性淡出)`，渲出来是一个**有硬边的圆盘**。已改成**多段径向渐变**（`color-mix` 做 66%/30%/10% 三级衰减，近似高斯钟形曲线）→ 柔边自然，且仍是零 blur 成本、无 `will-change`。
- **③ "点亮这一张"没体现**：除恢复原型口径（悬浮侧光晕 `opacity 1 + scale 1.3`、另一侧 `0.16`）外，新增**专辑色描边 + 提亮**（悬浮侧 `.art` 加 `0 0 0 2px var(--ac)` 环 + `saturate(1.08) brightness(1.05)`），另一侧封面压到 `0.82` —— 一眼就能看出"鼠标在哪张上"。
- **④ "颜色和专辑不够呼应"**：取色饱和度上限从 34% 提回 **44%**（光晕是半透明的，太低会显得跟专辑没关系）。
- **⑤ 验证（真页面实测）**：Playwright 打真实 dev 服务 → 悬浮左侧专辑后读**计算后**样式：`dataLit='l'`、悬浮侧 `box-shadow` 含 `rgb(176,69,69) 0px 0px 0px 2px`（专辑色描边）、`filter: saturate(1.08) brightness(1.05)`、另一侧 `opacity 0.82`；**`sparksOnCover = 无（都不在封面上）`**；小组赛 4 张卡 4 个专辑色 + 舞台渐变跟着首末专辑。截图：`tools/pw/shots/glow-hover.png`。
- **⑥ 下一轮开工：歌手组合（用户自建 + 后台维护）**
  - 后端新增 `models/Combo.js` + `modules/combos/`（service/controller/routes），挂载 `/api/combos`：
    - `isSystem=true`（ownerId=null）→ **系统组合**，管理员维护、所有人可见；
    - `ownerId=用户` → **用户自建组合**，只有本人看得到（"我收藏组合"）。
    - 权限：改/删只能本人；系统组合只有管理员；**查过 `restrictUntil` 那种"字段设了没人读"的坑**，这里权限是**查询时就拦住**（`assertCanManage`）。
  - **E2E 实测**：建我的组合 ✓（`mine=true`）/ 建系统组合 ✓ / 列表系统在前 ✓ / 改名 ✓ / **游客建系统组合被拒 2002** ✓ / 游客建自己的组合 ✓ / **游客删系统组合被拒 2002** ✓ / 清理 ✓。
  - 前端：`comboApi` 接入；创建页把写死的 PRESETS 换成**数据驱动**（系统组合 + 我的组合），新增「**＋ 收藏当前组合**」（选满 2 位歌手才可点，弹框起名，存进自己账号）与我的组合上的**删除 ×**；后端还没有系统组合时**退回内置 4 个兜底组合**，界面不会空。
  - **创建页实测**：4 个兜底组合可点、`＋ 收藏当前组合` 在未选歌手时**正确置灰**。
- **验证**：后端 6 文件 `node --check` 全过；组合 API E2E 通过；前端 `vite build` 通过（7.40s）。
- **提交**：`1655b77`（舞台观感）+ `ff7e75c`（组合）。
- **回退**：`git checkout -- backend/src frontend/src`
- **下一轮剩余（明确列出）**：后台「组合管理」页面（增删改系统组合 + 快速搜歌手名单可维护）、个人中心列出"我的组合"、歌手图片 + 悬浮二级彩蛋（按用户口径放再下一批）、搜索过滤更严 + 已选歌手单独一排（P1-7）、指定对决交互（P1-8 优先）。

## 2026-09-18 晚（三）· 取色算法改为「色相家族面积」+ 下一轮功能：后台歌手组合管理

- **改动名**：color-by-hue-family-and-admin-combos
- **触发**：用户两张图 —— "蓝色专辑取不到蓝色 七里香这张又是粉的 午夜又是绿色的 **你这个逻辑到底怎么写的 完全一塌糊涂 必须改进**"。
- **① 先测准（不猜）**：写了两个诊断脚本，用**真实模块 + 真实封面**跑出数据。
  - `tools/pw/diag-color.mjs`：同 URL / 带 cache-buster 各取一次 → **都成功、无跨域污染**，说明取样本身没坏（排除了"CORS 导致退回哈希色"这个假设）。
  - `tools/pw/diag-color3.mjs`：把封面按**色相家族（±30°）**聚合，看各家族占全图多少：
    - 《陶喆同名專輯》：h=216° 占 **98.6%** → 模块其实**给的就是蓝色** rgb(63,106,171)（用户感知"取不到蓝"，实际是光晕被混色/透明度冲淡，不是取色错）。
    - 《七里香》：h=28° 29.3% + h=52° 28.9% + h=104° 21.2%（**整片是暖橄榄/绿系**），而模块给的是 **rgb(176,69,69) 红** —— 因为**旧逻辑按"饱和度加权"选，红字 logo 只占 8%，却因为够艳把 29% 的草绿压下去了**。
    - 《reputation》：全图 s=0% → 中性灰（正确）。
- **② 算法改成「按色相家族面积选主色」**（`coverColor.js`）：5bit 量化 → 过滤近白/过曝 → 取前 64 桶 → **按 ±30° 聚成色相家族、以家族总面积为唯一判据**选冠军 → 家族内按像素数加权求平均色 → 压进友好区间（饱和 ×1.25、下限 22% 上限 46%，亮度 46–64%）。黑白/近灰封面（家族饱和 <6%）→ 中性蓝灰。
  - **改后逐张复测**：陶喆同名專輯 = 蓝 ✓｜Midnights = 暖米白（原为暖灰）✓｜七里香 = 橄榄棕（原为红）✓｜范特西 = 红 ✓｜依然范特西 = 暖棕 ✓｜reputation = 中性灰 ✓。
- **③ 顺带修掉"凭空怪色"**：以前取色失败会退回**按 albumId 算的随机色相**（绿 / 粉都能冒出来）。按用户口径改为**一律回中性蓝灰**（`accentStyleOf` / `ensureAlbumAccent` 都不再用 `hashColor`）；并给封面加载加**失败重试一次**（带 cache-buster），减少偶发取不到。
- **④ 下一轮功能：后台「歌手组合」管理页**（用户要求"这里的歌手和组合我需要管理员可以在后台改变增删"）
  - 新增 `views/admin/AdminCombosView.vue`（用 `AdminShell` + 设计系统表格样式），功能：**搜歌手新建系统组合**（2–6 位，可点 × 移除，设每位张数）、**改名**、**删除**；用户自建组合只读展示（不越权改别人的私人组合）。路由 `/admin/combos` + 后台导航加「歌手组合」。
  - **实测（真 UI + 真接口）**：后台页标题渲染 ✓、搜歌手返回 4 个候选 ✓、**用 1 位歌手保存会被正确拦下**（应用要求 ≥2，验证了校验真的生效）✓；再用接口建一个 `isSystem` 组合后，**前台创建页的兜底组合被真实系统组合替换** ✓、**点一下即装填出两位歌手**（"装填后已选歌手: 周杰倫 / 林俊傑"）✓、测完已清理 ✓。
- **验证**：前端 `vite build` 通过（9.47s，一次瞬时挂起后重跑）；诊断脚本留在 `tools/pw/`（`diag-color*.mjs`）可复跑。
- **提交**：`3d8bfbc`（取色）+ `2d3bb1e`（后台组合页）。
- **回退**：`git checkout -- frontend/src`
- **剩余**：个人中心列出"我的组合"、搜索过滤更严 + 已选歌手单独一排、指定对决交互、歌手图片 + 悬浮二级彩蛋。

## 2026-09-18 晚（四）· Edge 闪屏按 Chromium 已知缺陷查证后整改 + 下一轮两项功能

- **改动名**：edge-flicker-chromium-probe-and-round2
- **① Edge 闪屏：按用户要求查了 GitHub / Chromium Issue / 网络资料**
  - **查到根因（有出处）**：Chromium 的**已知渲染缺陷** —— 有开发者提交过 bug（**crbug.com/483220231**）并做过对照实验：注释掉全部 `backdrop-filter` 后，闪烁从"整个组件消失"降级为"偶发一小块"，说明 **`backdrop-filter` 不是根因，但会极大放大问题**；根因在**合成器（compositor）层管理**：内容/尺寸变化时合成器重算 backdrop 采样区域，会**整层丢弃** → 闪几帧空白。另有 **crbug.com/1202369** 专门是 `backdrop-filter` / **`mix-blend-mode` 叠在带裁剪的合成层上**导致闪烁（文中还点名了 `will-change` 的同类问题）。**Chrome 与 Edge 同引擎都会中招，Firefox / Safari 不复现**（与用户"Edge 闪、Chrome 偶发"的观察一致）。
  - **Windows 侧还有一条**：微软 **KB5039212** 修的就是"Chromium 系浏览器在部分 Win11 机器上画面撕裂/闪块/留影"的 Windows 图形栈问题。**已把这条建议写进交付说明**（建议装该更新 + 检查显卡驱动 + 必要时关硬件加速）。
  - **本轮整改**：Edge（`html.ua-edge`）下 **关闭 `mix-blend-mode`**（这是刚才查到的经典触发项），并**把伪元素也覆盖到** —— `*` 通配**选不到 `::before/::after`**，实测 `.sunlit::before` 的 `mix-blend-mode: screen` 一直没被关掉（这是上一版残留的缺口）；同时给 `.sunlit::before` / `.gbeam` 撤掉 blur 并压低透明度，保住"有光"的观感。
  - **实测（Edge UA vs Chrome，同一场对局页）**：Edge → `html.ua-edge`、极光 `display:none`、顶栏 `backdrop-filter: none`、`.gbeam` `mix-blend-mode: normal`、**`.sunlit::before` `normal` / `filter: none`**、`.vstage` 无毛玻璃、封面正常加载 ✓；Chrome → 极光 `block`、顶栏 `blur(22px)`、`.gbeam` `screen + blur(18px)`、`.vstage blur(28px)` **全部保留** ✓ —— **视觉设计没有被破坏**（满足"不能破坏现有设计"）。
  - ⚠️ **诚实边界**：沙箱里**开不了 Edge 窗口**，所以"点最小化是否还闪"这一步只能你真机确认；我能确认的是"所有已知触发项在 Edge 下都已关闭"。
- **② 下一轮功能（两项）**
  - **创建页 · 已选歌手单独一排 + 搜索过滤**：已选歌手挪进带虚线上分隔与「已选 N 位」标签的独立一排；搜索结果**过滤掉已选过的**并截到 6 个。**故意不做"名字必须互相包含"的硬过滤** —— 实测搜简体「周杰伦」返回的是繁体「周杰倫」，硬过滤会把正确答案误杀。
  - **个人中心 · 「我的组合」**：新增第 4 个标签页，列出**我自建**的歌手组合（组合名 + 成员 + 每位张数），带「去用」（跳创建页）与「删除」；空状态给出"在创建页点＋收藏当前组合"的引导。
- **③ 新增 `scripts/restart-dev.mjs`**（用户要求"每次改完自己重启动前后端验证"）：杀 3000/5173 旧进程 → 用写死的托管 Node 22 拉起后端(`--watch`)与前端(vite)。
  - ⚠️ **重要发现**：**沙箱会在这条命令结束时杀掉它派生的所有子进程**（即使 `detached + unref`）—— 所以**我无法把服务留在后台**。于是改为「**在同一条命令内启动服务 → 立刻验证 → 再结束**」，并在交付时明确告知用户服务当前是关的、双击 `start-dev.bat` 即可恢复。
- **验证（单命令内完成：build → 起库/后端/前端 → Playwright 实测）**：`vite build` 通过（10.09s）；创建页「已选 1 位 / 周杰倫」✓、二次搜索候选已不含已选歌手 ✓；个人中心我的组合列出「周杰倫 × 林俊傑 · 每位 8 张」✓；Edge 检查三项全绿 ✓。
- **提交**：`e35a245`。**回退**：`git checkout -- frontend/src scripts`
- **剩余**：指定对决交互、歌手图片 + 悬浮二级彩蛋。

## 2026-09-18 晚（五）· 收藏重复报错 + 撤销上一步 + 结果页折叠重排与宽度约束（+ 守则第 73 条）

- **改动名**：favorite-idempotent-undo-resultfold-width
- **① 收藏"记录已存在"红字（用户：刷新后又出现，图标也不亮）——两处根因都修了**
  - **后端**：`addFavorite` 直接 `create`，重复收藏撞唯一索引 → 冒到全局错误处理变成「记录已存在，请勿重复操作」。
    收藏按钮本质是**切换**，重复请求不该报错 → 改成**幂等**：已存在就直接当成功返回（`already: true`）。
  - **前端**：收藏状态以前只靠**每个收藏按钮自己 onMounted 拉**，页面刚进来时一律显示"未收藏"，
    这时点下去就是一次重复提交、且图标不变 → 改为在 `AppShell` 里 **登录状态就绪就拉一次**（`watch(isLoggedIn, {immediate:true})`）；
    并在 store 的 `toggle` 里把「服务端已有 / 服务端没有」这两种**本地状态过期**的情况当成成功处理（不再弹红字）。
  - **实测**：同一张专辑连收两次 → `code 0 / 0`、第二次消息是「**收藏成功**」（不再是"记录已存在"）。
- **② 撤销上一步（用户："增加一个返回上一个选择，给用户撤销的机会 写着选错了没关系可以回"）**
  - 后端新增 `POST /api/battles/:id/undo`（`undoLastStep`）。**难点是让赛程自洽**：一次投票可能已经"本轮打完 → 生成下一轮"。
  - 规则（写在代码注释里）：`matchOrder` 是**全局递增序号** → 「这一票之后才被创建出来的场次」= `matchOrder` 更大的场次，**全部删掉**；
    同时删掉本人在这些场次/该分组上的票；把这一场**重新计票 + 重判胜方**；最后把 battle 拉回 `playing`（冠军清空），让 `/next-step` 重新出题。
  - 前端：对局页顶部加「↩ 撤销上一步」+ 文案"选错了没关系，可以撤销上一步重选"（小组赛与淘汰赛两处都加了）。
  - **实测**：投前 `81496e` → 投票后推进到 `81496f` → **撤销后回到 `81496e`**，消息「已撤销『淘汰赛 · semi』」✓
- **③ 结果页：宽度约束 + 重排 + 折叠（用户："这种拉这么长影响整体可读性和美观的不能再出现"）**
  - 用原生 `<details>/<summary>` 做成**可折叠二级菜单**：`夺冠之路`（默认展开，含完整晋级图）+ `小组赛与复活`（默认折叠）。
  - **顺序按用户要求重排**：夺冠之路 → 完整晋级图 → 小组赛与复活。
  - 折叠块加 `max-width: 1120px + margin:auto` 的两边距离约束；**小组卡的视觉一个字没改**（用户说"小组赛的视觉很不错 不用改"）。
  - **实测**（打完一场 8 张的对决后打开 `/battle/:id/result`）：`foldCount = 2`、标题顺序 `["夺冠之路","完整晋级图","小组赛与复活"]`、
    冠军/亚军/季军内容完好；容器宽 1180、**无任何元素超出容器** ✓
- **④ 切歌提示（用户："默认 100% 缩放时用户可能注意不了下面可以切歌"）**：撤销条旁边写明"想多听几首就点下方播放条切歌，或点「试听清单」里的任意一首"。
- **⑤ 《前端设计守则》新增第 73 条（硬规则）**：任何内容区必须有**左右边距约束**；禁止内容拉成贴边窄柱、禁止宽屏无限拉长；
  长列表/长表格一律给 `max-width` 并居中；**验收口径：窗口拉到 1920 宽截图看有没有"贴边"或"窄柱"**。
- **验证**：后端 4 文件 `node --check` 全过；前端 `vite build` 通过（9.84s）；①②③ 均有真后端/真页面实测输出。
- **提交**：`2ec2950`。**回退**：`git checkout -- backend/src frontend/src docs/design`
- **⚠️ 本轮未做完（用户在本次消息里点名、但一轮做不完，明确列出以免漏）**：
  1. **分享页布局修复**；2. **好友一起玩**（新功能，需要"非发起人也能投票"的数据/接口改动，要做完整方案）；
  3. **指定对决交互修复**（默认全勾、取消反而进对阵表）；4. **按流派自动抓取 Apple Music 流派榜前 ~30 位歌手**（补曲库量）；
  5. **后台数据看板加"用户喜爱的 PK 组合"面板**。

---

## 2026-09-18（第五批）· 上批 5 项未完成全部做完

> 承接上批「本轮未做完」清单，**5 项全部落地**。提交 `2ec213e`（好友同玩）+ `ea20f74`（其余四项）。

- **① 分享页布局（上批遗留）** —— **根因：`.sharewrap` 这个类名压根没定义过**。所以两个区块退化成竖排、分享卡被拉到满屏宽。
  - 改：补两栏栅格 `minmax(0,420px) minmax(0,1fr)`、`gap:28px`、`max-width:1120px`、`margin:20px auto 0`；`<900px` 退单栏且卡片居中。
  - **实测**（真页面 + 计算样式）：容器 `x=160`、宽 `1120`、`gridTemplateColumns = "420px 672px"`、卡片宽 `420` → **不再拉满** ✓
- **② 好友一起玩**（新模块）
  - 数据：`Battle` 加 `shareCode` + `originBattleId`，索引 `(shareCode, createdAt)`。
  - 接口：`POST /battles/:id/invite`（生成/取回邀请码，只能本人）、`GET /battles/join/:code`（看签表）、
    `POST /battles/join/:code`（接龙开局）、`GET /battles/join/:code/compare`（对比）。路由必须排在 `/:id` 之前。
  - **签表为何必然一致**：接龙局**不重新抽专辑**，直接用发起方的 `albumIds` 原样复制（v2 走 `createBattleV2`、v1 直接复制 Battle + 场次/分组文档）。
    分组只依赖专辑顺序，**只有"抽张"才随机** —— 所以分组与对阵必然相同。
  - 幂等：同一人重复点「开始」返回已有的那局（不刷出一堆局）。
  - 前端：新增 `/battle/join/:code` 页（盲盒式只露 1–2 张封面），冠军页加「和好友一起玩」按钮。
  - **实测**：后端 18 项断言全过（含**同一小组同一批专辑**、非本人生成被拒 `2002`、无效码正确拒绝）+ 真 UI 10 项全过。
- **③ 指定对决交互（守则 37）** —— **根因：对勾徽章 `.pk .ck` 默认就是显示的**，而"已选中"走的是 `.off`（藏勾 + 变灰）→ 语义整个反了。
  - 改：`.pk .ck` 默认 `opacity:0`，**只有选中（`:not(.off)`）才显示**；指定对决另立 `duelpk` + `pick/done` 三态（避免走 `off` 又把勾全亮起来）。
  - 新增「待成组 1/2」托盘 + **再点一次可撤销**；候选池**累积**（原来每位歌手都会替换整池 → 只能"自己打自己"，即守则 38③）；已载入歌手独立成排可 ×；成组时标「同室操戈」。
  - **实测**：默认可见对勾 **0 个** ✓ → 点 1 张出现托盘且**只有 1 张显示对勾** ✓ → 再点可取消 ✓ → 凑 2 张成组、两张禁用变灰 ✓ →
    换第二位歌手后候选池 `9 → 26` 张（**累积而非替换**）✓
  - 顺带修：`.searchrow` 缺 `align-items:center`，搜索按钮被 flex 拉成 48px 高的"蓝色药丸"。
- **④ 流派歌手扩充（用户："一个流派有那么多人艺人 我们这里才几个"）**
  - ⚠️ **实测得到的技术事实**：iTunes Search API **没有**"按流派筛选"的参数（`attribute=genreTerm` 与普通关键词搜索行为一致，不是筛选器）；
    真正的流派信息在每条结果的 `primaryGenreName` 上 → 策略改为「**用流派检索词搜 → 再用 `primaryGenreName` 反筛**」。
    （不加反筛时 `term=流行` 会混进"中山女高流行音樂傳播社"这类杂项。）
  - 新增 `backend/src/modules/music/genreExpand.js`：流派→检索词+认可标签表（覆盖简繁两套写法、具体项排在宽泛项之前）；
    `GET /music/genres/discover`（只读）+ `POST /music/genres/warm`（每批最多 8 位，并发 2 防 iTunes 限流）。
  - **降级兜底**：中文区没有对应流派标签时（实测 `Reggaeton` → "拉丁都市音乐"），退化为按相关度收录并打 `loose: true`，界面如实说明。
  - 前端：流派模式加「流派歌手不够？从 Apple Music 补足」面板：先预览 30 位 → 分批入库 → 已入库的逐个变绿 → 完成后刷新歌手数。
  - **实测**：发现 30 位（100% 命中「國語流行樂」）→ 入库 4 位全成功 → 该流派歌手数 **7 → 13** ✓；超 8 位被字段校验拒绝 ✓；表外流派有兜底 ✓
- **⑤ 后台「用户喜爱的 PK 组合」**
  - **口径（我定的）**：不看用户"保存了什么"，只看**真实开过多少局** —— 保存是意愿，开打才是喜爱。
    组合键 = 歌手 id 排序后拼接 → 「周杰倫+林俊傑」与「林俊傑+周杰倫」**合并统计**。
    ⚠️ 初版把单歌手模式也算了进去（榜首变成"周杰倫 36 局"），**它不是 PK 组合** → 已收紧为 `scopeType ∈ {multi-artist, aligned}` 且 ≥2 位歌手。
  - 新增 `GET /api/combos/popular`（管理员）；后台页顶部成表：局数 / 人数 / 完赛比例 / 最近一次 / **一键采纳**为系统组合（已是系统组合的标出来，避免重复）。
  - **实测**：接口榜首「林俊傑 × 周杰倫」局数 18（多轮测试数据合并后）→ 真页面 20 行渲染正常，「已是系统组合」标记正确 ✓
- **文档同步**：`进度与执行清单.html` 三行改「已做」+ 新增两行；`音格-前端设计守则.html` 冠军页结构 / 好友同玩 / 第 22 / 37 / 38 条全部更新。
- **验证**：4 个临时 E2E 脚本（好友同玩 18+10 项、指定对决 16 项、流派 12 项、分享页+后台 13 项）全部通过；`vite build` 通过。**测完已删除临时脚本**。
- **回退**：`git revert ea20f74`（或 `git checkout ea20f74^ -- backend/src frontend/src docs`）
- **踩坑记录（重要）**：自动化写"打完一局"的脚本会被**防刷票限流**拦住（`1003 操作过于频繁`，两次投票间隔须 ≥300ms）。
  这是**正确行为**，不要为了跑测试去关它 —— 脚本按真人节奏（≥480ms）投票即可。

---

## 第六批（2026-09-18 夜 · 12 张截图反馈）· 「本轮已投完」卡死 + 专辑准入 + 流派大咖 + 晋级图重做 + 玻璃框架

改动范围：`backend/src/modules/{battles,music,combos}` + `backend/scripts` + `frontend/src/{styles,components,views,utils}` + `docs/`
新增脚本：`backend/scripts/repair-brackets.mjs`（签表体检修复）、`verify-undo-fix.mjs`（撤销回归）、`verify-genre.mjs`（流派扩库）
新增组件：`frontend/src/components/BracketNode.vue`（自递归树节点）、`BracketTree.vue`（完整晋级图）、`frontend/src/utils/bracketTree.js`

### ① ⭐ 「本轮已投完」永久卡死（用户截图 /battle/6aad508a…）
- **真根因（查库确认，不是猜）**：该局 semi 轮只有 **1 场**、决赛是 **isBye=true 的轮空场**。
  链路：`undoLastStep` 按 `matchOrder > 本场` 删"更晚的场次" → 但 `matchOrder` **在同一轮内也递增** →
  撤销一场半决赛时把**另一场半决赛一起删了** → 剩下残缺轮次 → 推进逻辑看"最后一轮全判完了" →
  用 1 个胜者生成**轮空决赛**（没有可投票的对阵）→ 推进再也不会被触发 → 前端永远停在「本轮已投完」。
- **修法三件套**：
  1. `stageRankOf(match)`（`battle.service.js` 导出）：小组/对位/指定=0、复活=1、淘汰赛按轮次 2+n。
     `undoLastStep` 改为只删**严格更晚阶段**的场次；顺带回写 `currentRound`。
  2. `getNextStep` 加**兜底自愈**：读完状态顺手 `progressBattle` 一次并递归（深度上限 5）——
     专治"最后一轮全是轮空场、没有任何投票机会"这类没有触发点的局面。
  3. `repair-brackets.mjs`：逐轮核对"场次数 vs 上一轮胜者数"，找到第一处不对的轮次 →
     用**确定性配对规则**重建（能对上号的老场次复用、票数与胜方不丢），并作废其后所有轮次。
     幂等、默认 dry-run（`--apply` 才写库）。
- **实测**：全库体检 83 局 → 发现 **3 局**同类损坏，全部修复；复检 **0 异常**。
  用户那一局：半决赛补回第 2 场、假的轮空决赛删除、`matchOrder` 重排 1..14。
- **回归**：`verify-undo-fix.mjs` **36/36** 断言通过（含"两场半决赛就是原来那两场""决赛是真打不是轮空""自愈不留卡死态""撤销小组票会连带清掉淘汰赛"）。
- **教训（已写入守则 79）**：凡"按序号删除/回退"的逻辑，先自问"序号在同一个批次内是否也递增"。

### ② 专辑准入新增 5 类过滤（用户 图7/图8）
- 新增 `MAX_TRACK_COUNT = 30`（原来只有下限 7）；新增关键词组 `remix`、`single`，`soundtrack` 大幅扩充，新增 `reissue` 硬剔组。
- 新增 `looksLikeArtistList(artistName)`（从**歌手名**判断拼盘：≥2 个逗号顿号 / 1 逗号 + `&` / feat. / Various Artists / 群星）。
  为什么必须看歌手名：图 8 的「Lady Gaga, 遥恩, 巴夫, Harold Faltermeyer & Hans Zimmer」专辑名看不出是拼盘。
  保守设计：`Tyler, The Creator`、`Simon & Garfunkel` 这类**不误杀**。
- **刻意不硬剔 `deluxe/expanded/remaster`**：实测 Rihanna 的 `ANTI` 港区**只有 Deluxe**，硬剔会把整张专辑弄丢；
  这类交给规则 7「同名去重」，并**扩展归一化词表**（reloaded / complete confection / anniversary / 加值 / 加曲 / 超值版…）。
- **实测（全库重算 `reevaluate-admission.mjs`）**：扫描 1120 张合格 → **新剔 91 张**
  （体量 46 / 混音 30 / 单曲EP 5 / 拼盘 6 / 原声 3 / 再版 1）。用户点名的**每一张**都逐一核实：
  The Remix🚫、Judas (Remixes)🚫、Teenage Dream: The Complete Confection🚫、Good Girl Gone Bad: Reloaded🚫、
  Black Panther: Wakanda Forever🚫、Top Gun: Maverick (Music from the Motion Picture)🚫、Disease - Single🚫；
  保留：Teenage Dream✅、One of the Boys✅、ANTI✅（Deluxe 是唯一版本）、Rihanna 仍有 7 张合格。

### ③ ⭐ 流派扩库换数据源（用户 图3/图4/图5）—— 「大咖进不来」的真因
- **技术事实（实测）**：iTunes Search **没有流派筛选参数**；`term=Rock` 在 hk 区前 40 位是
  BTS / Ed Sheeran / BLACKPINK / Taylor Swift…… 与摇滚无关。只能靠 `primaryGenreName` 反筛，人数少且混噪声。
- **正解**：老版**榜单接口** `/{country}/rss/topalbums/limit=N/genre=<id>/json` —— **支持流派**，
  且 **hk / us 各区一份**；条目里 `im:artist.attributes.href` 形如 `…/artist/journey/486597?uo=2`，
  **歌手 ID 直接就在里面**（30 位歌手省下 30 次搜索请求）。
- 探针把流派 ID 1~40 全拉一遍对出映射表（`GENRE_RSS`）：5=古典 6=乡村 7=电子 8=节庆 10=民谣 11=爵士
  12=拉丁 14=流行 15=R&B 16=原声 17=舞曲 20=另类 21=摇滚 24=雷鬼 27=日本流行。
- 两区**轮流穿插**取人 → 华语与欧美同时在场。新增噪声过滤 `looksLikeCuratedArtist`：
  iTunes 自制歌单/电台伪歌手（「流行摇滚」「摇滚老太」「我的摇滚青春」「Today's Hits」）+ 多人拼盘署名；
  词曲作者/制作人（Greg Kurstin / Ari Levine / Mike Elizondo）靠"榜单优先"自然挤掉。
- **华语系流派故意不进榜单表**：hk 区 14 号榜混着 aespa / IVE 这类 K-pop，靠关键词源 + 标签反筛更准。
- **实测**：摇滚 30 位含 Journey/Pink Floyd/TOOL/Fleetwood Mac/Boston/Eagles；
  Hip-Hop 30 位同时有 Drake/Eminem/JAŸ-Z/Kanye West/The Game **和** LMF/MastaMic/廿四味/G-DRAGON。
  `verify-genre.mjs` **17/17** 通过。

### ④ 流派补库支持逐个点选（用户 图1）
- 原来只有一个"把前 N 位补进曲库"的全量按钮；现在歌手是**可点选的 chip**（点击切换选中、`✓` 前缀、描边高亮），
  另有「全选未入库（N）」与「入库已选 N 位」；已入库的显示为「已入库 X 张」并置为不可选。
- 顺带：发现提示里标出**数据来源**（榜单 / 榜单+关键词 / 关键词）。

### ⑤ ⭐ 玻璃框架：默认就要有阴影（用户 图6）—— 挖出真根因
- **`--gsh` 被写成两个颜色**（`rgba(255,255,255,.95), rgba(8,58,92,.14)`），而它**全部**用在
  `box-shadow: 0 18px 46px var(--gsh)` 里 → 替换后逗号后那段是"没有偏移量的影子" →
  **整条 box-shadow 非法、被浏览器直接丢弃** → 所有玻璃卡/胶囊默认**一个阴影都没有**。
- 修：拆成 `--gsh`（阴影色）+ `--gsh-hi`（顶部内高光），并给 `.g-card/.chip/.preset/.seg` 统一补默认阴影。
- **新增守则 74**：任何要拼进 `box-shadow`/`filter` 的 CSS 变量，自身必须是**合法单条值**。

### ⑥ 组合区：我收藏的组合换色 + 前置（用户 图6）
- `listCombos` 排序由 `isSystem: -1`（系统在前）改为 `isSystem: 1`（**我收藏的在前**）。
- 我收藏的组合：**金色框 + 金色「我的」小标**，与内置的蓝白框区分（`--gold` 主题自适应）。

### ⑦ 冠军页：收藏按钮统一放大 + 水印/冠军字形（用户 图2/图12）
- 星标：`.favbtn.icon` / `.sm` 两条 padding 互相覆盖 → 统一为 **26×26 方形**并放大一档；实测三张完全一致。
- 结果页冠军块：金色描边 + 金色渐入；冠军名 **40px/900 金色带光晕**；「CHAMPION·冠军」改金色渐变药丸。
- 分享图：水印 11→**13.5px/800**、冠军名 20→**26px 亮金**、★冠军★ 加大加亮。
- 方形图兜底：封面原按"卡宽 58%"定尺寸，换成 1:1 后高度超出可用区 → 被 `overflow:hidden` 硬裁掉半张；
  改为**按高度定尺寸**（`height:38%` + `aspect-ratio` 自动出宽），实测 113×113 完整落在卡内。

### ⑧ ⭐ 晋级图重做为树状（用户 图10 + 图12）
- 原实现是"每轮一列 × 列内拆 `左|比分|右` 三栏"，列一多每侧只剩几十 px，专辑名全被省略成「…」。
- 新增 `utils/bracketTree.js`（由扁平场次还原树，`winnerExtIdOf` 统一用**外部 albumId**）
  + `components/BracketNode.vue`（**自递归**，左浅右深，两段 CSS 画支架线：孩子横线 + 竖直母线 `top:25%/height:50%` —— 因为每个孩子都是 `flex:1`，两条横线正好落在 25%/75%，母线严丝合缝）
  + `components/BracketTree.vue`（树 + 金色冠军格 + 横向滚动）。
- **结果页「夺冠之路」内嵌同一组件**（二级折叠 `details.fold.sub`）；原"按轮次列全部对局"改名「全部淘汰赛对局」避免重名。
- 对阵卡固定 **206px**、专辑名**最多两行**；**40 张 / 31 场**大签表实测 63 节点、容器内横滚不撑破页面。

### 验证（本轮全部实测，非推断）
| 脚本 | 结果 |
| --- | --- |
| `backend/scripts/verify-undo-fix.mjs`（新） | **36/36** 断言 ✅ |
| `backend/scripts/verify-genre.mjs`（新） | **17/17** 断言 ✅ |
| `backend/scripts/repair-brackets.mjs`（新，dry） | 83 局体检 → **0 异常** ✅ |
| `backend/scripts/verify-batch1.mjs`（回归） | 12/12 ✅ |
| `backend/scripts/verify-v2.mjs`（回归） | 59/59 ✅ |
| `tools/pw/verify-r5.mjs`（新，真浏览器 + 真造两局打完） | **25/25** 断言 ✅ |
| `frontend` `vite build` | 9.99s 通过 ✅ |

`tools/pw/verify-r5.mjs` 覆盖：领奖台三星标尺寸一致(26/26/26) / `.preset`+`.seg` 默认有阴影 / `--gsh` 已合法 /
我的组合金色且在前 / 18 张树 31 节点·1298px 宽·46 个名字仅 1 个截断 / 40 张树 63 节点不撑破 /
结果页内嵌晋级图 31 节点·冠军 40px 金色渐变 / 分享图水印 13.5px·冠军 26px / 方形图封面 113×113 未裁。
截图存 `tools/pw/shots/r5-*.png`。

### 回退
- 整批：`git revert <本批 commit>`
- 只回退撤销/准入/流派三处逻辑：
  `git checkout <本批 commit>^ -- backend/src/modules/battles/battle.service.js backend/src/modules/music/admission.js backend/src/modules/music/genreExpand.js`
- 只回退晋级图与玻璃框架：
  `git checkout <本批 commit>^ -- frontend/src/components/BracketNode.vue frontend/src/components/BracketTree.vue frontend/src/utils/bracketTree.js frontend/src/styles/tokens.css frontend/src/styles/components.css`
- 准入规则回滚后需重跑：`cd backend && node scripts/reevaluate-admission.mjs`（会把 91 张重新置回合格）；
  签表如再被打坏，跑 `node scripts/repair-brackets.mjs --apply` 修回。

### 明确留到「最终 UI 大改」的三件事（用户点名）
1. **分享图方形版重做**（信息被硬裁、整体观感差）—— 本轮只解掉"封面撑破被裁"。
2. **全站文字类统一玻璃框架** —— 本轮已给 chip/preset/seg/g-card 补默认阴影，属方向性第一步。
3. **类 iOS 液态玻璃视效**整体升级。

（已写入守则第 80 条与《进度与执行清单》第 23/24 行。）

---

## 第七批（2026-09-19 凌晨 · 4 张截图反馈）· 结果页折叠定稿 + 排行榜夺冠次数 + 创建页模式大厅

改动范围：`backend/src/modules/rankings` + `frontend/src/{components,views,api}` + `docs/`

### ① 结果页折叠顺序定稿 + 晋级图"太大太长"（用户 图1/图2）
- **顺序定稿（用户原话）**：`小组赛与复活 → 夺冠之路 → 淘汰赛·全部对局 → 完整晋级图`，除夺冠之路默认展开外**其余默认收起**。
  用户一屏先看到冠军、名次与数据，细节全收进折叠（"小组赛与复活这种折叠思路就很好啊"）。
  实现：完整晋级图从「夺冠之路」内部抽出、提到最后，成为独立折叠块；原「全部淘汰赛对局」也包进折叠并改名「淘汰赛 · 全部对局」。
- **真问题（这才是"太大太长"的主因）**：晋级树的**种子格 `<img>` 没有任何尺寸约束** ——
  iTunes 封面是 600×600 原图，直接被塞进卡里；一列 16 个种子把整棵树撑到 **2292px** 高。
  修：种子格改**横向小行**（22px 小封面 + 名字）+ 紧凑版（`compact` 真正接上：封面 16px / 卡宽 148px / 字号 12.5px /
  连线与留白收窄，冠军格同步缩小）。实测整树 **2292px → 568px**，46 个专辑名仅 1 个截断，横向可拖动。
- ⚠️ **新硬规则（守则 81）**：给 `<img>` 必须同时给宽高约束，绝不能假设图片"刚好那么大"——外部封面动辄 600×600。

### ② 排行榜新增「专辑夺冠次数」榜（用户 图3）
- 后端 `rankings.service.js` 新增 `championsRank` / `championsCount`，`GET /api/rank/champions`：
  统计 `status=finished` 且冠军已决出的对局，按 `championAlbumId` 聚合"当过多少次冠军"；
  并列按**最近一次夺冠时间**排（越新越前），再按 `_id` 兜底，结果稳定可复现。
- 前端排行榜加「榜单」切换：**最受欢迎（按有效票数）／夺冠次数**；标题、说明、领奖台与列表口径随切换
  （"36 票" ↔ "5 次夺冠"）。两个榜互补：**票数衡量受欢迎，夺冠衡量能打**。
- 实测：夺冠榜 Top3 = 5 / 3 / 2 次夺冠，共 31 张专辑当过冠军。

### ③ 创建页改成游戏大厅式「混战 / 对位」两族（用户 图4）
- 用户原话：**指定对决是对位赛的衍生玩法**，别和"手动挑选"搞混；多歌手混战是默认首选；
  UI 想要"像一些游戏的等待大厅里会有各种模式选择一样"。
- 改成**两族大厅**：
  - **混战模式**（图标 `VS`，品牌蓝，默认首选）：多歌手混战 / 单歌手内战 / 按流派·年代 / 手动挑选 —— 都是杯赛制、产生冠军；
  - **对位模式**（图标 `1v1`，青色）：经典对位赛（序号对序号）/ 指定对决 —— 两两一组、不淘汰、出对照表。
- 族色贯穿：板块顶部色带、图标块、族内选中态全用族色（蓝 / 青），当前所选模式所属的整块高亮；
  页头文案同步改成「选个模式，开一局」+ 两族一句话区别。
- **只重排 UI，底层 `mode` 值与全部现有逻辑不变**：`MODES`（`currentMode`/`cupMode` 仍从这取）改由
  `LOBBY.flatMap(...)` 派生 —— 单一数据源，不会出现两份模式表打架。

### 验证
| 脚本 | 结果 |
| --- | --- |
| `tools/pw/verify-r6.mjs`（新，真浏览器 + 真造一局打完） | **26/26** 断言 ✅ |
| `frontend` `vite build` | 11.87s 通过 ✅ |

`verify-r6` 覆盖：折叠顺序 4 块且次序正确 / 完整晋级图与小组赛默认收起 / 紧凑树 568px·卡宽 148px·46 名字仅 1 截断 /
排行榜默认票数口径、切夺冠榜后标题·说明·领奖台·列表口径全对 / 创建页两族（4+2 项）、默认落混战·多歌手混战、
族色图标 VS/1v1、点经典对位赛后整块高亮切到青色、对位赛说明块正常展开。
截图存 `tools/pw/shots/r6-*.png`。

### 回退
- 整批：`git revert <本批 commit>`
- 只回退排行榜（后端+前端）：`git checkout <本批 commit>^ -- backend/src/modules/rankings frontend/src/views/RankView.vue frontend/src/api/index.js`
- 只回退创建页大厅：`git checkout <本批 commit>^ -- frontend/src/views/BattleCreateView.vue frontend/src/styles/components.css`
- 只回退结果页/晋级树：`git checkout <本批 commit>^ -- frontend/src/views/BattleResultView.vue frontend/src/components/BracketNode.vue frontend/src/components/BracketTree.vue`

---

## 第八批（2026-09-19 凌晨 · 5 张截图反馈）· 池子严重 bug + 对位赛取色 + Edge 闪屏第三刀

改动范围：`backend/src/modules/battles/battle.service.js` + `frontend/src/{views,styles,layouts}` + `docs/`
新增脚本：`backend/scripts/verify-pool.mjs`

### ① ⭐⭐ 严重 bug：5 位歌手 × 每位 8 张 → 只出 5 张、5 场就"比完了"（用户 图4/图5）
- **真根因（查库确认）**：多歌手混战取池时是「**取所有歌手合格专辑数的最小值** 再统一截断」——
  只要有一位歌手只有 1 张合格专辑，**其他歌手的池子也全被拖到 1 张**。
  实测库里 `Chappell Roan` 就只有 1 张合格专辑 → 5 位歌手 × 1 = 5 张 → 赛程公式算出 5 场 ✓（与截图完全吻合）；
  "五大 diva"那组是某位歌手只有 4 张 → 4×5=20 张，所以**改 8 张还是 10 张赛程都不变**（约束在歌手身上，不在档位上）。
  "周王陶林方"那组每位都有十几张，所以看起来正常。
- **修复口径**：**各歌手按所选张数抽，合格专辑不够就出几张**，总数 = 各歌手出战数之和；
  全局仍封顶 32，用**轮转取张**（每位歌手轮流出一张）保证不挤掉任何歌手；
  某位歌手一张都没有 → 明确报错（不静默）。
- **前端同步改**：出战张数由"最小值×人数"改为"逐位求和（封顶 32）"；
  并新增**明示**：哪位歌手只有几张、将按几张出战 —— 不再说清楚用户就会以为"改了张数赛程没变"（正是这次的观感）；
  某位歌手 0 张合格专辑时开局按钮不亮（否则后端必报错）。
- **实测**：`verify-pool.mjs` **13/13**（含"8/6/1 → 15 张""4×10 → 32 封顶且每位都到 8""改 4 张 → 9 张""0 张 → 明确报错""真开局 15 张、步数=规划值"）；
  真浏览器实测：选 Chappell Roan(1 张)+李克勤 → **已选 9 张**、改 4 张 → **已选 5 张**、赛程 10 场 ↔ 5 场跟着变。
- **教训（用户要求"吸取经验 以后绝对不能再有"）**：涉及"取最小值/取交集"的池子逻辑，
  必须先回答"**最坏情况下池子还剩多少**"，并让 UI 把裁剪原因说出口。

### ② 对位赛专辑取色全一样（用户 图1）
- **真根因**：对位赛 / 指定对决是旧赛制，加载走 `loadLegacy()` 分支 —— 那条分支**漏调 `loadAccents()`**，
  取色缓存里没有这两张专辑 → `accentStyleOf` 全部退回同一个兜底蓝灰 `rgb(84,118,138)`。
  新赛制路径（`load()`）调了所以"默认模式取色正常"。已补一行。
- 实测：真建一局对位赛，两张专辑取到 `rgb(70,63,171)` 与 `rgb(99,130,162)`，不再相同。
- **教训**：取色是"工具函数 + 各页面各自调用"的结构，**新增加载路径时必须核对调用链**。

### ③ Edge 闪屏（用户说仍未修复）→ 对照原型补齐差距 + 给一键开关
- 用户给了关键线索："**uidemo 就没这个问题**"。实测对比：原型 `filter:blur` **0** 处、动画 **0** 处；
  而实现里 `filter:blur` 20 处、动画一堆。CSS 侧此前已全关毛玻璃 / mix-blend-mode / 极光层 / animation / will-change，
  本轮再把漏网的两处补上：`.spark` 的 `blur(0.7px)`、装饰层（`.gbeam/.vglow/.spark/.sunlit`）的 hover 过渡（会整层重绘）。
- **给一个看得见的一键开关**：Edge 下页脚出现「Edge 还闪屏？点我开稳定模式」按钮（等价于以前的 `?lite=1`），
  点一下切到最彻底的降级（连装饰光效一起关），不用再手动改网址。只在 Edge 显示，其它浏览器看不到。
- ⚠️ 我这边无 Edge 真机，**这一条只能交给用户验收**；若仍闪，请点页脚按钮开稳定模式并把现象告诉我。

### ④ 用户点名留到 UI 阶段的（本批不动）
- 对战页顶部「淘汰赛·XX」徽标与「已投 0/8 场」太小太挤 → 加大、往左上移、拉开间距；
- 全站字号整体上调一档。
（已写入守则 85 条与《进度与执行清单》31 行，避免反复返工。）

### 验证
| 脚本 | 结果 |
| --- | --- |
| `backend/scripts/verify-pool.mjs`（新） | **13/13** 断言 ✅ |
| `backend/scripts/verify-v2.mjs`（回归） | 59/59 ✅ |
| `backend/scripts/verify-undo-fix.mjs`（回归） | 36/36 ✅ |
| `backend/scripts/verify-genre.mjs`（回归） | 17/17 ✅ |
| `backend/scripts/verify-batch1.mjs`（回归） | 12/12 ✅ |
| `backend/scripts/repair-brackets.mjs`（dry） | 62 局 → 0 异常 ✅ |
| `tools/pw/verify-r7.mjs`（新，真浏览器） | **7/7** ✅ |
| `frontend` `vite build` | 10.15s 通过 ✅ |

### 回退
- 整批：`git revert <本批 commit>`
- 只回退池子逻辑：`git checkout <本批 commit>^ -- backend/src/modules/battles/battle.service.js frontend/src/views/BattleCreateView.vue`
- 只回退取色：`git checkout <本批 commit>^ -- frontend/src/views/BattlePlayView.vue`
- 只回退 Edge 降级：`git checkout <本批 commit>^ -- frontend/src/styles/components.css frontend/src/layouts/AppShell.vue`


---

## 第九批（2026-09-19 凌晨 · 用户纠错）· 恢复多歌手混战的大场玩法（总池上限 32 → 100）

改动范围：`backend/src/modules/battles/battle.service.js` + `frontend/src/views/BattleCreateView.vue` + `docs/`

### ⭐ 用户纠错（原话）
> "32张封顶 那这个10张意义在哪，我们之前上个版本并没有这样限制吧 你这样直接破坏我的100张玩法，
> 上个版本5歌手10张40多场玩的好好的，**修复bug不能破坏改变已经好了的玩法 这也是规则 而且你没问我**"

### 发生了什么
- 第八批修"取最小值"严重 bug 时，我把多歌手混战的总池封顶写成了 `MAX_POOL=32` ——
  那是**顺手改了玩法**：5 位歌手 × 每位 10 张 = 50 张的大场被砍到 32 张。
  旧版这里根本没有 32 封顶（接口本来就允许每位最多 50 张），用户"5 歌手 10 张 40 多场"就是这么玩的。
- 这是我的错：**修 bug 的改动范围越界了**，而且没有先问。

### 修复
- 多歌手混战总池上限改为 **100**（`MULTI_POOL_MAX = 100`，与"自选专辑最多 100 张"同口径）。
  单歌手档位（最大 32）与流派/年代模式（ERA_MAX_POOL=32）**维持原样不动** —— 用户没有抱怨这两处。
- 前端出战数同步改用 100；提示文案从"到 32 张封顶"改为"多歌手混战最多 100 张，单歌手档位最大 32 张"。
- 回归脚本 `verify-pool.mjs` 更新为 17 项断言，新增：**5 位 × 10 张 → 50 张 → 45 场**（"40 多场"回来了）、
  15 位 × 8 张 = 120 → 封顶 100。
- 真浏览器实测：4 位歌手 × 每位 10 张 → **已选 40 张 · 10 组小组赛 → 复活捞 12 张 → 32 强淘汰赛 · 共 42 场**。

### ⭐ 新硬规则（守则 86 条）
> **修 bug 的改动范围必须局限在"让错误行为变正确"。**
> 任何玩法参数（上限 / 张数 / 场次 / 规则 / 数值）哪怕看起来"顺手就该一起改"，也**必须先出方案问用户**。

### 验证
| 脚本 | 结果 |
| --- | --- |
| `backend/scripts/verify-pool.mjs`（更新） | **17/17** ✅（含 5×10=50 张/45 场、15×8=120→100 封顶） |
| `backend/scripts/verify-v2.mjs`（回归） | 59/59 ✅ |
| `tools/pw/verify-r8.mjs`（新，真浏览器） | **4/4** ✅（4×10 → 40 张/42 场，文案已改） |
| `frontend` `vite build` | 10.66s 通过 ✅ |

### 回退
- `git revert <本批 commit>`（恢复 32 封顶 = 恢复错误的限制，**不建议**；若真要回退请先确认玩法上限口径）

---

## 第十批（2026-09-19 上午 · 用户报手动挑选）· 可勾选池子的"未选中态"不许淡显

改动范围：`frontend/src/{views/BattleCreateView.vue, styles/components.css}` + `docs/`
新增脚本：`tools/pw/verify-r9.mjs`、`tools/pw/repro-custom.mjs`（复现脚本）

### 用户原话
> "单歌手怎么最多还是只有14张，复现后可以选超过17张 但是加入一个歌手后，这里会无法取消勾选
> 界面卡死，手动挑选模式很重要，优化优先级最高"

### 排查过程
- 真浏览器按同样步骤复现（加载周杰伦 → 勾 5 张 → 加载陶喆 → 取消勾选 → 加载林俊杰）：
  **勾选/取消/累积/响应全部正常、无报错** —— 说明不是逻辑死循环，而是**视觉语义**问题。
- **查样式找到真根因**：可勾选池子里 `.pk.off .art { opacity: 0.32; filter: grayscale(0.6) }` ——
  **未勾选的封面 0.32 透明 + 灰度**，看起来就像"这张不存在"：
  · 17 张里只看到清晰的 14 张 → 读成"怎么最多只有 14 张"；
  · 点一下取消勾选 → 整张**直接隐身** → 读成"取消不了 / 界面卡死"。

### 修复
- **未选中态正常显示封面**（opacity 1、无灰度），选中态用**蓝色对勾 + 品牌描边**表达。
  新增 `.pickpool` 作用域类，只作用于「手动挑选」和「自己挑」这两个可勾选池子；
  参赛池预览（`.pool` 无 pickpool）的淡显是「盲盒未揭晓」，**有意设计，保持不变**。
- **勾选工具条**：`已选 N / 100 张 · 池里共 X 张` + 「清空选择」+「全选池里 X 张」——
  任何时候都知道选了几张，"觉得卡了"也有一键清空这个出口。
- **加载反馈**：拉一位没缓存过的歌手要打一次 iTunes + 准入过滤（好几秒），
  以前点了候选 chip 毫无反应 → 像卡死。现在 chip 显示「加载中… / 正在拉 TA 的专辑」并禁用重复点击。
- **健壮性**：勾选状态统一按数字比较（`isPicked`/`toggleCustom` 全部 Number 归一），
  彻底排除字符串/数字类型不一致导致的"点不动"一类问题。

### 新硬规则（守则 87 条）
> **一个状态如果只能靠"变透明/变灰"表达，用户就会读成"这个东西没了"。**
> **可交互的未选中态必须保持完整可见，用徽标/描边表达选中。**

### 验证
| 脚本 | 结果 |
| --- | --- |
| `tools/pw/repro-custom.mjs`（复现脚本） | 旧代码下勾选/取消/累积/响应全部正常（确认是视觉语义问题） |
| `tools/pw/verify-r9.mjs`（新） | **10/10** ✅（17 张全选/取消/来回切换、第二位歌手累积后仍可取消、清空、未选中 opacity=1、工具条计数同步） |
| `frontend` `vite build` | 9.53s 通过 ✅ |

### 回退
- `git revert <本批 commit>`
- 只回退样式：`git checkout <本批 commit>^ -- frontend/src/styles/components.css`
- 只回退交互：`git checkout <本批 commit>^ -- frontend/src/views/BattleCreateView.vue`

---

## 第十一批（2026-09-19 上午 · 用户纠正）· 设计恢复：未选中=灰+透明，蓝色对勾只给可勾选池子

改动范围：`frontend/src/styles/components.css` + `docs/`
新增脚本：`tools/pw/verify-r10.mjs`

### 用户原话
> "现在勾选功能确实没问题 但是上个版本那种未被选中的是灰和透明的效果很好啊 选中被点亮这个设计是对的
> **我让你解决的是bug 不是我们的设计**"
> "这两个功能加的很好 不过字体稍微明显点"
> "除了可以自己手动挑选的模式专辑选择右上角有蓝色勾选 其它不能勾选的不要有这个勾选设计 会让用户以为可以取消"

### 改动
1. **恢复设计**：可勾选池子（手动挑选 / 自己挑）**未选中 = 灰 + 透明（opacity .32 + 灰度）、选中 = 点亮**
   （蓝色对勾 + 2px 品牌描边）。上一批我曾误把这套视觉当 bug 改掉，本批撤回。
   之所以不会再误读成"只有 N 张"：勾选工具条把「已选 N / 100 张 · 池里共 X 张」写得明明白白。
2. **蓝色对勾徽标限定在 `.pickpool` 作用域**：参赛池预览等**不能勾选**的专辑网格一律不出现对勾
   （用户："会让用户以为可以取消"）。
3. **工具条字体加大**：正文 14.5px、数字 16.5px、按钮 13.5px/600。
4. **保留上一批的功能性修复**：歌手加载中显示"加载中…"、勾选状态统一按数字比较、清空/全选。

### ⭐ 硬规则（替换守则 87 条的旧表述）
> **报 bug 时先分清「bug」与「设计」。修 bug 只动逻辑，不顺手动视觉；
> 拿不准就在方案里写明"这块我会动 / 不会动"。**

### 验证
| 脚本 | 结果 |
| --- | --- |
| `tools/pw/verify-r10.mjs`（新） | **7/7** ✅：未选中 opacity=0.32、选中 outline=2px+对勾可见、未选中对勾隐藏、工具条字号≥14px、参赛池预览 0 个可见对勾 |
| `frontend` `vite build` | 10.72s 通过 ✅ |

### 回退
- `git revert <本批 commit>`（会回到"未选中正常显示"的中间态，不建议）

---

## 第十二批（2026-09-19→20 · Edge 窗口层根因 + P1 歌手图/介绍 + 字号第一轮）

改动范围：`start-dev.bat`（GBK 字节级改） + `backend/src/modules/music/music.service.js` + `frontend/src/{main.js,styles,layouts,views,components}` + `docs/`
新增：`frontend/src/components/ArtistChip.vue`、`tools/pw/verify-r11.mjs`

### ① Edge「最小化就闪」——真凶在**窗口层**，不在页面
- **页面侧实测归零**：用 Playwright 把 UA 伪装成 Edge（`Edg/130`），`html.ua-edge` 生效后逐项数页面还在吃 GPU 的东西：
  **毛玻璃 0 · 滤镜 0 · 动画 0 · 混合模式 0 · 全屏固定层 0** —— 页面已经彻底安静。
- **对照用户给的参照物**："UI 原型不闪"。原型是**双击 html** 打开的；app 是 `start-dev.bat` 起的。
  差别就在这里：启动器开了**两个黑色日志窗**，而且自己用 **`pause` 一直停在前台**。
  点最小化浏览器时，Windows 把焦点交给下一个窗口 —— 就是那个黑窗；表现就是
  "点最小化一直切 / 回不到桌面"。
- **修复**：`start-dev.bat`（GBK+CRLF 字节级改）
  · 后端 / 前端两个日志窗 → `start ... /min`（最小化到任务栏，不再抢前台）
  · 启动器 **5 秒后自动退出**（去掉 `pause`），不再有黑窗留在桌面上
- **页面侧再加一档「极简模式」+ 页脚 GPU 自检读数**：完整 → 稳定 → 极简 三档一键切换；
  读数显示 `毛玻璃 / 滤镜 / 动画 / 混合 / 全屏固定层`，**全 0 会变绿**——
  用户自己就能确认"页面到底还有没有东西在吃 GPU"。
- ⚠️ **新硬规则（守则 89）**：报"页面卡/闪"之前，先确认**窗口与进程环境**，并拿"能正常工作的参照物"对比。

### ② P1 · 歌手图片 + 介绍（用户点名"p1里的内容歌手图片介绍"）
- **事实**：iTunes Search API 的 `musicArtist` 结果**没有歌手头像**（Apple Music API 有但要开发者密钥）。
- **做法**：后端 `searchArtists` 富化每位候选 ——
  · 曲库已缓存 → 头像取**较新的一张合格专辑封面**（零额外请求）
  · 未缓存 → 去 iTunes 查该歌手专辑 1 次（并发 3，礼貌限流），头像取第一张有封面的正式专辑
  · 简介**只用真实字段拼**：`流派 · 曲库 N 张正式专辑 · 年代跨度`（未入库则 `代表作《X》· 未入库`）
    —— 不写"著名歌手/殿堂级"这类空话
- 前端新增 `ArtistChip.vue`（头像 + 姓名 + 简介 + 动作），创建页三处候选列表（多歌手/单歌手、手动挑选、指定对决）统一使用。
- 实测：搜"周杰伦"→ 4 张卡都带真封面，简介如 `國語流行樂 · 曲库 17 张正式专辑 · 2000–2026`。

### ③ 第一轮基础前端优化：字号（用户点名）
- 字阶整体上调一档：`--fs-body 14→15`、`--fs-sm 13→14`、`--fs-xs 12→13`、
  `--fs-h3 17→18`、`--fs-h2 22→24`、`--fs-h1 30→32`、`--fs-display 44→48`。
- 组件里**写死的 px** 一并上调（脚本可复跑，映射：9.5/10/10.5→12、11→12.5、11.5→13、12→13、12.5→13.5、12.8→14、13→14、13.5→14.5、14→15），共 **211 处**。
- 实测：正文 15px、**全站没有任何小于 12px 的文字**（最小 12px）。**新硬规则（守则 88）：文字不得小于 12px。**

### 验证
| 脚本 | 结果 |
| --- | --- |
| `tools/pw/verify-r11.mjs`（新） | **15/15** ✅：歌手卡有图有简介 / 正文 15px 且无 <12px 文字 / Edge 三档切换 / GPU 读数全 0 |
| `start-dev.bat` 字节复核 | GBK 编码 ✓ · CRLF 86 ✓ lone LF 0 ✓ · 无 BOM ✓ · `/min` ×2 ✓ · 无 `pause` ✓ |
| `frontend` `vite build` | 9.43s 通过 ✅ |

### 回退
- 只回退启动器：`git checkout <本批 commit>^ -- start-dev.bat`（注意该文件是 GBK，用字节级方式还原）
- 只回退字号：把 tokens 的 7 个 `--fs-*` 改回，并反向跑一次字号脚本（映射表在改动记录里）
- 只回退歌手卡：`git checkout <本批 commit>^ -- backend/src/modules/music/music.service.js frontend/src/components/ArtistChip.vue frontend/src/views/BattleCreateView.vue`
